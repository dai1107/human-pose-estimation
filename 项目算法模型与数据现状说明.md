# 项目算法、模型、数据现状与下一步优化说明

> 项目阶段：Alpha  
> 当前目标：优先判断人体动作是否正确，暂不把场地、器械重量、比赛距离、墙球命中高度等比赛条件作为主要优化对象。

> 实时优化快照（2026-08-11）：任务清单第 1–12 轮已完成第一版工程实现和现有数据验证；第 13–14 轮离线 OpenCap 参考、目标设备与最终发布验收尚未完成。

本说明系统审阅了仓库中的源代码、配置、测试、现有说明、数据 manifest、结构化标注和评估报告。视频、ONI、模型权重、逐帧缓存等二进制或大规模资产，按其 manifest、审计报告和抽样结果整理，没有重新逐字节读取或人工重看每一帧。

## 1. 先说结论

当前程序不是一个“直接看 RGB 视频就理解动作”的端到端人工智能模型。它的正式工作方式是：

1. 用 MediaPipe 从每帧 RGB 图像中提取 33 个人体关键点。
2. 根据关键点计算膝、髋、肘、躯干角度，以及人体相对位置、速度、可见度和身体尺度。
3. 用每个动作独立的状态机识别“站立、下降、最低点、起身、落地”等阶段。
4. 用人工编写的规则判断触地、伸展、双脚同步、补步等要求。
5. 证据充分时输出 `VALID` 或 `NO_REP`，证据不足时输出 `UNSURE`。

这套方法的优点是可解释、能实时运行、每条错误都能说明原因。它的主要瓶颈也很明确：**程序实际主要在看二维骨架，而不是直接看人体外观和真实接触。**

因此，当前效果不理想不只是某几个阈值不合适，而是以下问题叠加造成的：

- Burpee 胸部触地、Lunge 后膝触地、脚掌起落等关键事件，在 33 个姿态点中没有直接的“接触传感器”。
- 核心精标数据中，Burpee 和 Lunge 全是正面视角，正面视角又最难看出前后深度和是否真正触地。
- 原始姿态虽然有 96.76% 的帧能检出人体，但全身完整可见的帧只有 25.18%。
- 30 条手机视频均已有终态人工结论，其中 29 条形成可用精标；核心 15 条仍只有 70 个官方动作次数，而且同一批数据既用于调阈值又用于评估。
- 所有可用 RGB 的 rep/分析周期、阶段、事件和错误结果已进入审计覆盖层；派生标注中的历史 AI proposal 备注已更新为“已人工核对”。
- 当前阈值放宽提高了候选检出量，但放宽阈值不能凭空补出被遮挡的脚、远侧膝或真实胸部接触证据。

所以，接下来不应继续只做全局阈值微调。当前进度和后续优先级应当是：

1. 已清洗标注，并建立当前可用的内部回归集、数据角色和泄漏检查。
2. 已按动作、视角、规则、角色和临时 subject 拆分报告；独立校准须等待核心
   validation/hold-out。
3. 下一步补直接接触真值，再训练 Burpee 胸部接触、Lunge 膝触地和脚部事件的局部 RGB
   证据头。
4. 样本量和冻结验证主体足够后，再加入轻量时序模型学习阶段和事件，保留现有规则层负责
   解释和最终裁决。
5. 优先补多主体、多视角、自然错误和边界动作数据。

### 1.1 本轮可执行任务的完成边界

现有代码和数据能够安全完成的工程项已经收口：

- Codex 1 已完成：30 条手机 RGB 的终态复核已导入，29 条可用精标具有结构校验、来源哈希和修改审计；高人工分歧队列延期，不再作为导入门。
- Codex 2 已完成到当前数据允许的范围：统一报告已包含候选、计数、状态、规则、事件、数据角色、临时 subject 和泄漏检查指标。
- Codex 3 已完成：已生成 75 个不超过 1 秒的 TP/FP/FN/`UNSURE`/状态不匹配短片段，并记录数据、实现代码和配置哈希。
- Codex 4 的现有姿态、地板和脚部规则链已实现并通过回归；局部 RGB 接触分类头仍缺独立的胸/膝/脚接触真值，不能宣称完成。
- Codex 5 已部分完成：已有身体尺度归一化、毫秒约束，以及动作方向/主侧在候选内锁定；个体可靠站立角基线和独立分组校准仍缺核心 validation/hold-out。
- Codex 6–8 暂不具备可信启动条件：分别缺足量冻结训练/验证样本、每个动作 × 视角的 5–10 个人工模板，以及 idle/transition/unknown/OOD/多人干扰数据。
- 实时优化第 1–11 轮已完成第一版：统一性能指标、Latest-Frame、显示/分析双滤波、推理专用分辨率、统一 2D/3D 指标、3D 可靠性、人体坐标、时序地板估计、过期姿态抑制和自动负载控制均已接入。
- 第 12 轮角度验证框架与现有 150 条人工标注基线已完成，但缺 30°/45°标定视角和成对旧新版程序事件帧；严格 MAE/P95 非回归未通过，3D/canonical 3D 不得提升为正式规则输入。

当前发布门槛远未达到。此结论不是“还应继续在同一批数据上调参”，而是应停止把内部回归提升解释为独立泛化，并优先补新主体和直接接触证据。

## 2. 当前程序到底是什么

### 2.1 正式运行链路

```text
手机/摄像头/视频最新帧
        ↓
MediaPipe Pose Landmarker（正式分析默认 Full）
        ↓
33 个 image landmarks
+ 可选 segmentation mask
+ MediaPipe world landmarks
        ↓
显示/分析两套独立 One Euro
（真实时间戳；显示副本不能回流规则）
        ↓
统一 JointMetric：raw/filtered 2D/3D
+ 骨长/速度/脚接触可靠性
+ body canonical 3D + 时序 GroundEstimator
        ↓
每个动作独立的阶段状态机
        ↓
接触、脚部事件、伸展、同步、补步等动作规则
        ↓
单条规则：PASS / FAIL / UNSURE
        ↓
动作候选：VALID / NO_REP / UNSURE
        ↓
按动作 × 视角 × 规则进行可观测性门控
        ↓
实时反馈、计数和分析报告
```

正式姿态配置在 `configs/product_pose.yaml`。当前关键约束是：

- 后端为 `mediapipe`。
- 正式分析模型为 `full`。
- Lite 主要用于速度和等价性实验，不是默认高精度分析模型。
- 分析流使用自适应 One Euro 平滑。
- 分析流明确禁止预测，避免“预测出来的未来姿态”进入正式动作判定。
- 桌面摄像头和有窗口视频使用单槽 Latest-Frame；姿态结果携带 `frame_id` 和时间戳，超过 120 ms 或允许帧差后不再绘制或分析。
- 桌面自动负载只调 pose FPS、推理分辨率和可选分析频率，不降低视频播放或渲染 FPS。

网页显示骨架有另一条仅用于视觉体验的显示链路，可以使用更激进的平滑和最多 45 ms 的短时预测。**显示骨架不能回流到动作规则和正式报告。**

网页采用 local-first 思路：浏览器优先在本机执行 MediaPipe，再把 image/world landmarks 交给 Python 动作规则；需要时才使用服务器回退。

### 2.2 输入、特征和输出

程序真正使用的主要输入不是原始 RGB 像素，而是姿态模型生成的关键点：

- `x、y`：关键点在图像中的归一化位置。
- `z`：MediaPipe 内部的相对深度表示。
- `visibility/presence`：模型对关键点可见性和存在性的估计。
- world landmarks：以人体为中心的相对三维点，不是相机坐标，也不是可靠的真实米制坐标。
- segmentation：人体区域的概率分割，可辅助部分接触判断。

随后计算：

- 左右膝、髋、肘、肩角度。
- 躯干相对竖直或水平的角度。
- 肩、髋、脚、腕的相对位置。
- 关键点速度、角速度和阶段变化。
- 身高、腿长、脚长等相对人体尺度。
- 关键点置信度和缺失掩码。
- 脚、膝、胸部相对局部地板线的位置。

最终输出分两层：

| 层级 | 可能结果 | 含义 |
|---|---|---|
| 单条规则 | `PASS` | 有足够证据认为通过 |
| 单条规则 | `FAIL` | 有足够证据认为失败 |
| 单条规则 | `UNSURE` | 证据不足，不能可靠判断 |
| 单条规则 | `NOT_APPLICABLE` | 当前情况不适用 |
| 整次动作 | `VALID` | 所有必需规则通过 |
| 整次动作 | `NO_REP` | 至少一条必需规则明确失败 |
| 整次动作 | `UNSURE` | 没有明确失败，但至少一条必需规则证据不足 |

这一设计是合理的：看不清时应该拒绝武断判断。但如果关键点经常缺失，就会产生较多 `UNSURE`，并拉高当前使用的严格错误率。

## 3. 当前使用了哪些模型

### 3.1 正式产品模型

| 模型/算法 | 当前角色 | 是否进入正式默认判定 |
|---|---|---|
| MediaPipe Pose Full | 从 RGB 提取 33 点姿态、分割和相对 world landmarks | 是 |
| 自适应 One Euro | 平滑分析关键点，降低抖动且保持因果性 | 是 |
| 动作状态机 | 识别每个动作的阶段顺序 | 是 |
| 人工动作规则 | 判断深度、伸展、接触、同步、补步等 | 是 |
| 可观测性门控 | 证据不足时输出 `UNSURE` | 是 |
| Latest-Frame + 姿态年龄门控 | 丢弃旧推理帧和过期结果，保持视频/摄像头时钟 | 是 |
| `RealtimeBudgetController` | 按 P95 推理、姿态年龄和队列动态控制桌面推理负载 | 是 |
| 统一 `JointMetric` / `GroundEstimator` | 保存 2D/3D、可靠性、canonical 坐标和时序地板辅助证据 | 是；正式阈值仍以 2D 为主 |

### 3.2 已实现但目前属于实验、辅助或影子链路

| 模型/算法 | 当前用途 | 当前限制 |
|---|---|---|
| YOLO11n Pose | 17 个 COCO 点的实验性姿态/主体锁定 | 脚部细节少，不是正式动作主链 |
| YOLO11n 人体检测 ROI | 先锁人，再对人体区域跑姿态 | 当前精度和端到端延迟门未通过，默认关闭 |
| YOLO + MediaPipe | YOLO 锁定同一个人，MediaPipe 补脚和 world landmarks | 实验后端，不是默认链路 |
| YOLO + RTMW WholeBody | 133 点 WholeBody 姿态实验 | 模型大、部署复杂；约 229 MB ONNX 不在 Git |
| MediaPipe 3D Assist | 用 3D 膝、髋、肘、肩角度辅助 2D 判断 | 只能增强或降低置信度，不能替代接触和地板规则 |
| 参考动作 + DTW | 将个人动作与参考片段做时序对齐比较 | 当前黄金视频没有模板，CLI 多模板能力未完整落地 |
| 自动动作识别 | 多项 Logistic Regression + 温度校准 + 滞回切换 | 只有代码和契约，没有合格训练集和正式模型 |

ROI 实验的结果说明了一个重要问题：人体检出率从 96.76% 小幅提高到 97.27%，但跨方案关节误差 P95 达到人体尺度的 9.82%，端到端延迟门也没有通过。因此 ROI 仍保持关闭。**检出更多人不等于关键点足够准确。**

### 3.3 3D Assist 的正确理解

MediaPipe world landmarks 能帮助判断某些关节角度，但它不是“真实世界三维测量”：

- 它是人体相对坐标，不是相机坐标。
- 相机内参仍是估计值。
- 当前没有真实 2D/3D 姿态真值可用于验证绝对误差。
- 它不能直接证明胸部、膝盖或脚掌与地面发生物理接触。

当前 3D Assist 的策略是：

- 2D 与 3D 一致：最多提高置信度。
- 2D 与 3D 严重冲突：降级为 `UNSURE`。
- 3D 缺失：完整回退到 2D。

这比让不可靠的相对 3D 直接推翻 2D 结果更安全。

第 6–10 轮进一步增加了历史骨长中位数、左右骨长一致性、孤立速度异常、脚接触置信度、以髋中心为原点的人体正交坐标系、时序地板估计以及帧号/源时间戳同步。它们只修正可靠性、增加辅助证据或输出 shadow 指标，不强行移动骨架，也不覆盖正式规则阈值。

第 12 轮会同时导出 raw/filtered 2D、raw/filtered 3D、canonical 3D 和 selected-rule angle。现有 150 条人工投影 2D 标注中，代理旧版与新版比较的 Median/P90 有改善，但 MAE/P95 分别退化 `0.136°/1.5032°`，严格非回归未通过。人工投影 2D 也不是空间 3D 真值，因此当前不能宣称 3D 绝对角度准确。

### 3.4 DTW 参考动作工具

仓库已经具备以下能力：

- 提取角度、速度、角速度和稳定性特征。
- 左右镜像规范化。
- 将不同长度动作重采样到固定长度。
- 使用约束 DTW 对齐，默认窗口比例为 0.15。
- 输出整体距离、逐特征误差和高误差时间段。

但它目前还不是完整的生产评分系统：

- 当前 8 个黄金视频没有配置 DTW 模板。
- CLI 当前主要支持个人单参考片段。
- 多模板比较和模板 CLI 支持仍不完整。
- 单个“标准视频”不能代表所有正确人的动作速度、幅度和身体比例。
- `动作评分标准实现方案.md` 中的四层评分更多是设计方案，不代表所有评分已落地。

正确的后续方向是：每个“动作 × 视角”准备 5–10 个经人工确认的标准模板，按阶段比较，而不是用一个视频作为唯一标准答案。

## 4. 八个动作目前分别怎么判断

### 4.1 能形成 `VALID / NO_REP / UNSURE` 候选的三类动作

| 动作 | 主要阶段 | 当前必需规则 | 当前最大限制 |
|---|---|---|---|
| Lunge | `stand → descent → bottom → ascent → stand` | 后膝触地、膝完全伸展、髋完全伸展、左右腿交替、无补步/碎步 | 后膝接触是 2D 地板代理；正面视角深度压缩 |
| Wall Ball | `stand → squat_down → bottom → drive → throw_extension` | 高位起始、髋低于膝、向上完全伸展、双手投掷代理 | 只判断人体投掷动作，不判断球、命中和目标高度 |
| Burpee Broad Jump | `chest_down → step/jump in → takeoff → flight/move → landing` | 胸部触地、双脚同步起跳/落地、起跳/落地错位、无补步、手脚距离、向前跳 | 胸部触地和脚部事件都依赖代理证据 |

#### Lunge 当前逻辑

Lunge 现在不会在刚从最低点上升时立刻结算，而是在以下边界之一结算：

- 已确认完全伸展；
- 下一次下降开始；
- 视频流结束。

这解决了“还没真正站直就提前算完一次”的主要逻辑问题。正面视角要求双侧证据，侧面视角允许使用更可靠的一侧关节链。

目前配置中的完全伸展阈值为膝和髋 165°，并按灵敏度要求保持 1–3 帧。下一步更适合改为“相对该受试者本段站立基线”，例如以本人可靠站立角度减去容差，而不是所有人永远共用同一个绝对角度。

#### Wall Ball 当前逻辑

正面视角仍要求双侧；side、rear、oblique 视角会固定选择更可靠的一侧，而且阶段触发和最终规则使用同一侧策略，避免“阶段看左侧、结算看右侧”的不一致。

当前主要阈值包括：

- tall start 膝角 150°、髋角 150°；
- full extension 膝角和髋角 152°；
- 髋低于膝的归一化 margin 为 -0.02；
- 手腕上升量：普通视角 0.08 个身体高度，rear 为 0.03；
- 手腕胸部带宽：普通视角 0.35 个身体高度，rear 为 0.48。

这些阈值已经比历史说明文档中的旧值更宽松。当前应以 `configs/hyrox/wall_ball.yaml` 和实际代码为准。

#### Burpee Broad Jump 当前逻辑

Burpee 已经把同一帧增强后的地板估计继续传给：

- 左右脚支撑和起落事件；
- 手部相对地板证据；
- 虚拟胸部表面与地板的接触证据。

最后一候选也可在视频流结束时结算，避免最后一个动作因没有下一次起始动作而丢失。

但“胸部触地”仍不是直接测量。程序使用肩、髋和躯干构造虚拟胸部表面，再结合地板距离、低速停留和可选 segmentation 判断。人体关键点中没有真正的胸骨/乳头线接触标签，因此该规则仍是最需要 RGB 局部模型补强的部分。

### 4.2 其他五类动作的真实语义

| 动作 | 当前计数语义 | 能做什么 | 不能宣称什么 |
|---|---|---|---|
| Rowing | `analysis_cycle` | 识别划船周期和持续早站起代理 | 不是官方有效次数 |
| SkiErg | `analysis_cycle` | 识别滑雪机动作周期 | 不是官方有效次数 |
| Sled Push | `analysis_cycle` | 识别推行运动周期 | 不能判断雪橇重量和比赛距离 |
| Sled Pull | `analysis_cycle` | 识别拉行动作周期、跪姿/坐姿代理 | 不是官方有效次数 |
| Farmers Carry | `continuous_monitor` | 连续监控手臂伸展、手臂是否位于身体侧面 | 不产生动作次数，不能判断重量和赛道距离 |

其中 Rowing、SkiErg、Sled Push、Sled Pull 的通用 `rep_count` 为兼容统一结果结构而存在，实际含义是分析周期。代码明确给出 `official_rep_count_supported: false`。Farmers Carry 是连续监控，周期和次数始终为 0。

## 5. 通用动作算法组件

| 组件 | 主要文件 | 功能 |
|---|---|---|
| 2D 特征 | `hyrox/features.py` | 角度、中心点、人体比例、左右侧置信度等 |
| 局部地板 | `hyrox/floor_reference.py` | 根据可用脚点构造图像中的局部地板线 |
| 身体接触 | `hyrox/contact.py` | 膝/胸虚拟表面、进入/退出双阈值、速度和持续帧 |
| 脚部事件 | `hyrox/foot_events.py` | 支撑、腾空、起跳、落地、同步、错位和补步 |
| 阶段与候选 | `hyrox/base.py` | 阶段确认、允许的可选阶段、候选生命周期 |
| 规则结算 | `hyrox/validity.py` | 汇总 `PASS/FAIL/UNSURE` 并执行可观测性门控 |
| 视角策略 | `hyrox/view_policy.py` | 正面双侧、侧后方可靠侧等策略 |
| 统一关节指标 | `src/biomechanics/joint_metrics.py` | raw/filtered 2D/3D、来源、置信度和可观测性 |
| 3D 可靠性与人体坐标 | `src/biomechanics/kinematics_3d.py`、`body_coordinates.py` | 骨长、速度、脚接触、canonical 坐标和 shadow 角度 |
| 时序地板估计 | `src/biomechanics/ground_estimation.py` | 稳定足部样本、地板置信度和接触辅助证据 |
| 实时帧与预算 | `src/realtime/latest_frame.py`、`scheduler.py`、`budget.py` | 单槽最新帧、异步姿态、过期门控和动态降载 |
| 动作实现 | `hyrox/actions/*.py` | 八项动作各自的状态机和规则 |
| 动作参数 | `configs/hyrox/*.yaml` | 动作阈值、持续帧、容差和反馈限制 |

当前可观测性基础门槛为：

- 必需关键点置信度：0.60；
- 整次动作平均置信度：0.65；
- 决定性规则置信度：0.72。

程序已经支持 `动作__视角__规则` 的覆盖值。Burpee front、Lunge front、Wall Ball side/rear 已设置更宽松的独立门槛。这个方向是对的，后续不应再回到“所有动作、所有视角、所有规则共用一个全局门槛”。

## 6. 已实现、实验中和未真正完成的功能

| 状态 | 内容 |
|---|---|
| 正式已用 | MediaPipe Full、33 点姿态、Latest-Frame、显示/分析双滤波、过期姿态门控、自动推理预算、八动作状态机、规则结算、可观测性门控、网页/桌面实时分析 |
| 已用但仍需提高准确率 | Burpee/Lunge/Wall Ball 的有效性候选、地板/接触/脚部事件、按视角的左右侧策略 |
| 实验或影子功能 | YOLO Pose、YOLO ROI、YOLO+MediaPipe、YOLO+RTMW、3D/canonical shadow 指标、自动动作识别 shadow、DTW 参考比较 |
| 只有工程框架或契约 | 自动选择八种动作并可靠切换、unknown/OOD 拒绝的正式模型 |
| 尚未完整落地 | 大规模监督时序模型、多模板分阶段 DTW 生产评分、可靠 RGB 接触分类头 |
| 当前明确不做 | 场地合法性、实际器械重量、官方距离、墙球是否命中和真实目标高度 |

需要特别注意：软件工程测试通过，只能说明接口、状态转换和历史行为没有被意外破坏，**不等于真人动作准确率已经达标**。

## 7. 当前数据资产

### 7.1 手机 RGB 数据

Round 6 的正式清单包含：

| 项目 | 数量 |
|---|---:|
| 真实 MP4 | 30 条 |
| 总帧数 | 15,515 帧 |
| 总时长 | 517.17 秒，约 8.62 分钟 |
| “标准录制意图” | 17 条 |
| “错误录制意图” | 13 条 |
| 原始 Pose 检出率 | 96.76% |
| 全身完整可见率 | 25.18% |

动作分布：

| 动作 | 视频数 |
|---|---:|
| Burpee Broad Jump | 7 |
| Farmers Carry | 3 |
| Lunge | 5 |
| Rowing | 3 |
| SkiErg | 2 |
| Sled Pull | 2 |
| Sled Push | 5 |
| Wall Ball | 3 |

视角分布：

| 视角 | 视频数 |
|---|---:|
| front | 13 |
| side | 11 |
| oblique_rear | 4 |
| rear | 2 |

这 30 条数据中的大多数是单主体或极少主体数据。考虑到现有视频较少，当前采用临时主体分组：SkiErg 和 Sled Push 各按两个 `subject_id` 分组，其余每个动作暂按一个 `subject_id` 分组。该分组用于当前内部开发/验证组织，不代表跨动作身份已经独立核验，也不能替代后续新受试者 hold-out。

Round 7 的主体跟踪结果：

- 15,166 / 15,515 帧成功锁定目标，比例 97.75%。
- 5,640 帧存在其他人并生成 ignore mask。
- 生成并复核了 3,235 个 crossing proposals。
- 记录 2 个疑似身份切换。
- 没有独立第二名人工逐帧复核，因此身份切换漏检率仍未知。

Round 8 的姿态缓存：

- Lite 成功 15,071 帧，正式目标绑定 14,790 帧。
- Full 成功 14,766 帧，正式目标绑定 14,721 帧。
- 有 2,439 个高分歧或需要复核的帧。
- Lite 与 Full 的比较只能反映后端一致性和可用性，因为没有真实 2D/3D 关键点真值。

### 7.2 人工标注数据

最新 reviewer A 数据包包含：

- 30 条手机视频动作类别确认。
- 29 条可用区间确认。
- 29 条可用视频精标。
- 214 个官方 rep 或非核心分析周期。
- 943 个阶段/错误区间。
- 804 个事件。
- 一致性警告 0。
- 29 条可用于内部 RGB 规则校准。
- 29 条可用于内部监督模型实验；由于数据量、主体信息和独立划分仍不足，不能据此宣称生产泛化。

15 条核心精标的分布极不均衡：

| 动作和视角 | 视频数 |
|---|---:|
| Burpee front | 7 |
| Lunge front | 5 |
| Wall Ball side | 2 |
| Wall Ball rear | 1 |

标注的当前状态和剩余限制：

1. 30/30 条手机 RGB 已有终态人工结论；29 条可用精标中的 AI proposal 只表示来源，不再表示待审核。
2. 29 条可用数据已标记为 `reviewed_ground_truth`，可用于内部规则校准和监督模型实验；`phone_sled_push_005` 按人工“视频不可用”结论排除。
3. 本项目当前不再安排第二名独立复核者，因此不计算双人复核一致性；发布判断改由单人审计记录、结构一致性检查和冻结 hold-out 结果共同约束。
4. 所有手机标注的 `OBSERVABLE` 是由统一用户声明覆盖的。这只能说明“原始 RGB 人眼可看”，不能说明“MediaPipe 关键点也可靠可看”。
5. `phone_lunge_004` 的 revision 2 审计缺口继续保留在审计字段中，但不再把已确认的最终标注判为不可信。
6. 现有视频仍同时参与规则/模型优化和回归评估，存在校准/评估泄漏，不能当作独立测试集。

### 7.3 旧黄金视频

`configs/hyrox_golden_videos.json` 中的 8 条黄金视频是软件回归样例：

- 它们冻结的是当前程序允许输出的范围。
- 它们不是独立人工动作真值。
- 多个核心动作的黄金预期本身就是 `NO_REP` 或 `UNSURE`。

因此它们适合检查“改代码后有没有破坏历史行为”，不适合计算准确率，也不能证明跨人、跨视角泛化。

### 7.4 ONI Depth/IR 数据

当前 ONI 资产包括：

| 项目 | 数量/状态 |
|---|---|
| ONI 记录 | 32 条 |
| Depth/IR 流 | 64 路 |
| 采样主体 proposal | 1,536 个 |
| Color 流 | 0 |
| 人工复核 modality | 0 |
| 人工确认主体 | 0 |
| 已验证动作错误 | 0 |

现有 ONI 与手机 RGB 没有时间、空间或主体配对，也没有 RGB-depth 标定、地面平面和身体接触标注。因此：

- 不能把 IR 当作 RGB。
- 不能把手机帧硬匹配到 ONI 帧。
- 不能将 ONI 轮廓直接当作手机 RGB 的阶段、接触或错误真值。
- 不能用未配对深度数据做“教师真值”蒸馏。

现有 ONI 仍可用于：

- 验证 Depth/IR 解码、采样和跟踪工具链。
- 设计下一轮同步 RGB-D 补拍方案。
- 研究粗轮廓、深度表面距离和地板/接触假设。

若要真正服务手机 RGB 动作判断，需要重新采集硬件同步、完成标定的 RGB-D，或者同步双视角 RGB。

## 8. 当前评估结果应该怎么理解

最新优化后核心 RGB 评估为：

| 指标 | 结果 |
|---|---:|
| 人工 rep | 70 |
| 程序候选 | 57 |
| 计数完全一致的视频 | 4 / 15 |
| 计数和状态都完全一致的视频 | 0 / 15 |
| 成功时序对齐的候选 | 52 / 70 |
| 状态匹配 rep | 17 / 70 |
| candidate recall | 74.29% |
| candidate precision | 91.23% |
| count MAE | 0.87 rep/视频 |
| exact count rate | 26.67% |
| 匹配候选上的状态准确率 | 32.69% |
| 明确结论覆盖率 | 34.62% |
| `UNSURE` rate | 65.38% |
| 严格 rep 状态错误率 | 75.71% |
| 有标注且当前支持的错误视频 | 10 |
| 成功检出的错误视频 | 2 / 10 |

分动作和视角：

| 动作和视角 | 人工 rep | 程序候选 | 严格状态错误率 |
|---|---:|---:|---:|
| Burpee front | 22 | 16 | 86.36% |
| Lunge front | 29 | 24 | 93.10% |
| Wall Ball rear | 7 | 6 | 57.14% |
| Wall Ball side | 12 | 11 | 25.00% |

当前少数有明显信号的错误规则：

| 错误 | TP | FP | FPR |
|---|---:|---:|---:|
| `HEEL_RISE` | 1 | 0 | 0% |
| `NOT_DEEP_ENOUGH` | 1 | 2 | 14.29% |
| `FOOT_DESYNCHRONIZED` | 0 | 1 | 7.14% |
| `HIP_NOT_EXTENDED` | 0 | 3 | 21.43% |

其余多项错误虽然误报率看起来是 0，但召回也接近 0。这不是好结果，而是程序几乎没有做出该类明确判断。

终端事件的 52 个匹配样本中，绝对误差中位数为 42 帧、P90 为 92 帧；5 帧内命中率和
200 ms 内命中率均只有 13.46%。因此当前主要短板不只在状态分类，还包括动作边界和事件
定位。

当前 15 条核心视频全部属于 `development`。角色清单虽已做到训练/评估记录无重叠、临时
subject 无跨角色冲突，但核心 `validation` 和 `test` 都为空，泄漏检查状态为
`no_detected_role_leakage_but_no_independent_core_holdout`。本节结果只能作为内部回归基线，
不能表述为独立验证或新主体性能。

### 8.1 “75.71% 严格错误率”不是一个纯分类指标

这个严格指标把以下情况都算错：

- 程序漏掉一个人工 rep。
- 程序多算一个候选。
- 人工是 `VALID`，程序是 `UNSURE`。
- 人工是 `NO_REP`，程序是 `VALID`。
- 候选时间边界不同导致无法正确对齐。

所以它不能直接解释为“每十次动作有八次把动作好坏分类错了”。但它仍然说明：**从发现动作、正确计数到判断状态的整条链路，目前都没有达到可用准确率。**

当前统一评估已经拆开报告以下指标；后续报告必须继续保留这些维度：

- candidate recall：人工动作中有多少被程序发现。
- count MAE 和 exact count：计数差多少。
- detected-rep conditional status accuracy：只在成功匹配的动作上看状态准确率。
- definite decision coverage：`VALID + NO_REP` 占比。
- abstention rate：`UNSURE` 占比。
- 每条规则的 precision、recall、FPR 和 FN。
- 起跳、落地、触地、完全伸展等事件的帧误差。
- 按动作、视角和 subject 分组的结果。

## 9. 为什么当前错误率仍然不理想

| 问题 | 直接原因 | 为什么只调阈值不够 |
|---|---|---|
| Burpee 胸部触地难判 | 关键点没有真实胸部接触标签，只能用肩髋虚拟表面 | 阈值变宽会把“接近地面”误当“已经接触” |
| Lunge 后膝触地难判 | 膝关节中心不等于膝盖表面，正面视角深度被压缩 | 放宽会把悬空膝误判为触地 |
| 起跳、落地和同步不稳 | 脚跟/脚尖点经常遮挡、抖动或短暂消失 | 缺失不是不同步，放宽可能制造假事件 |
| Lunge 伸展误报 | 不同人的自然站立角、镜头透视不同 | 单一绝对角度不适合所有人和视角 |
| Wall Ball 视角差异大 | rear 和 side 的手腕、肩、髋可见性不同 | 一个阈值不能同时适配所有视角 |
| 候选漏检 | 阶段状态机依赖连续可靠关键点 | 阈值只能改阶段边界，不能恢复丢失的姿态 |
| `UNSURE` 较多 | 可观测性门控保护了不可靠结果 | 直接降低门槛会把“不知道”变成错误的确定答案 |
| 评估波动大 | 样本量小、视角偏斜、错误类别只有 1–3 条 | 一条视频就能让百分比大幅变化 |
| 泛化未知 | 同一数据调参和评估，且主体信息不完整 | 在这批数据变好不代表新用户会变好 |

更根本地说，当前系统是“二维骨架 + 人工阈值 + 状态机”。它适合判断明显的关节角度和阶段顺序，但真实接触、遮挡脚部和前后深度正好是它的弱项。

## 10. 现有数据为什么有缺陷

### 10.1 量太少

约 8.62 分钟、30 条视频只能用于原型验证，无法覆盖不同人的：

- 身高、腿长、躯干比例。
- 柔韧性和自然站立角度。
- 动作速度和停顿习惯。
- 服装遮挡。
- 左右侧习惯。

### 10.2 主体和设备信息不完整

大量 device、焦距、距离、相机高度、光照、服装等 metadata 为 unknown，subject ID 也未正式冻结。结果是：

- 无法按人切分训练、验证和测试。
- 无法判断错误来自算法、某个拍摄设备，还是某个受试者。
- 同一人出现在训练和测试中时会产生隐性泄漏。

### 10.3 核心动作的视角过于单一

精标 Burpee 和 Lunge 只有 front，Wall Ball 只有 side/rear。程序无法从这些数据证明其他视角的规则有效，也无法可靠学习视角差异。

### 10.4 错误动作大多是刻意示范

刻意错误通常动作幅度大、持续时间长、很容易和标准动作分开。真实使用中的错误往往是：

- 只差一点点的边界错误。
- 一次动作同时出现两种问题。
- 疲劳后逐渐变形。
- 某一帧遮挡导致看起来像错误。
- 正确与错误之间没有清晰间隔。

只训练刻意示范，会让离线结果看起来好，但实际使用容易失效。

### 10.5 标注并非完全独立人工生成

当前精标很多时间边界最初源于 AI proposal，但用户已确认全部经过人工复审。派生标注会保留 proposal 来源和源包哈希，同时把历史“待人工核对”备注更新为“已人工核对”。因此这 15 条可用于内部规则校准和监督实验；但由于同一小批数据仍同时参与开发和回归评估，不能作为独立测试集或生产泛化证据。

### 10.6 “人眼可观察”和“姿态点可观察”被混在一起

原始 RGB 中能看清脚，不代表 MediaPipe 的 heel/toe 点稳定；人眼能看出胸部触地，也不代表肩髋构造的虚拟胸部表面可靠。因此标注至少要拆成：

- `image_observable`：人看 RGB 能否判断。
- `pose_observable`：当前关键点能否支持规则。
- `event_confidence`：事件帧本身是否有歧义。

### 10.7 缺少负背景和连续切换数据

现有数据主要是已经切好的动作视频，缺少：

- idle 和准备姿势。
- 动作之间的 transition。
- 非 HYROX 动作和 unknown/OOD。
- 部分人体入镜。
- 多人走过镜头。
- 连续切换不同动作。

因此自动动作识别目前没有合格训练数据，`--hyrox-action auto` 保持不可用是正确的。

## 11. 现有数据还能怎么继续榨取价值

### 11.1 15 条核心精标 RGB

适合：

- 按规则绘制阈值分布和错误案例。
- 调试阶段边界、触地、完全伸展、起跳和落地事件。
- 建立固定的规则单元回归案例。
- 对每次代码修改做逐 rep 差异分析。

不适合：

- 同时作为阈值开发集和独立最终测试集。
- 直接训练正式监督模型。
- 宣称对新用户、新设备和新视角泛化。

### 11.2 其余 15 条手机 RGB

这 15 条已完成终态人工复核。当前处理结果：

1. 14 条形成可用精标，`phone_sled_push_005` 因人体不完整而保留为不可用。
2. `phone_skierg_002` 的 73 个分析周期按用户明确确认记为 `VALID`。
3. 非核心动作缺失的事件从已人工核对的阶段边界确定性派生，原始导出包保持不变。
4. 当前已按临时 subject 冻结 3 条非核心 validation 记录；核心三动作仍没有独立验证记录，
   下一步应补新主体核心动作、idle、setup、transition 和局部接触证据。

### 11.3 2,439 个高分歧帧

这些帧适合做主动学习优先队列，但不是已完成 RGB 精标的导入门，当前可延期：

- 先复核 heel/toe、手腕、远侧膝和遮挡帧。
- 区分 Lite/Full 分歧、跟踪失败和真实动作边界。
- 将相邻帧扩成 0.5–1.0 秒短 clip，标注事件前后过程。

以后若启动该队列，只看单帧不足以判断起跳和触地，必须把高分歧帧扩成短时序片段。

### 11.4 Lite/Full 姿态缓存

适合分析：

- 哪些关节、动作和视角最容易缺失。
- 不同平滑方案的延迟和抖动。
- Full 和 Lite 的可用性差异。
- 哪些规则对模型后端最敏感。

不适合当作真实姿态 ground truth。

### 11.5 原始 RGB

原始 RGB 比已有姿态缓存更有潜力，因为接触信息可能仍在像素中，只是姿态点没有表达。可以从头生成：

- 胸部附近与地面的局部短 clip。
- 后膝与地面的局部短 clip。
- 左右脚起跳/落地短 clip。
- 手脚相对位置和脚跟抬起短 clip。

这些局部任务比直接训练大型全视频网络更省数据，也更容易解释。

### 11.6 ONI 和 DTW

- ONI：用于设计未来同步 RGB-D 采集和研究工具链，不混入手机 RGB 真值。
- DTW：先为每个动作/视角建立多个确认模板，用于相似度和差异定位，不替代触地和违规真值。

## 12. 如何修复现有数据缺陷

### 12.1 手机 RGB 精标的可信层已确认

用户已确认全部 30 条手机 RGB 的最终结果都经过人工复审；其中 29 条可用视频形成精标，
`phone_sled_push_005` 按“视频不可用”排除。导入器已经：

1. 校验 rep/分析周期、连续 phase 区间、事件归属、帧界和 revision 链。
2. 将派生标注中的“AI 候选，待人工核对”等残留更新为“AI 候选，已人工核对”。
3. 保留原始复审包、哈希、proposal 来源、原始可观察性和 revision 审计信息。
4. 将 29 条记录标记为可用于内部规则校准及监督模型实验。
5. 按用户确认将 `phone_skierg_002` 的 73 个候选分析周期记为有效，并只从人工阶段边界
   确定性派生非核心动作缺失事件。

不再安排第二名独立复核者和仲裁。`phone_lunge_004` 的历史 revision 2 缺口继续保留供审计，但不否定用户确认的最终标注。

### 12.2 当前可执行的数据角色拆分已完成

当前主体不足时，已使用人工 3 的临时 `subject_group` 分组，并保持完整视频不可拆分：

- 27 条 `development`，均为当前可训练记录。
- 3 条 `validation`，其中 2 条可评估，另 1 条为已排除的不可用视频。
- `test` 暂时为空，留给后续新主体冻结 hold-out。
- 训练/评估记录重叠为 0，临时 subject 跨角色冲突为 0。

后续获得真实且稳定的 subject ID 后，必须重新按真实 subject 分组，不能让同一人的视频同时出现在训练和测试中；当前临时分组结果不得被表述为独立主体泛化结论。

### 12.3 给数据补齐最小 metadata

对现有视频至少补：

- `subject_id`。
- action 和 view。
- device 或手机型号。
- fps 和分辨率。
- camera height、distance 和大致焦距/倍率。
- 是否完整看到头、手、髋、膝、脚和地板。
- 目标人物轨迹是否有效。
- train/validation/test 的唯一角色。

## 13. 下一步算法优化路线

以下按责任拆分为“必须由人工完成”和“Codex 可以协助完成”两条有先后依赖的路线。人工负责产生可信真值、作出动作语义裁决和采集真实数据；Codex 负责整理待审材料、实现代码、训练模型和生成评估报告。Codex 可以生成候选和辅助复审，但不能代替人工确认胸/膝/脚是否真实接触，也不能把现有姿态缓存自动当作 ground truth。

### 13.1 必须由人工完成的工作（按顺序）

#### 人工 1（已完成）：确认 15 条核心精标 RGB

15 条核心 RGB 的 rep、phase、关键事件、`VALID / NO_REP / UNSURE` 和错误结果已由用户确认完成人工复审。历史“AI 候选”只作为 proposal 来源保留，不再视为待审核；当前 15 条均可用于内部规则校准和监督实验。

#### 人工 2（取消）：不再进行第二名独立复核与仲裁

本项已决定不做。当前阶段由单人人工复核结果、逐条审计记录、结构校验和后续冻结 hold-out 评估共同提供质量约束，不再把双人一致性或仲裁作为工作项。

#### 人工 3（当前可完成范围已完成）：确认主体、拍摄信息和数据集角色

1. 现有小数据集先采用临时主体分组：`phone_skierg_001`、`phone_skierg_002` 分别归入 `subject_group_skierg_01`、`subject_group_skierg_02`；`phone_sled_push_001`–`003` 归入 `subject_group_sled_push_01`，`phone_sled_push_004`–`005` 归入 `subject_group_sled_push_02`；其余每个动作暂只使用一个对应的 `subject_group_<action>_01`。
2. 以上 ID 是当前数据组织用的临时组，不宣称跨动作身份已经核验。标注网站会给出建议分组，人工仍可按实际画面修正。
3. 同一临时 subject 只能属于一个集合。当前建议 `_01` 进入开发/校准集、`_02` 进入验证集；现有数据不建立可对外宣称的独立测试集，最终测试留给后续新主体冻结 hold-out。
4. action、view、fps、分辨率及统一的人眼可观察性已进入结构化记录；真实跨动作身份、
   手机型号、相机高度/距离和焦距等无法从现有材料可靠恢复，仍需后续采集时现场记录。

#### 人工 4（RGB 已完成，高分歧队列延期）：扩充现有数据的可信标签

1. 其余 15 条手机 RGB 已完成终态复核；14 条形成可用精标，1 条按人工结论不可用。
2. 2,439 个高分歧帧属于可延期的主动学习队列，不再作为本阶段人工阻塞项。
3. 优先标注胸部触地、后膝触地、左右脚起跳/落地、补步、脚跟抬起和遮挡/缺失。
4. 保留动作前后的 idle、setup 和 transition 片段。

#### 人工 5：补拍核心三动作及接触真值数据

1. 优先补 Burpee、Lunge、Wall Ball 的多主体、多视角、自然错误和边界动作数据。
2. Burpee/Lunge 优先增加 side、oblique；脚部和接触事件优先使用 60 FPS。
3. 接触真值优先使用同步 front + side/oblique 双视角 RGB；条件允许时使用真正硬件同步且完成标定的 RGB-D。
4. 先补 5–8 名完全未参与开发的受试者作为小规模冻结 hold-out，再逐步扩展到第 14 节的数据规模。
5. 采集时同时保留正确动作、单一错误、混合错误、自然发生错误和接近阈值的边界动作。

#### 人工 6：为后续功能确认模板和额外数据

1. 从人工确认的正确动作中，为每个“动作 × 视角”选择 5–10 个 DTW 标准模板，不能只用一个所谓标准视频。
2. 若要开放自动动作识别，再单独采集并确认 idle、setup、transition、unknown/OOD、非 HYROX 动作、部分人体入镜、多人干扰和连续动作切换数据。
3. 最终是否达到发布要求，必须由人工依据第 16 节门槛和冻结 hold-out 报告作出决定。

### 13.2 Codex 可以协助完成的工作（按顺序）

#### Codex 1（已完成）：先搭建复审、审计和数据整理工具

1. 检查现有标注的 schema、一致性、缺失字段、残留 proposal 备注和 revision 链。
2. 自动导出带帧号、上下文和局部 ROI 的复审短 clip，并生成待审清单。
3. 提供单人逐条复核、修改审计、高分歧短片段队列和版本追踪所需的数据结构或界面。
4. 根据人工最终裁决更新结构化数据；未确认项保持待审或 `UNSURE`，不自动伪造真值。

完成结果：30/30 条终态结论和 29 条可用精标已导入；原始复审包未修改，高分歧队列保留
为后续主动学习任务。

#### Codex 2（当前数据允许范围已完成）：实现统一评估体系

至少输出：

- candidate recall。
- count MAE、exact count rate。
- 匹配候选上的状态准确率。
- `VALID / NO_REP / UNSURE` 混淆矩阵。
- definite decision coverage 和 `UNSURE` rate。
- 每规则 precision、recall、FPR、FN。
- 事件帧绝对误差和容差内命中率。
- action × view × subject 分组指标。

同时加入数据泄漏检查，并分别支持开发、验证和冻结测试角色。没有拆开的指标，就无法判断“候选变多”是否以更高误报为代价，也无法知道阈值放宽到底帮助了哪条规则。

完成结果：上述指标及 `metrics_by_dataset_role`、`metrics_by_subject_group`、
`metrics_by_action_view_subject` 已进入优化报告。核心 validation/test 为空的限制被显式
记录，不生成虚假的 hold-out 结论。

#### Codex 3（已完成）：建立可回溯错误库和回归基线

1. 自动对齐人工 rep 与程序候选。
2. 将每次改动产生的 TP、FP、FN、`UNSURE` 和事件偏移导出为短 clip。
3. 建立“动作/视角/规则 → 指标 → 视频片段 → 代码版本/配置版本”的错误库。
4. 保留旧黄金视频用于软件行为回归，但不把它们当作准确率真值。

完成结果：`error_library_v1` 当前含 75 个 MP4：FN 18、FP 5、状态不匹配 3、TP 15、
`UNSURE` 34。每段最多 1 秒，媒体、源数据、实现代码和 YAML 配置均记录 SHA256。

#### Codex 4（规则链已完成，RGB 分类头受数据阻塞）：先改三个最高价值证据链

现有姿态、分割代理、地板参考、脚部事件、双阈值/持续时间和 `UNSURE` 门控已经实现并
完成回归。下面三个局部 RGB 分类头需要独立的接触/事件真值；当前不能把姿态代理自动
改名为真实接触标签，也不能在同一小批数据上训练后宣称完成。

1. **Burpee 胸部触地**：用肩髋定位躯干 ROI，输入接触前后 0.5–1.0 秒 RGB/分割短片段，结合人体分割下边界、胸髋相对地板高度、垂直速度和低速停留，输出 `contact / no_contact / unsure` 概率。
2. **Lunge 后膝触地**：用髋、膝、踝定位后膝 ROI，结合膝关节中心到地板距离、估计膝表面半径和局部 RGB；一次 rep 内锁定 trailing leg，front 视角保持更高不确定性。
3. **脚部事件**：统一 heel/toe 相对地板高度、垂直速度零交叉、短时光流和脚部局部 RGB；起跳、落地和落地后补步共享同一条脚部状态轨迹。
4. 事件继续使用进入/退出双阈值和最短持续时间；关键点缺失时输出 `UNSURE`，不能把缺失直接解释为违规。
5. 最终仍由现有规则层负责可观测性、解释和裁决。

#### Codex 5（部分完成，独立校准受数据阻塞）：改为个体基线并按组校准

配置继续按以下键保存：

```text
action × view × rule × sensitivity
```

依次实现：

1. Lunge 和 Wall Ball 完全伸展相对视频开头的可靠站立基线。
2. 地板距离按本人腿长/身体高度归一化。
3. 手腕上升按视角和本人肩髋尺度归一化。
4. 同步时间结合实际 fps，使用毫秒和帧数双重约束。
5. 每次 rep 内锁定动作方向和主侧，不逐帧重新选择。

校准时，违规检测优先控制误报，候选阶段优先提高召回，不可观察事件保留 `UNSURE`；不能只优化一个整体正确率。

当前已实现身体尺度归一化、实际 fps 的毫秒/帧约束，以及一次候选内的方向和主侧锁定。
Lunge/Wall Ball 的个体站立角已完成内部逐视频留一实验和质量门控原型，但尚未接入正式
analyzer；按独立动作 × 视角 × subject 组校准仍未完成。核心 validation/hold-out 为空时
继续同集调参只会增加泄漏。

#### 2D + 3D 影子证据逐视频留一实验（已完成，暂不进入正式裁决）

已新增身体相对 3D 时序特征：髋补偿膝/脚高度变化、腿部深度排序、3D 膝髋角、
脚速/停留/左右事件时间差、躯干俯卧与肩髋空间关系。二维局部地板线和二维触地检测
保持原样；3D 只允许调整置信度、修正 Lunge 腿侧/主侧以及在冲突时降为 `UNSURE`。
报告层硬性阻止 3D 创建候选、修改二维事件锚点、独立推断触地或把非 `VALID` 的二维
结果升格为 `VALID`。

第一版逐视频留一显示固定 25° 单帧冲突门会把正确 `NO_REP` 过多降为 `UNSURE`。
第二版把伴随低可见度、无效几何或时间跳变的差异改为不可用，并要求可信角度冲突至少
持续 5 帧且占可观察证据 50%；每折在质量门回退、仅身体时序、仅角度和组合证据四类
消融中只用其余 14 条视频选择。15 折全部选择质量门精确回退 2D，结果为 candidate
recall `74.29% → 74.29%`、状态准确率 `32.69% → 32.69%`、`UNSURE` rate
`65.38% → 65.38%`、事件帧 MAE `42.77 → 42.77` 帧，规则 FP/FN 也保持不变。
这消除了第一版的净退化，但也证明当前数据尚未给出 3D 稳定增益，暂不应进入正式裁决。
逐视频留一只降低同视频直接泄漏，仍不是独立测试或跨主体结论。可复现命令：

```powershell
python -m tools.run_2d_3d_shadow_evidence
```

完整 JSON/Markdown 位于
`datasets/hyrox/reports/internal_2d_3d_shadow_evidence_lovo_v2.{json,md}`。

#### 轻量阶段、站立基线、接触代理与置信度实验（已完成 v2，暂不进入正式裁决）

`src/temporal_evidence.py` 和 `tools/run_temporal_evidence_experiment.py` 已实现：

- Ridge 线性阶段发射模型和因果 HMM；
- 用人工阶段/rep 边界在训练视频中选择阶段持续帧和最多两个缺失阶段，审计候选合并、
  重复结算和漏检；
- 无需读取阶段标签的每视频站立候选，但必须通过膝髋角合理性、低髋速度、角度/身高
  稳定和双侧一致性质量门，否则回退固定阈值；
- 分割轮廓、二维地板距离和身体相对 3D 的接触代理消融；
- 只允许 `UNSURE → NO_REP` 的规则置信度校准，以及误报增加时的整项硬回退。

29 段视频的逐视频留一结果中，因果 HMM 相比线性逐帧把阶段边界 precision
`32.70% → 71.18%`、frame accuracy `49.90% → 51.60%`，但 boundary recall
`73.37% → 64.94%`，boundary MAE `4.65 → 5.10` 帧。加入训练折的缺失阶段校准后，
阶段流候选 recall `15.89% → 31.31%`、precision `59.65% → 94.37%`，终点 MAE
`26.35 → 24.31` 帧。该召回仍太低，HMM 只能用于现有规则候选的去抖、合并和结算提示，
不能作为独立候选产生器或漏检恢复器。

8 段 Lunge/Wall Ball 中只有 2 段自动站立基线通过质量门；5 段正面 Lunge 的候选膝角
约为 `67°–101°`，另 1 段 Wall Ball 的髋角约为 `122°`，均被拒绝并回退固定阈值。
质量门控个体基线帧级 F1 为 `20.44%`，固定 165° 为 `0.44%`；但个体基线仍产生
15 个 FP，因此只证明质量门能阻止荒谬基线进入后续实验，不足以启用正式伸展裁决。

接触代理共 51 个 rep，且接触负例很少。纯 2D 有零 FP、`UNSURE 88.24%`、事件 MAE
`14.43` 帧；加入分割后 `UNSURE 78.43%`，但有 2 个 FP；再加身体相对 3D 有 1 个 FP，
`UNSURE 88.24%`、事件 MAE `12.10` 帧。最终影子安全策略保留二维接触状态，只采用
分割/3D 的事件锚点，因此零新增 FP、事件 MAE `12.10` 帧，但不能降低 `UNSURE`，
也不能解释为力学或物理触地真值。

置信度阈值要求同一规则至少 5 个训练正例、覆盖至少 3 段视频且训练零误报。原始 LOVO
可把 `UNSURE 65.38% → 55.77%`，但人工 `VALID/UNSURE` 被判 `NO_REP` 的数量从
3 增至 8，误报硬门失败，最终策略自动回退基线，状态准确率和 `UNSURE` 均保持
`32.69%` 和 `65.38%`。这不是“校准成功降低 UNSURE”，而是安全门成功拒绝了不可靠
校准。当前数据下若必须不明显增加误报，就不能安全减少这部分 `UNSURE`。

可复现命令：

```powershell
python -m tools.run_temporal_evidence_experiment
```

完整报告位于 `datasets/hyrox/reports/internal_temporal_evidence_lovo_v2.{json,md}`。
逐视频留一只减少同一视频直接调参与评估泄漏；小样本、单人工复核和主体覆盖不足仍使
它只能称为内部实验。

#### Codex 6（受样本量和冻结验证主体阻塞）：加入轻量 Pose 时序模型

在人工作出训练资格确认且样本量足够后，先为每个动作训练独立 TCN 或 1D Conv，不直接上大型视频 Transformer。

输入包括：

- 2D 关节角和身体归一化相对位置。
- confidence、missing mask、速度和角速度。
- body scale、view embedding 和相对地板特征。

输出包括：

- phase 概率。
- event 概率。
- 每条规则的 evidence 概率。
- uncertainty。

现有状态机和规则层继续保留：

```text
时序模型负责“从噪声序列中找阶段和事件”
规则层负责“按比赛/动作要求解释和裁决”
```

必须与现有规则状态机做消融比较，并在冻结验证集上按动作、视角和 subject 报告结果。

#### Codex 7（受人工模板数量阻塞）：实现多模板、分阶段 DTW

在人工 6 确认模板后：

1. 支持每个动作 × 视角的 5–10 个模板。
2. 按阶段对齐，不用一个全程距离决定正确与否。
3. 输出“哪个阶段、哪个关节偏差最大”。
4. 只用于动作质量相似度和反馈，不替代接触、补步等明确规则。

#### Codex 8（受负样本与 OOD 数据阻塞）：最后开发并验证自动动作识别

只有在人工 6 补齐连续负样本和 unknown/OOD 数据后才开始：

1. 先训练现有 Logistic Regression 基线。
2. 再比较 XGBoost 或 TCN。
3. 评估 unknown rejection、误切换率、切换延迟和短动作漏检。
4. 达到门槛前继续由用户手动选择动作。

建议实际协作顺序为：

```text
人工 1 已完成核心可信标签确认
    → Codex 1–3 更新数据资格、评估与错误基线
    → 人工 2 已取消，不再等待独立复核
    → 人工 3 按临时 subject 分组组织开发/验证角色
    → 人工 4 扩充现有标签
    → Codex 4–5 修复核心证据链和分组校准
    → 人工 5 补拍并冻结新主体数据
    → Codex 6 训练轻量时序模型
    → 人工 6 确认模板/补充负样本
    → Codex 7–8 开发 DTW 和自动动作识别
```

## 14. 还需要采集什么数据

### 14.1 核心三动作的最小可用数据集

优先 Burpee、Lunge 和 Wall Ball：

- 12–15 名受试者。
- 覆盖不同身高、体型、服装和动作水平。
- 每动作每人至少 2 组标准动作，每组 5 reps。
- 每种核心错误至少 1 组，每组 3–5 reps。
- 三个动作总目标约 1,000–1,500 reps。
- 每个动作至少包含 front、side、oblique 三个视角。
- 脚部和接触动作优先 60 FPS。
- 20%–30% 错误应自然发生，而不是全部刻意夸张示范。
- 同时保留单一错误和混合错误。

推荐粗略分配：

| 动作 | 建议最小 rep | 重点错误/边界 |
|---|---:|---|
| Burpee Broad Jump | 350–500 | 胸未触地、起跳不同步、落地不同步、手脚过远、补步、向前位移不足 |
| Lunge | 350–500 | 后膝未触地、髋未伸展、膝未伸展、同腿连续、补步、接近阈值的边界动作 |
| Wall Ball | 300–400 | 下蹲不足、髋/膝未伸展、踮脚、手腕路径不完整、左右不对称 |

### 14.2 生产泛化数据

在核心原型稳定后扩大到：

- 30 名以上受试者。
- 3,000 个以上核心 rep。
- 20%–30% 完整受试者作为最终 hold-out。
- 多种手机、焦距、拍摄距离、相机高度、光照、服装和背景。

虽然当前只关心人体动作，但这些变化仍需采集，因为它们会改变人体关键点质量。这里不是要判断场地是否合法，而是要避免算法只适用于当前拍摄环境。

### 14.3 接触真值数据

部署阶段仍可以只使用单手机 RGB，但采集和标注阶段建议增加：

- 同步 front + side/oblique 双视角 RGB；或
- 真正硬件同步、完成标定的 RGB-D。

用途是给胸、后膝和脚部接触提供训练/验证真值，而不是要求最终用户必须购买深度相机。

### 14.4 自动动作识别额外数据

若以后要自动判断当前是哪种动作，还必须单独采集：

- idle。
- setup。
- 动作之间 transition。
- 连续动作切换。
- 非 HYROX 动作。
- 人体部分入镜和离开画面。
- 路人经过、多人物干扰。
- 做错动作后停止或重新开始。

## 15. 推荐标注规范

每个视频级标签：

- `subject_id`
- `action`
- `view`
- `device`
- `fps`
- `resolution`
- `camera_height`
- `camera_distance`
- `target_track_valid`
- `dataset_role`

每个 rep：

- `rep_start_frame`
- `rep_end_frame`
- `validity: VALID / NO_REP / UNSURE`
- `affected_side`
- `reviewer_id`
- `review_revision`

每个阶段和事件：

- phase 区间。
- exact event frame 或容差区间。
- error code。
- `image_observable`。
- `pose_observable`。
- reviewer confidence。
- 是否来自 proposal。
- 单人人工复核状态及完整修改审计。

数据角色必须互斥：

```text
train ≠ validation ≠ test
```

同一 subject 的所有片段必须只属于其中一个集合。

## 16. 建议的评估与发布门槛

以下是下一阶段可采用的工程目标，不代表当前已经达到：

| 指标 | 建议阶段目标 |
|---|---:|
| candidate recall | ≥ 95% |
| 视频计数 exact rate | ≥ 90% |
| count MAE | ≤ 0.2 rep/视频 |
| 匹配候选上的状态准确率 | ≥ 90% |
| 明确结论覆盖率 | ≥ 85% |
| `UNSURE` rate | ≤ 15%，且不能靠降低安全门硬压 |
| 核心错误 precision | ≥ 85% |
| 核心错误 recall | ≥ 85% |
| 核心错误 FPR | ≤ 5% |
| 新 subject 相对验证集性能下降 | ≤ 5 个百分点 |

实际发布前还应按动作和视角分别达标，不能用 Wall Ball 的好结果平均掉 Burpee 或 Lunge 的差结果。

## 17. 推荐的 4–8 周执行顺序

### 第 1 周：评估和数据角色

- 已完成 15 条核心 RGB 的人工复审确认。
- 已完成拆分指标和按事件帧的单调候选对齐。
- 已冻结当前可用的 development/validation 角色；test 留给新主体，核心动作尚无独立
  validation/test。
- 已将误判和回归样本导出为 75 个最长 1 秒的短 clip，建立
  “规则/类别 → TP/FP/FN/`UNSURE` → 视频片段 → 代码/配置哈希”的错误库。

### 第 2 周：完成人工 4 标注和错误库完善

- 已补齐其余 15 条手机 RGB：14 条可用精标，1 条按人工结论不可用。
- 2,439 个高分歧帧复核按用户决定延期；它是主动学习/后端分析任务，不影响本轮 RGB
  精标资格。
- 已完成按动作、视角、类别及实现/配置版本追踪的错误库。

### 第 3–4 周：修三条高价值证据链

- Burpee 胸部接触局部 RGB 头。
- Lunge 后膝接触局部 RGB 头。
- 脚部状态和起落事件统一重构。
- Lunge/Wall Ball 改为相对站立基线。

现有规则代理和脚部状态轨迹已实现；局部 RGB 头须先补独立接触真值，个体基线须先补
核心 validation/hold-out，不能直接在当前同集回归上继续校准。

### 第 5–6 周：轻量时序模型

- 训练每动作独立的 pose TCN/1D Conv。
- 预测 phase、event 和 uncertainty。
- 与现有规则状态机做消融比较。

### 第 7–8 周：新主体小规模验证

- 先补 5–8 名未参与开发的受试者。
- 覆盖 front、side、oblique。
- 只在冻结 hold-out 上报告最终指标。
- 若证据链仍失败，再决定扩大模型还是继续补特定数据。

## 18. 现在应该避免的做法

- 不要继续反复降低一个全局置信度门槛。
- 不要把关键点缺失解释为动作违规。
- 不要用视频文件名中的“标准/错误”直接当真值。
- 不要把旧黄金视频当作准确率测试集。
- 不要把 Full 和 Lite 的一致性当作真实姿态准确率。
- 不要把未配对 ONI Depth/IR 当作手机 RGB 的接触标签。
- 不要在同一批 15 条数据上调参后再称为独立测试。
- 不要只报告一个混合错误率。
- 不要因为场地暂时不重要就删除所有地板信息；胸、膝、脚接触仍需要“人体相对地板”的证据。

## 19. 仓库中的主要事实来源

| 内容 | 当前主要来源 |
|---|---|
| 正式姿态模型和平滑策略 | `configs/product_pose.yaml` |
| 动作阈值 | `configs/hyrox/*.yaml` |
| 可观测性门槛 | `configs/hyrox/observability.yaml` |
| 动作状态机 | `hyrox/actions/*.py` |
| 接触和脚部事件 | `hyrox/contact.py`、`hyrox/foot_events.py` |
| 最终规则结算 | `hyrox/validity.py` |
| 手机 RGB 清单 | `datasets/hyrox/manifests/phone_records.json` |
| 手机数据基线 | `datasets/hyrox/reports/round6_implementation_summary.json` |
| 主体锁定和 ROI 消融 | `datasets/hyrox/reports/round7_implementation_summary.json` |
| Full/Lite、时序与可观察性 | `datasets/hyrox/reports/round8_implementation_summary.json` |
| 人工精标 | `datasets/hyrox/reviews/human_rgb_fine_annotations_v1.json` |
| 数据角色与泄漏约束 | `datasets/hyrox/manifests/data_roles_v1.json` |
| 最新核心 RGB 评估 | `datasets/hyrox/reports/reviewed_phone_rgb_guidance_evaluation_optimized_v1.json` |
| 第 12 轮人工角度验证 | `outputs/angle_validation/round12/reviewed_angle_evaluation.json`、`ROUND12_VALIDATION_REPORT.md` |
| 实时优化任务状态 | `HYROX 实时姿态系统优化任务清单.md` |
| RGB 错误短片段库 | `datasets/hyrox/reviews/error_library_v1/index.json` |
| ONI 研究结论 | `datasets/hyrox/reports/round11_implementation_summary.json` |
| 软件回归视频 | `configs/hyrox_golden_videos.json` |

部分历史说明可能仍保留旧阈值。例如 `configs/hyrox/README.md` 中的部分 Wall Ball 数字可能滞后。涉及程序实际行为时，应按以下优先级确认：

```text
当前动作代码 + 当前 YAML 配置
        ↓
最新结构化评估报告
        ↓
历史说明文档
```

## 20. 最终建议

当前项目已经具备不错的工程骨架：实时姿态、状态机、规则解释、可观测性门控、回归测试、数据清单和评估工具都已存在。现在最缺的不是再加几十个全局阈值，而是**可信标签、独立测试、跨主体数据，以及对真实接触和脚部事件的更直接证据。**

最推荐的技术形态不是完全抛弃现有规则，而是：

```text
MediaPipe 姿态
+ 轻量 Pose 时序模型学习阶段/事件
+ 胸/膝/脚局部 RGB 接触模型
+ 现有规则层负责解释、可观测性和最终裁决
```

这样既能继续利用现有代码和精标数据，又能正面解决二维骨架无法表达真实接触的问题。阈值可以适当放宽，但必须以“动作 × 视角 × 规则”为单位，在独立验证数据上控制误报和漏报；否则只是把 `UNSURE` 转换成新的错误确定答案。
