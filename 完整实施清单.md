# 完整实施清单

------

# 零、目标、数据现状与总路线

## 0.1 最终目标

本项目的最终目标不是“复现某一段示例视频”，而是让普通手机或电脑 RGB 摄像头对
HYROX 动作提供可解释的实时能力：

```text
识别当前动作
→ 锁定真正运动者并切分每一次动作
→ 判定 VALID / NO_REP / UNSURE
→ 识别具体错误、发生阶段和严重程度
→ 输出可解释的分项评分与置信度
→ 给出当前最重要、能够执行的纠正指导
```

这里的“学习标准动作与不标准动作”必须落实为可复现的数据流程：文件名只提供录制意图，
经过主体锁定、人工复核和帧/次数级标注后，才能用于规则标定、DTW 模板或监督模型训练。
模型不得把单个录制者、单一视角或某一条“标准”视频当作唯一动作标准。

## 0.2 2026-07-24 数据快照

当前有两批彼此独立的数据，禁止按动作名、序号或时长建立伪配对：

| 数据源 | 数量与模态 | 当前用途 | 明确限制 |
|---|---:|---|---|
| `hyrox动作视频集（oni）/` | 32 个 ONI；全部只有 Depth + IR，没有 Color | 深度/轮廓/地面/接触研究，错误类型目录，辅助标注研究 | 不是普通 RGB；不能执行 RGB–Depth 配准；不能与手机视频逐帧对齐 |
| `hyrox手机录像数据/` | 30 个真实 RGB MP4；共 15,515 帧、约 517.17 秒 | 与最终摄像头场景一致的主数据入口、姿态提取、标注、模板和模型训练试点 | 当前主要是单次录制意图、视角和人员多样性有限，不能直接支撑生产级泛化结论 |

30 个真实视频已经逐帧解码到声明的最后一帧。29 个为 `720×1280 @ 30 FPS`，另一个
“滑雪机-标准-斜后方-1.mp4”为 `544×960 @ 约 30 FPS`。目录中另有 30 个隐藏的
`._*.mp4`，每个 4096 字节，属于 AppleDouble 元数据而不是视频；扫描器必须忽略，不能
把目录误报为 60 个样本。

ONI 录制意图分布：

| 动作 | ONI 数 | “标准”意图 | 错误意图 |
|---|---:|---:|---:|
| 波比跳 | 7 | 3 | 4 |
| 负重箭步蹲 | 5 | 1 | 4 |
| 滑雪机 | 4 | 4 | 0 |
| 划船机 | 3 | 1 | 2 |
| 拉雪橇 | 2 | 2 | 0 |
| 农夫行走 | 2 | 1 | 1 |
| 墙球 | 3 | 1 | 2 |
| 推雪橇 | 6 | 6 | 0 |

手机 RGB 录制意图分布：

| 动作 | 手机视频数 | “标准”意图 | 错误意图 |
|---|---:|---:|---:|
| 波比跳 | 7 | 3 | 4 |
| 负重箭步蹲 | 5 | 1 | 4 |
| 滑雪机 | 2 | 2 | 0 |
| 划船机 | 3 | 1 | 2 |
| 拉雪橇 | 2 | 2 | 0 |
| 农夫行走 | 3 | 2 | 1 |
| 墙球 | 3 | 1 | 2 |
| 推雪橇 | 5 | 5 | 0 |

因此现阶段只能建立数据管线、标注规范、规则/模板试点和小样本可学习性基线，不能宣称已经
学会全部 HYROX 动作。手机 RGB 已覆盖全部 8 类动作的“标准”录制意图，其中波比跳、
负重箭步蹲、划船机、农夫行走和墙球同时有错误意图；滑雪机、拉雪橇和推雪橇仍缺少错误
与边界样本。各具体错误通常只有一段，且受试者、视角、设备和环境多样性仍不足。ONI 中
同名动作只能提供跨模态辅助证据，不能冒充缺失的手机 RGB 样本。

## 0.3 主线与辅助线

```text
主线：手机/电脑 RGB
→ 多人检测与目标运动者锁定
→ RGB 姿态与质量门
→ 动作/次数/阶段/事件/错误标注
→ 规则 + 多模板 DTW + 轻量时序模型
→ 判定 + 分项评分 + 纠正指导

辅助线：现有 ONI Depth + IR
→ 主体与数据质量审计
→ 深度、地面、接触和空间运动研究
→ 只在证据可靠且能够映射到标签时提供辅助监督
```

正式产品运行时不得依赖 ONI、OpenNI、IR 或深度摄像头。当前 ONI 没有 Color，阶段 4 的
Color–Depth 同步和阶段 5 的 depth-to-color 配准对本批数据均不适用；不得继续把它们当作
手机 RGB 主线的前置阻塞项。

## 0.4 新网页示例视频候选

现有网页按 8 个动作提供示例，因此从手机“标准”意图视频中各选一段候选：

| 动作 | 选定候选 | 帧数 | 时长 | 选择依据 |
|---|---|---:|---:|---|
| 波比跳 | `波比跳-标准（双脚起）-正前方-1.mp4` | 508 | 16.93 秒 | 常见双脚起动作风格，主要阶段完整 |
| 负重箭步蹲 | `负重箭步蹲-标准-正前方-1.mp4` | 527 | 17.57 秒 | 当前唯一手机标准候选 |
| 滑雪机 | `滑雪机-标准-后方-1.mp4` | 609 | 20.30 秒 | 画面稳定、主体完整，较斜后方长视频更适合示例 |
| 划船机 | `划船机-标准-侧后方-1.mp4` | 769 | 25.63 秒 | 主体清晰、器械和动作周期完整 |
| 拉雪橇 | `拉雪橇-标准-侧方-2.mp4` | 737 | 24.57 秒 | 持续动作覆盖更完整 |
| 农夫行走 | `农夫行走-标准-侧方-1.mp4` | 161 | 5.37 秒 | 主体占比大，背景人员交叠少于 `-2` |
| 墙球 | `墙球-标准-正后方-1.mp4` | 421 | 14.03 秒 | 主体居中、全身和完整下蹲—投球周期清晰 |
| 推雪橇 | `推雪橇-标准-侧方-3.mp4` | 410 | 13.67 秒 | 侧面运动持续时间和动作覆盖较好 |

这 8 段均先标记为 `example_candidate`。文件名中的“标准”仍只是录制意图；每段必须完成
动作专家复核、主体轨迹审计、授权确认和 SHA256 固定，才能标记为
`verified_standard_example`。示例视频不自动进入训练集或黄金评测集，也不能成为该动作
的唯一 DTW 模板。

当前文件名已经正常，无需编码恢复。原视频保持只读；网页使用带来源 `record_id` 的
规范化派生副本。现有网页按 `HYROX视频/` 目录和旧文件 stem 硬编码发现示例，替换时必须
改为读取版本化示例 manifest，不能仅复制新文件后期待自动识别。

------

# 一、不可破坏的现有功能约束

下面这些要求应写进每一轮交给 Codex 的任务开头。

## 1. 保留默认运行方式

当前默认模式在新能力通过发布门之前继续是：

```text
普通电脑/手机 RGB
→ MediaPipe Pose Landmarker
→ 现有平滑
→ 现有动作状态机
→ 规则计数
→ 触地与脚步代理
→ DTW
→ 判定、分项评分、纠正指导与实时显示
```

其中判定、评分和纠正的新字段在尚未实现时保持缺省/关闭，不改变旧输出。多人候选检测与
目标运动者锁定先作为默认关闭的前置模块开发；通过多人评测和延迟门后再升级为产品默认，
但离线训练数据从现在起必须先完成主体锁定。

不得要求用户安装：

- OpenNI；
- Orbbec SDK；
- 奥比驱动；
- 深度处理依赖。

## 2. 所有新功能默认关闭

建议保留运行模式：

```text
rule_only
rule_dtw
rule_neural
rule_dtw_neural
offline_oni
```

当前默认仍然是现有模式，建议：

```text
rule_dtw
```

或者保持 README 当前默认值，不主动修改。

## 3. 输出结构只能扩展，不能破坏

现有结果字段、CLI 参数、网页接口和动作分析器调用方式保持兼容。

允许增加：

```text
depth_evidence
neural_evidence
quality_flags
model_confidence
validity
error_evidence
score_breakdown
score_confidence
correction_cues
```

不允许重命名或删除现有字段。

## 4. ONI 环境与产品环境分离

建议目录：

```text
project/
├── src/                       # 现有产品程序
├── webui/                     # 现有网页功能
├── configs/
├── tests/
├── tools/
│   ├── oni_bridge/            # 独立 C++/OpenNI 工具
│   ├── dataset/
│   ├── annotation/
│   └── evaluation/
├── training/                  # 神经网络训练
└── datasets/                  # 默认不提交 Git
```

OpenNI DLL 不复制进 `src/`，也不加入主程序强制依赖。

## 5. 每轮修改都必须回归

每完成一轮，至少执行：

```text
现有单元测试
现有网页测试
新手机 RGB 版本化回归
现有 8 项兼容性视频回归（仅在 HYROX视频/ 删除前执行）
普通电脑摄像头启动测试
神经网络关闭模式测试
无 ONI 文件环境测试
```

## 6. 当前数据边界与后续接口

当前事实必须作为后续每一轮的共同前提：

- 当前同时存在 32 个 Depth + IR ONI 和 30 个独立手机 RGB 视频；
- 手机 RGB 是普通摄像头产品的主数据入口，ONI 是不要求实时部署的辅助数据；
- 手机视频与当前 ONI 是两批独立数据，不建立一一对应、同一 session 或时间同步关系；
- `HYROX视频/` 将由用户删除；其 8 段视频不是训练标准、动作真值或最终评测集；
- 新网页示例按 8 个动作分别改用第 0.4 节选定的手机 RGB 规范化派生副本；
- 两批数据都可能包含其他人，任何姿态缓存、标注、阈值统计和训练样本都必须先锁定真正执行动作的运动者；
- 文件名中的“标准/错误”均只是录制意图，只有人工确认标签才是真值。

接口设计使用统一的 `source_type`：

```text
oni
phone_rgb
```

两类数据可以复用 manifest、标注和 RGB-only 特征 schema，但拥有独立的
`record_id`、`subject_id`、时间轴和数据分区。只有未来明确设计为同步采集的一组新数据，
才允许另外建立 `paired_group_id`；该字段默认必须为 `null`。

旧值 `phone_rgb_future` 只作为已有空 manifest/schema 的迁移兼容别名；新建记录一律使用
`phone_rgb`，迁移后不得继续生成新 `phone_rgb_future` 记录。

------

# 二、阶段 0：冻结现有工程基线

优先级：**P0，第一件事**

## 任务

-  给当前稳定代码打 Git 标签，例如：

```text
v0.9-rule-dtw-baseline
```

-  保存当前 Python、MediaPipe、OpenCV 和模型版本。
-  固定当前配置文件副本。
-  固定现有动作分析输出 schema。
-  保存现有 8 项兼容性回归视频及其 SHA256，明确标记为 `legacy_provisional`。
-  保存每个视频的：
  - 候选次数；
  - 有效次数；
  - NO_REP；
  - UNSURE；
  - 动作周期；
  - 各规则 PASS/FAIL/UNSURE；
  - DTW 结果。
-  记录当前摄像头实际 FPS。
-  记录 MediaPipe 推理 P50/P95。
-  记录姿态结果年龄和显示延迟。
-  增加一键基线测试脚本。
-  记录旧视频和网页示例的替换策略，不把旧视频结果用于训练、阈值标定或动作标准定义。

建议输出：

```text
reports/baseline/
├── environment.json
├── golden_results.json
├── latency_report.json
└── test_summary.md
```

## 验收

- 现有全部测试通过；
- 现有兼容性视频结果可重复；
- 新代码出现问题时，可以直接切回该版本；
- 没有 ONI、OpenNI 和神经网络依赖。
- 报告明确区分“软件兼容性基线”和“数据/动作质量基准”。

------

# 三、阶段 1：整理 ONI 与独立手机 RGB 原始数据

优先级：**P0**

## 目录结构

```text
datasets/hyrox/
├── raw/
│   ├── oni/                    # 32 个 Depth + IR ONI
│   └── phone_rgb/              # 30 个独立手机 RGB 视频
├── manifests/
├── extracted/
├── synchronized/
├── registered/
├── pose_cache/
├── annotations/
├── clips/
├── splits/
└── reports/
```

## 原始数据要求

-  原始 ONI 只读保存。
-  当前手机视频同样只读保存，但不与当前 ONI 配对。
-  不覆盖、不重新编码后替换原文件。
-  每个文件计算 SHA256。
-  每个 ONI 和手机视频分别使用唯一且稳定的 `record_id`。
-  `record_id` 只标识一个原始记录，不暗示存在对应手机视频。
-  记录动作类型、受试者、视角、录制意图、错误类型和主体运动者信息。
-  原文件名原样保留；规范化字段写入 manifest，不批量重命名原始文件。
-  手机视频当前中文文件名正常，`source_filename` 原样保存，不执行编码恢复或静默重命名。
-  递归扫描时忽略 `._*` 和无法被视频解码器打开的 AppleDouble 元数据；当前 30 个
   4096 字节的隐藏 `._*.mp4` 不得生成 `record_id`。

现有文件名以 `hyrox动作视频集（oni）/` 为准，主要模式为：

```text
{动作}-{录制意图或错误类型}-{视角}.oni
{动作}-{录制意图或错误类型}{序号}-{视角}.oni

负重箭步蹲-标准-斜前方.oni
负重箭步蹲-后膝未触地-斜前方.oni
波比跳-标准（双腿起）-斜前方.oni
推雪橇-标准11-斜前方.oni
```

文件名中的“标准”或错误描述只是录制意图，不直接作为训练真值。正式标签必须经过人工
复核和帧级标注。为了避免不同语言、空格、括号和序号影响程序，manifest 同时保存
`source_filename`、原始中文字段和规范化字段。

解析规则：

- 先匹配已知动作前缀和视角后缀，中间完整保留为 `recording_intent_raw`；
- `标准1`、`标准11`、`标准（双腿起）` 等字符串不得被简单截断；
- 若能无歧义识别末尾序号，另外写入 `take_index`，同时保留未修改的原始字符串；
- 文件名没有受试者编号时，不从文件顺序猜测 `subject_id`，先使用
  `subject_pending`，再由拍摄记录或人工确认；
- `record_id` 一旦生成不得因后续修正动作名、视角或标签而改变。

## Record manifest

```json
{
  "record_id": "oni_lunge_001",
  "source_type": "oni",
  "source_filename": "负重箭步蹲-后膝未触地-斜前方.oni",
  "source_file": "raw/oni/负重箭步蹲-后膝未触地-斜前方.oni",
  "action": "lunge",
  "action_raw": "负重箭步蹲",
  "subject_id": "subject_pending",
  "camera_view": "oblique_front",
  "camera_view_raw": "斜前方",
  "recording_intent": "error",
  "recording_intent_raw": "后膝未触地",
  "expected_errors_unverified": [
    "NO_KNEE_CONTACT"
  ],
  "paired_group_id": null,
  "target_athlete": {
    "track_id": null,
    "selection_status": "pending",
    "selection_method": null,
    "identity_switch_segments": []
  },
  "other_people_present": "unknown",
  "full_body_visible": true,
  "floor_visible": true,
  "sha256": "",
  "usage_authorization": "",
  "notes": ""
}
```

手机视频沿用同一顶层结构，但使用独立的 `record_id` 和
`source_type: "phone_rgb"`。不得为了复用代码而把手机记录塞入当前 ONI 的
`record_id`，也不得生成虚假的配对关系。

手机记录至少增加：

```json
{
  "record_id": "phone_farmers_carry_001",
  "source_type": "phone_rgb",
  "source_filename": "农夫行走-标准-侧方-1.mp4",
  "source_file": "raw/phone_rgb/农夫行走-标准-侧方-1.mp4",
  "action": "farmers_carry",
  "camera_view": "side",
  "recording_intent": "standard",
  "recording_intent_verified": false,
  "paired_group_id": null,
  "target_athlete": {
    "track_id": null,
    "selection_status": "pending"
  },
  "example_status": "example_candidate",
  "sha256": ""
}
```

## 旧视频和示例视频的定位

- `HYROX视频/` 删除前只用于保护当前规则、网页接口和回放流程；删除后测试不得再硬编码
  该目录，缺失时应明确跳过旧基线而不是导致产品启动失败；
- 不使用旧视频拟合阈值、选择“标准动作”、训练模型或报告新数据集性能；
- 先基于主体身份稳定且完成标注的手机 RGB 建立新回归集；
- 新回归集必须版本化，例如 `hyrox_golden_v2_phone_rgb`，并保存来源
  `record_id`、标注版本和切片范围；
- 用户删除旧目录后，以已保存的旧结果摘要维持兼容性历史，不要求继续保留旧视频副本；
- 网页示例视频、预计算姿态缓存、manifest、文档和指纹校验必须作为一个原子版本同时替换。
- 8 个网页示例候选固定为第 0.4 节的选择；通过动作复核前只用于内部预览，不作为黄金
  真值或唯一 DTW 模板；
- 示例发现机制改为读取 manifest 中的 `action`、`camera_view` 和
  `published_example_path`，不再依赖旧 `ACTION_BY_SAMPLE_STEM` 的精确文件名映射。

## 验收

- 当前每段 ONI 和手机视频都有唯一 `record_id`、SHA256 和原始文件名；
- 手机视频 30/30 可以解码、获得自身时间轴并生成独立 manifest；
- manifest 校验明确禁止默认建立 ONI—手机一一配对；
- 原始数据没有被转换文件覆盖；
- 数据来源和使用授权有记录；
- 文件名中的录制意图与已确认标注分开保存；
- 每段数据都能记录主体运动者和其他人员干扰状态；
- AppleDouble 元数据被明确排除且不会增加样本数；
- 8 个网页示例候选、规范化发布名和审核状态明确。

------

# 四、阶段 2：实现独立 ONI 审计工具

优先级：**P0**

暂时只处理录制好的 ONI 文件，不连接奥比摄像头。

建议使用：

```text
C++ + OpenNI2
```

实现：

```text
tools/oni_bridge/oni-inspect.exe
```

调用方式：

```powershell
oni-inspect.exe input.oni --output oni_inventory.json
```

## 审计内容

### 文件级

-  是否成功打开；
-  是否能够完整播放；
-  文件大小；
-  OpenNI 版本；
-  是否发生解码错误；
-  总持续时间。

### 流级

分别检查：

```text
Color
Depth
IR
```

记录：

-  是否存在；
-  分辨率；
-  像素格式；
-  标称 FPS；
-  实际帧数；
-  第一帧、最后一帧时间戳；
-  时间戳是否递增；
-  frame index 是否连续；
-  帧间隔 P50/P95；
-  丢帧和异常间隔。

### 深度质量

-  深度最小值、最大值；
-  0 值比例；
-  无效像素比例；
-  中心区域深度分布；
-  是否整段缺少有效深度。

## 审计分类

```text
A：Color + Depth（可以另外包含 IR）
B：有 Depth、无 Color
C：有 Color、无 Depth
D：只有 IR 或其他不完整流组合
E：损坏或无法完整读取
```

分类只描述 ONI 文件内部实际包含的流，不使用当前手机视频补齐或改变 ONI 分类。

## 验收

- 所有 ONI 都生成审计报告；
- 明确每个文件实际包含哪些流；
- 不再假设 ONI 一定同时包含 RGB 和深度；
- 不合格数据被单独标记。

------

# 五、阶段 3：实现 ONI 无损导出工具

优先级：**P0**

实现：

```text
oni-export.exe
```

调用方式：

```powershell
oni-export.exe input.oni --output extracted/record_id
```

## 输出结构

```text
extracted/record_id/
├── metadata.json
├── color/
│   ├── color.mp4
│   ├── frames/
│   └── index.csv
├── depth/
│   ├── frames/
│   └── index.csv
├── ir/
│   ├── frames/
│   └── index.csv
└── preview/
    └── depth_preview.mp4
```

## 彩色流

保存：

- 方便查看和标注的 `color.mp4`；
- 必要时保存逐帧 PNG；
- 每一帧对应原始时间戳。

`index.csv`：

```text
output_frame
source_frame_index
timestamp_us
width
height
pixel_format
```

## 深度流

必须保存真实深度值：

```text
16 位 PNG
或 .npy/.npz
```

禁止只保存伪彩色视频。

深度索引：

```text
output_frame
source_frame_index
timestamp_us
depth_scale
invalid_pixel_ratio
```

## 确定性要求

同一个 ONI 重复导出时：

- 帧数相同；
- 时间戳相同；
- 深度文件 hash 相同；
- 不因计算机性能而跳帧；
- 不因播放速度而改变结果。

## 验收

- Color、Depth、IR 能分别导出；
- 深度仍保留原始数值精度；
- 每帧均可追溯到 ONI 原始 frame index 和 timestamp；
- 导出工具不依赖现有产品程序。

------

# 六、阶段 4：完成离线时间同步

优先级：**P0**

## 4.1 ONI 内部 RGB 与 Depth

配对优先级：

```text
已确认录制时开启同步且 frame index 匹配
→ 按 frame index

否则
→ 按最近时间戳
```

输出：

```text
rgb_frame
depth_frame
rgb_timestamp_us
depth_timestamp_us
delta_us
sync_method
```

不能直接认为：

```text
第 N 张彩色帧 = 第 N 张深度帧
```

## 4.2 当前手机视频的独立时间轴

当前手机视频与现有 ONI 属于独立数据，禁止执行：

```text
手机帧 ↔ 当前 ONI 帧配对
t_phone = a × t_oni + b
运动曲线互相关强行对齐
```

手机视频必须建立自身时间轴：

```text
record_id
source_frame_index
timestamp_ms
source_fps
timestamp_source
```

只有在未来新采集方案明确使用两台设备同步录制、manifest 中存在非空
`paired_group_id`，并且有拍手、闪光等可验证同步事件时，才另行启用双点偏移和漂移
修正。该能力不是当前 ONI 数据处理的验收项。

## 同步质量

```json
{
  "sync_scope": "oni_internal_color_depth",
  "sync_method": "nearest_timestamp",
  "offset_ms": 0.0,
  "drift_scale": 1.0,
  "median_error_ms": 12.5,
  "p95_error_ms": 27.1,
  "sync_quality": "good"
}
```

建议分级：

```text
good
usable
video_level_only
invalid
```

## 验收

- 每个含 Color + Depth 的 ONI 记录都有内部同步报告；
- 时间差可以被量化；
- 低质量同步数据不会进入精细事件训练；
- 原始时间戳得到保留；
- 当前 30 个手机视频都有独立时间轴和解码审计；
- 不生成当前 ONI 与手机视频之间的伪同步关系。

------

# 七、阶段 5：条件性离线深度—彩色空间配准

优先级：**当前数据不适用；仅对未来含 Color + Depth 的同一记录启用**

当前 32 个 ONI 均无 Color，30 个手机 RGB 又不是与 ONI 同步采集的数据，因此本阶段对
当前数据的正确结果是：

```text
applicable = false
reason = missing_color_in_oni_and_unpaired_phone_rgb
```

不得用手机画面与 ONI 深度做空间配准，不得把 IR 冒充 Color，也不得让本阶段阻塞手机
RGB 的主体锁定、姿态缓存和标注。以下任务只保留为未来真正 RGB-D 记录的条件接口。

时间同步只说明“同一时刻”，还需要解决：

```text
彩色图像中的关节点像素
对应深度图中的哪个位置
```

## 任务

-  读取设备标定参数；
-  使用 OpenNI 坐标转换或官方 registration；
-  生成 `aligned_depth_to_color`；
-  保存配准参数和版本；
-  制作深度边缘与彩色人体叠加预览；
-  计算配准质量。

禁止简单使用：

```text
depth[y, x]
```

直接查询彩色图中的同坐标像素，除非已经明确完成配准。

## 重点抽查帧

- 静止站立；
- Lunge 最低点；
- Burpee 胸部接近地面；
- 起跳；
- 落地；
- 手臂快速移动；
- Wall Ball 下蹲最低点。

## 验收

- 当前 32 个 ONI 全部明确记录 `registration_not_applicable`；
- 当前手机 RGB 没有生成任何 ONI 深度映射；
- 对未来适用的同记录 RGB-D：人体轮廓和深度边缘基本对齐；
- 对未来适用的同记录 RGB-D：关节点附近不会大量采到背景深度；
- 对未来适用记录配准失败时标记 `registration_invalid`；
- 在任何情况下都不使用分辨率缩放冒充相机配准。

------

# 八、阶段 6：锁定主体运动者并建立统一姿态缓存与坐标层

优先级：**P0**

这一步开始与现有 MediaPipe 代码复用，但仍然是离线批处理。

## 6.1 主体运动者锁定

手机 RGB 以及 ONI 的 IR/深度预览中存在其他人时，必须先确定真正执行目标动作的运动者，
再生成正式姿态缓存或深度证据。不得简单假设“最大框”“画面中心的人”或“置信度最高的
人”始终是目标。手机 RGB 主线优先完成；ONI 辅助线单独建立目标轨迹，两批数据的
`target_track_id` 绝不互通。

推荐流程：

1. 在预览中由人工在目标运动者清晰出现的帧上指定 bbox 或关键点；
2. 给目标分配稳定的 `target_track_id`；
3. 使用位置连续性、人体框 IoU、骨段比例、外观和运动连续性维持身份；
4. 在遮挡、交叉、出画和重新入画处检测身份切换；
5. 可疑区间由人工重新指定主体，保存分段重初始化记录；
6. 全片抽查开头、中间、结尾以及每次人与人交叉的帧；
7. 只有 `target_locked=true` 且身份质量达标的区间才能进入标注、阈值统计和训练。

必须输出：

```text
target_track_id
target_selection_method
target_lock_confidence
other_people_present
other_person_count
identity_switch_suspected
identity_switch_segments
bystander_overlap
target_occluded
target_out_of_frame
manual_reinitialization_frames
```

处理原则：

- 原始全画面永久保留，目标裁剪、人体 mask 和其他人 ignore mask 只作为派生数据；
- 允许缓存所有人的候选轨迹用于审计，但正式 `mp_image_2d`、`mp_world_3d` 和
  `orbbec_surface_3d` 必须明确绑定 `target_track_id`；
- 不把其他人的姿态、深度、动作阶段或错误标签混入目标运动者；
- 背景人物不能自动作为负样本，除非经过独立标注；
- 主体无法可靠确认的片段标记 `target_ambiguous`，不得进入训练集；
- 切片后仍保留原始 `record_id`、`target_track_id` 和源帧范围，避免身份信息丢失。

多人质量标志：

```text
MULTI_PERSON_PRESENT
TARGET_NOT_LOCKED
TARGET_IDENTITY_SWITCH
TARGET_OCCLUDED
BYSTANDER_OVERLAP
TARGET_AMBIGUOUS
```

## 6.2 必须区分三类数据

### 1. MediaPipe 图像坐标

```text
mp_image_2d
```

用于：

- 现有 2D 规则；
- 画面相对位置；
- 现有地板代理；
- 可视化；
- 普通 RGB 产品部署。

### 2. MediaPipe World Landmarks

```text
mp_world_3d
```

用于：

- 当前 3D Assist；
- 三维关节角；
- 人体相对旋转；
- 2D/3D 一致性检查。

### 3. 奥比传感器表面三维点

```text
orbbec_surface_3d
```

用于：

- 真实深度尺度；
- 关节附近人体表面距离；
- 到地板的距离；
- 腾空与落地辅助；
- 深度监督。

不能统一命名为模糊的：

```text
3d_landmarks
```

## 每段视频保存

```text
timestamp
frame_index
record_id
target_track_id
raw_image_landmarks
raw_world_landmarks
smoothed_image_landmarks
smoothed_world_landmarks
normalized_landmarks
visibility
presence
pose_age
segmentation_quality
camera_view
multi_person_quality_flags
```

## 深度关节采样

不要采单个中心像素。

应：

1. 映射关键点到已配准深度；
2. 取局部邻域；
3. 结合人体 mask；
4. 去除 0 值；
5. 去除明显背景值；
6. 使用中位数或加权中位数；
7. 保存有效像素比例；
8. 质量不足时返回缺失值。

输出：

```text
depth_valid
depth_confidence
neighborhood_valid_ratio
registration_quality
occlusion_flag
```

## 人体归一化

-  双髋中心平移到原点；
-  以肩宽、髋宽或躯干长度归一化尺度；
-  构建身体朝向坐标系；
-  统一镜像语义；
-  保留原始坐标；
-  使用真实 timestamp 计算速度。

## 3D 质量门

检查：

- visibility；
- presence；
- 骨段长度跳变；
- z 方向异常；
- 左右身份交换；
- 结果过旧；
- 遮挡；
- 主体身份切换；
- 其他人与目标重叠；
- 2D、World 3D 与 Depth 冲突。

输出：

```text
3d_reliable
3d_confidence
failure_reason
```

## 验收

- 原始和平滑数据分开保存；
- 三种坐标不会相互覆盖；
- 深度失效时现有 MediaPipe 处理仍可正常运行；
- 镜像不改变左右动作语义；
- 每个三维结论都能追溯到质量门；
- 多人场景下所有正式姿态和深度证据都绑定同一个 `target_track_id`；
- 主体不确定或疑似换人区间不会进入训练和阈值统计。

------

# 九、阶段 7：建立判定、评分与纠正所需的正式标注体系

优先级：**P0**

建议分两批进行。

## 第一批：正误覆盖较完整的核心动作

优先：

1. Lunge（1 个标准意图 + 4 个错误意图）；
2. Burpee Broad Jump（3 个标准意图 + 4 个错误意图）；
3. Wall Ball（1 个标准意图 + 2 个错误意图）。

这 15 段先用于验证主体锁定、次数/阶段/事件/错误标注、评分 rubric 和纠正提示是否能够
闭环。它们仍然主要是单个录制意图和有限视角，试点通过不等于具备生产泛化能力。

## 第二批：其余已有正误动作

- Rowing（1 个标准意图 + 2 个错误意图）；
- Farmers Carry（2 个标准意图 + 1 个错误意图）。

## 第三批：当前只有标准意图的动作

- SkiErg；
- Sled Push；
- Sled Pull；

这三类动作可以先做动作识别、阶段和标准模板试点，但在补齐错误和边界 RGB 前，不训练或
发布“不规范动作识别”结论。ONI 中的错误目录可用于设计补拍清单和标签本体，但不能替代
手机 RGB 错误样本。

## 六类标注

### 视频级

```text
action_type
subject_id
camera_view
usable
overall_quality
recording_intent
recording_intent_verified
target_track_id
target_locked
other_people_present
```

`recording_intent` 来自文件名，只表示录制时计划展示的标准或错误动作；
`recording_intent_verified` 和后续错误标签才表示人工确认结果。

### 次数级

```text
rep_id
start_frame
end_frame
VALID / NO_REP / UNSURE
```

### 阶段级

例如 Lunge：

```text
stand
descent
bottom
contact
ascent
stand
```

Burpee Broad Jump：

```text
hands_down
chest_down
takeoff
flight
landing
stabilization
```

### 事件级

```text
rear_knee_contact
chest_contact
takeoff_left
takeoff_right
landing_left
landing_right
full_extension
hands_down
bottom_reached
```

### 错误区间

```text
error_code
start_frame
end_frame
severity
affected_side
confidence
```

建议错误码：

```text
NOT_DEEP_ENOUGH
NO_KNEE_CONTACT
NO_CHEST_CONTACT
LEAN_TOO_MUCH
KNEE_VALGUS
HIP_NOT_EXTENDED
KNEE_NOT_EXTENDED
ASYMMETRIC_TAKEOFF
ASYMMETRIC_LANDING
EXTRA_STEP
FOOT_STAGGER
```

每个错误标签还必须保存可解释证据，避免模型只输出不可验证的“动作不好”：

```text
criterion_id
phase
measured_value
unit
pass_range
fail_range
observability
evidence_source
annotator_id
review_status
```

### 评分与纠正标签

评分不是从“与某一条标准视频的相似度”直接换算。每次动作先通过可观测性和有效性质量门，
再输出版本化分项：

```text
validity_gate          # VALID / NO_REP / UNSURE，不被总分掩盖
completion_score       # 规则要求和动作幅度
control_score          # 稳定性、节奏、非必要步伐
symmetry_score         # 仅在该动作和视角可观测时计算
technique_score        # 动作特定技术标准
overall_score          # 可解释加权结果，0–100
score_confidence
unobservable_components
scoring_rubric_version
```

原则：

- `NO_REP` 必须单独显示，不能被较高的流畅度分数掩盖；
- 看不见的指标标记不可观测，不得凭空给满分或零分；
- 分数权重按动作分别定义并经教练/规则专家复核；
- 当前 30 个手机视频只用于验证评分数据结构和小样本排序，不用于确定最终分数分布；
- “标准”允许多个合规动作风格，不强制所有人复制同一运动者的轨迹。

每个错误区间关联一个候选纠正提示：

```text
correction_id
trigger_error_code
priority
cue_short
cue_detailed
target_phase
expected_change
suppress_if
review_status
```

实时指导每次最多突出 1–2 条最高优先级提示，按规则严重度、置信度、可纠正性和分数影响
排序；提示必须具体到阶段和身体部位，例如“下一次下蹲让髋部继续下降至膝线以下”，而不是
泛泛地说“姿势不标准”。低置信度、主体丢失或视角不可观测时输出“无法可靠判断/请调整
机位”，不输出确定性纠正。

### 数据质量标签

```text
rgb_valid
depth_valid
sync_valid
registration_valid
full_body_visible
floor_visible
target_locked
identity_switch_free
bystander_overlap
target_ambiguous
```

## 数据集划分

必须按受试者划分：

```text
train_subjects
validation_subjects
test_subjects
```

同一个 ONI `record_id` 派生的：

- ONI；
- ONI 彩色流；
- ONI 深度和 IR；
- 切片；
- 增强版本；
- 不同模态；

必须属于同一个数据分区。

当前手机视频是独立记录，不因为动作名、人员或拍摄日期相似就与当前 ONI 绑定。它同样
按自身 `subject_id` 划分；如果确认手机数据与 ONI 是同一位受试者，仍必须避免该受试者
跨越训练、验证和测试集合，但这属于受试者防泄漏，不代表两段视频是同步或配对数据。

多人场景的其他人不参与当前记录的 `subject_id` 和标签。若未来要利用背景人员数据，
必须拆成独立记录并重新获得主体身份、授权和标注。

## 验收

- 每次动作有开始和结束；
- 不规范动作不是简单标记为 `bad`；
- 同一次动作允许多个错误；
- 模糊情况允许使用 `UNSURE`；
- 每项评分能追溯到动作规则、观测值和版本；
- 每条纠正提示能追溯到已确认错误和发生阶段；
- 训练、验证、测试受试者互斥；
- 文件名中的“标准/错误”不会未经复核直接变成标签；
- 每个标注区间的主体身份明确，疑似换人区间被排除或单独标记。

------

# 十、阶段 8：并行开展 ONI Depth + IR 辅助研究

优先级：**P1，不阻塞手机 RGB 主线**

这部分不加入普通实时摄像头主链路。

它的用途是：

1. 研究地面、距离、接触、人体轮廓和空间运动；
2. 为同类错误建立可解释的几何假设；
3. 指导下一批手机 RGB 的拍摄与标注重点；
4. 判断未来同步 RGB-D 采集是否值得进行。

由于当前 ONI 没有 Color，且与手机视频不配对，当前深度值不能直接给手机帧生成标签，
不能直接标定普通 RGB 阈值，也不能做逐帧教师—学生蒸馏。只有 ONI 自身主体能够在 IR/
深度中可靠锁定并经过人工复核的区间，才允许形成深度原生证据。

## 8.1 地板平面

利用深度点云和 RANSAC 建立：

```text
floor_plane
floor_confidence
floor_inlier_ratio
floor_age_ms
```

## 8.2 关节到地板距离

重点计算：

- 左右膝；
- 脚跟；
- 脚尖；
- 手腕；
- 髋部；
- 胸部代理点。

## 8.3 三个核心动作

### Lunge

研究：

- 后腿识别；
- 后膝到地板距离；
- 接触持续时间；
- 站起后髋膝是否完全伸展；
- 躯干倾角。

### Burpee Broad Jump

研究：

- 胸部代理到地板距离；
- 手部支撑；
- 左右脚起跳时间差；
- 腾空；
- 左右脚落地时间差；
- 落地后碎步。

### Wall Ball

研究：

- 髋部最低高度；
- 膝关节活动范围；
- 站起完全伸展；
- 躯干稳定性；
- 动作最低点。

## 深度与现有系统的关系

```text
Depth 可靠
→ 作为额外证据或标签依据

Depth 缺失
→ 继续使用现有 2D + World 3D

Depth 与 MediaPipe 一致
→ 提高置信度

Depth 与 MediaPipe 冲突
→ 标记冲突，不覆盖原始结果
```

上述 MediaPipe 对照仅适用于未来同一记录中有可验证 RGB/姿态的情况。对当前 ONI，
`mp_image_2d` 不存在时不得伪造对照；可以单独研究 `oni_depth_ir_evidence`。

## 验收

- 深度规则只在离线 ONI 分析模式启用；
- 普通摄像头程序不需要深度；
- 深度结论有质量字段；
- 深度数据不能直接覆盖现有规则状态机；
- 当前 ONI 研究结论不伪装成手机 RGB 的逐帧标签或阈值真值。

------

# 十一、阶段 9：用数据重新标定现有规则和 DTW

优先级：**P0/P1**

## 9.1 数据驱动阈值

以下“标准、错误、临界”类别均指人工确认标签，不指文件名录制意图，也不指旧
`HYROX视频/` 示例的现有输出。

普通摄像头规则和 DTW 的主标定集必须来自手机/摄像头 RGB。当前 ONI 可用于提出候选
错误特征和检查几何合理性，但因模态、视角和记录均不同，不能替代手机 RGB 的正负样本。
当前手机数据存在类别单边缺口，补齐前只报告试点结果，不发布生产阈值。

对每条规则统计：

```text
标准动作分布
指定错误动作分布
临界动作分布
不同受试者分布
不同视角分布
不同速度分布
```

不要只设置一个硬阈值，应形成：

```text
PASS 区间
FAIL 区间
UNSURE 区间
最小持续时间
最小连续帧
最低姿态质量
允许视角误差
```

例如后膝触地，可以同时考虑：

```text
2D 膝盖—地板相对距离
MediaPipe World 运动趋势
奥比膝部表面—地板距离
最低点持续时间
动作阶段
```

## 9.2 完善困难负样本

单独收集：

- 只出现一帧异常；
- MediaPipe 关键点跳动；
- 遮挡引起的假触地；
- 衣服接近地板但身体未触地；
- 摄像头倾斜；
- 快速动作端点漏检；
- 非目标动作过渡；
- 看起来不标准但规则上合规。

## 9.3 多模板 DTW

标准动作库按以下维度组织：

```text
动作
├── 视角
├── 左右侧
├── 速度区间
├── 身体比例
└── 动作风格
```

DTW 特征使用：

- 关节角；
- 关节角速度；
- 身体中心轨迹；
- 骨段方向；
- 动作阶段；
- 可见度权重；
- 可靠 3D 特征。

不要直接用所有关键点原始坐标做单一全程 DTW。

## 9.4 分阶段 DTW

例如 Lunge：

```text
descent
→ 比较下降控制

bottom/contact
→ 比较深度、触地和稳定性

ascent
→ 比较伸展和躯干控制

stand
→ 比较完全站直
```

## 验收

- 每条关键规则有正例、反例和边界例；
- 阈值能通过数据分布解释；
- 规则系统在未见受试者上形成稳定结果；
- DTW 可以定位具体阶段；
- 新手机 RGB 派生、人工确认的数据基准不能退化；
- 删除 `HYROX视频/` 后保留其历史结果摘要用于兼容性追溯，但不参与阈值选择。

------

# 十二、阶段 10：训练可部署的轻量时序模型

优先级：**P1**

## 核心设计决策

由于最终用户主要使用手机或电脑摄像头，第一版正式部署模型应当是：

```text
RGB-only deployable model
```

输入只依赖普通摄像头可以获得的数据：

```text
MediaPipe 2D landmarks
MediaPipe World landmarks
visibility/presence
joint angles
joint velocity
angle velocity
bone vectors
现有触地与脚步代理
```

奥比深度不作为正式部署的必需输入。

## 训练前数据就绪门

当前 30 个手机 RGB 仍不能直接进入生产模型训练。每个待训练动作至少先满足：

- 标准、每个目标错误和临界/难判样本都有人体主体已锁定的 RGB；
- 至少多个受试者，且训练/验证/测试能够按受试者互斥划分；
- 包含产品支持的主要视角、距离、光照、服装和背景人员变化；
- 次数、阶段、事件、错误、可观测性和质量标签完成双人复核；
- 不同集合中不存在同一原视频的切片、增强或近重复帧泄漏；
- 先发布规则/DTW/传统模型基线，再决定 TCN 是否有真实增益。

具体最低数量由第一个标注试点的学习曲线和置信区间决定，清单不使用一个缺乏实证的固定
数字冒充充分性标准。

## 深度数据的用途

对未来真正同步的 RGB-D 数据，深度可以用于：

- 生成更可靠的触地事件标签；
- 训练时的辅助监督；
- 离线教师模型；
- 评价普通 RGB 模型误差；
- 深度消融实验。

推荐形成：

```text
离线深度教师或标注系统
            ↓
RGB-only 实时学生模型
```

这样不会导致产品必须绑定奥比硬件。

当前 ONI 没有 Color、也不与手机配对，因此现阶段只能先验证“离线深度证据系统”，不能
声称已经形成可逐帧监督手机 RGB 的教师模型。只有未来具备可靠跨模态对应关系时，才启用
上面的辅助监督、教师—学生和深度消融项。

## 训练顺序

### 第一步：传统机器学习基线

- Logistic Regression；
- Random Forest；
- XGBoost/LightGBM；
- SVM。

用于检查标签和特征是否可学习。

### 第二步：轻量 TCN

每个动作先训练独立模型：

```text
lunge_tcn
burpee_tcn
wall_ball_tcn
rowing_tcn
...
```

### 任务顺序

1. 动作阶段识别；
2. 动作开始和结束边界；
3. 瞬时事件检测；
4. 多标签错误识别；
5. VALID/NO_REP/UNSURE 辅助判断；
6. 分项评分与置信度辅助预测。

### 多任务输出

```text
Temporal Encoder
├── phase_head
├── boundary_head
├── event_head
├── error_head
├── validity_head
└── score_component_head
```

## 必做消融

```text
A. 仅 2D
B. 2D + MediaPipe World
C. 2D + World + 运动学特征
D. 2D + World + 深度训练监督
E. 规则系统
F. 规则 + TCN
G. 规则 + DTW + TCN
```

## 验收

- 阶段识别优于纯阈值状态机；
- 快速动作端点漏检减少；
- 动作重复切分减少；
- 至少若干核心错误获得稳定 Macro-F1；
- 未见受试者上有效；
- 可导出 ONNX；
- 模型不要求实时深度输入。

------

# 十三、阶段 11：异步接入现有实时程序

优先级：**P1/P2**

此阶段仍然不接奥比摄像头。

## 实时架构

```text
普通摄像头
    ↓
现有 MediaPipe callback
    ↓
最新 landmarks
    ├── 现有规则系统立即处理
    ├── 现有 DTW
    └── TCN latest-window 异步旁路
```

## 强制要求

-  神经网络不得在 MediaPipe callback 中同步运行；
-  队列只保留最新窗口；
-  忙时丢弃旧窗口；
-  不允许持续积压；
-  模型结果携带输入时间区间；
-  过期结果不得进入当前动作；
-  模型崩溃时现有程序继续运行；
-  关闭模型后行为恢复当前版本。

## 统一 Evidence

```json
{
  "source": "temporal_model",
  "name": "lunge_phase_tcn",
  "target": "phase",
  "value": "bottom",
  "confidence": 0.91,
  "start_ms": 2150,
  "end_ms": 2430,
  "quality_flags": []
}
```

其他 source：

```text
rule_2d
mediapipe_world
dtw
temporal_model
offline_orbbec_depth
```

其中 `offline_orbbec_depth` 只用于离线报告和训练数据，不出现在普通实时摄像头推理中。

## 融合优先级

```text
可观测性和质量门
    ↓
硬规则
    ↓
时序模型
    ↓
DTW
    ↓
MediaPipe World 3D Assist
```

硬规则明确失败时，模型不能直接覆盖。

例如：

```text
rule_result = NO_REP
neural_result = VALID
fusion_result = NO_REP
conflict = true
```

冲突样本自动加入困难样本池。

## 验收

- 神经网络关闭时现有功能完全正常；
- 模型异常不会导致摄像头停止；
- 显示帧率和 sensor-to-photon 延迟不明显恶化；
- 队列不会随运行时间增长；
- 结果不会错配到下一次动作。

------

# 十四、阶段 12：完整评测和发布门槛

优先级：**P1，贯穿全过程**

## 阶段识别

- Frame Accuracy；
- Macro-F1；
- Segmental F1；
- Edit Score；
- 过度切分次数；
- 阶段输出延迟。

## 瞬时事件

- Precision；
- Recall；
- F1；
- ±50/100/200 ms 命中率；
- 平均时间误差；
- 重复检测率；
- 漏检率。

## 计数

- Count MAE；
- Count exact match；
- VALID Precision/Recall；
- NO_REP Precision/Recall；
- UNSURE 质量；
- 重复计数率；
- 候选动作漏检率。

## 错误识别

- 每类错误 Precision/Recall/F1；
- Macro-F1；
- Macro-AUPRC；
- 多标签 mAP；
- 错误区间边界误差。

## 评分与纠正指导

- 与教练分项评分的 MAE、Spearman 相关和加权一致性；
- 同一动作重复评分稳定性；
- `NO_REP` 与总分逻辑一致性；
- 分数置信度校准误差；
- 不可观测指标的误判率；
- 首要纠正建议 Top-1/Top-2 命中率；
- 建议与实际错误阶段、身体部位的一致率；
- 错误消失后旧提示的残留率；
- 主体丢失、低质量和未知视角下错误强提示率；
- 教练对建议“正确、具体、可执行”的独立评分。

## 泛化

分别测试：

- 未见受试者；
- 未见视角；
- 不同设备；
- 不同动作速度；
- 低光；
- 遮挡；
- 宽松衣物；
- 背景人物；
- 摄像头倾斜；
- 部分身体出画。

## 实时性能

- MediaPipe P50/P95；
- 规则计算 P50/P95；
- TCN P50/P95；
- 姿态结果年龄；
- 显示延迟；
- sensor-to-photon；
- 队列丢弃率；
- 过期结果比例；
- 长时间运行内存增长。

## 发布门槛

任何新模型进入默认产品前，必须满足：

- 当前兼容性回归和新版本化数据基准均不退化；
- 普通摄像头功能不退化；
- 未安装 OpenNI 仍可正常运行；
- 模型关闭可以完整降级；
- 计数准确率不能只在训练人员上提升；
- 延迟没有明显恶化；
- 新模型版本和数据版本可追溯；
- 多人数据的主体锁定通过审计，训练样本中没有混入旁观者轨迹；
- 判定、分项评分和纠正提示都可追溯到结构化证据与 rubric 版本；
- 网页示例替换与新回归集、预计算缓存和文档版本一致；
- 示例视频不得被当作唯一标准模板或测试集样本。

------

# 十五、当前明确暂缓的任务

以下内容不进入目前的实施范围。

## 暂缓 1：奥比实时摄像头接入

不实现：

```text
Orbbec Capture Thread
实时 Depth Worker
实时 RGB-D 配对
奥比断线处理
实时 depth-to-color registration
```

已有 ONI 只通过离线工具处理。

## 暂缓 2：让正式模型依赖深度输入

正式产品模型不能要求：

```text
实时奥比深度
```

深度只作为辅助监督或研究对照。

## 暂缓 3：大型视频网络

暂不优先实现：

- 大型 Transformer；
- 原始 RGB-D 视频模型；
- PoseC3D 正式部署；
- 大型视觉语言模型直接判断动作。

## 暂缓 4：不透明的单一总分

不暂缓评分体系本身；从标注阶段就实现 `score_breakdown`、证据、置信度和 rubric 版本。
在计数、阶段、事件和错误识别稳定前，只把分项分数作为试验输出，不发布缺少解释的单一
总分，也不宣传为教练级最终结论。

## 暂缓 5：自然语言大模型直接看视频

先产生结构化证据：

```text
phase
error_code
severity
affected_joint
measured_value
confidence
```

再通过模板生成提示。

纠正指导本身不暂缓：第一版使用经过专家复核的结构化规则与提示模板，确保相同证据产生
稳定、可测试的建议。后续语言模型只能润色表达，不能绕过判定证据自行创造错误。

------

# 十六、推荐交给 Codex 的实际轮次

第 1–5 轮的“当前完成状态”是完成当时的历史快照；其中“手机接口为空”和
`phone_rgb_future` 描述不再代表最新数据现状。最新现状及后续要求以第零节和第 6 轮为准。

每轮都应附带：

```text
不得删除或重构现有功能；
不得修改默认实时运行链路；
新功能必须默认关闭；
完成后运行现有全部测试、旧兼容性视频回归和当前已建立的新数据基准。
完成后实时更新readme和使用说明等内容。
```

## 第 1 轮：冻结现有基线

实现：

- Git 基线标签建议；
- 环境信息导出；
- 旧兼容性视频结果保存并标记 `legacy_provisional`；
- 延迟基线；
- 一键回归脚本。

## 第 2 轮：ONI 数据目录和 record manifest

实现：

- ONI `record_id`；
- 文件 hash；
- manifest 校验；
- 原始数据只读检查；
- 按现有中文文件名解析动作、录制意图、序号和视角；
- 录制意图与确认标签分离；
- 当时的 `phone_rgb_future` 可选空接口；
- 禁止默认建立 ONI—手机配对；
- 主体运动者和多人干扰字段。

当前完成状态（2026-07-24）：

- 已建立 `datasets/hyrox/` 全部标准目录；
- 已为 32 个 ONI 生成稳定 `record_id`；
- 已制作 32 个独立完整副本，共约 11.86 GiB，不使用硬链接；
- 源文件和备份均设为只读，32/32 SHA256 一致；
- 已生成 ONI manifest、当时为空的手机接口、schema 和验收报告；
- 未生成任何 ONI—手机配对；
- `subject_id`、使用授权范围和主体运动者选择因缺少可靠人工信息而明确保持待确认，
  不从文件名或画面位置猜测。

## 第 3 轮：ONI 审计

实现：

- `oni-inspect.exe`；
- 流类型检测；
- 时间戳和 FPS 报告；
- 深度质量报告；
- 批量扫描。

当前完成状态（2026-07-24）：

- 已实现并构建隔离的 Windows x64 `tools/oni_bridge/oni-inspect.exe`，使用
  OpenNI2 2.3.0.65，仅包含 `OniFile` 文件驱动，不连接摄像头；
- OpenNI DLL、文件驱动和 MinGW 运行库均封装在 `tools/oni_bridge/`，没有加入
  `src/`、默认 Python 依赖或产品运行入口；
- 已对 manifest 中 32/32 个 ONI 完成全片播放审计，并为每条记录保留
  `record_id`、中文文件名和 manifest SHA256；
- 32/32 均成功打开并完整播放，解码错误、时间戳不递增、frame index 断点、
  异常间隔和估算丢帧均为 0；
- 实际流组合为：Color 0/32、Depth 32/32、IR 32/32，因此分类结果为
  `A=0, B=32, C=0, D=0, E=0`；
- Depth 共 18,709 帧，单条 32–1,974 帧，整幅 0 值比例范围为
  0.428004–0.766279；
- 已输出 32 份逐条 JSON、`batch_report.json`、`invalid_records.json` 和
  `audit_summary.md`；全部 B 类单独列出，明确表示“可读但不满足 Color + Depth
  的 RGB-D 入口条件”，不将其误写为动作标签不合格；
- 手机接口未参与审计或补齐分类；主体运动者、多人员干扰、文件名录制意图和使用授权
  仍保持待确认，不能因本轮文件审计通过而绕过后续人工审计。
- 完整回归通过：Python `551 passed`、Node `16 passed`、旧兼容视频 `8/8`，
  无摄像头/无 ONI/神经网络关闭冒烟和当前主机物理摄像头检查均通过。

## 第 4 轮：ONI 无损导出

实现：

- Color；
- 16 位 Depth；
- IR；
- index.csv；
- metadata.json；
- 确定性测试。

当前完成状态（2026-07-24）：

- 已实现并构建隔离的 `tools/oni_bridge/oni-export.exe`，与审计器共用仅含
  `OniFile` 的离线 OpenNI2 运行环境，不连接摄像头；
- 32/32 个 ONI 已完整导出到 `datasets/hyrox/extracted/{record_id}/`；
- 当前实际导出 18,709 帧 Depth 和 18,713 帧 IR，均为 `640×480` 小端
  `uint16 NPY`，载荷共 24,228,045,828 字节；真实深度数值没有被伪彩预览替代；
- 每帧索引保留 `output_frame`、原始 `source_frame_index`、`timestamp_us`；
  Depth 另保留 `depth_scale` 和 `invalid_pixel_ratio`；
- 32 个 `depth_preview.mp4` 仅作为派生查看文件；当前无 Color，因此未生成
  `color.mp4`，工具仍保留 RGB888 无损帧和 Color 预览接口；
- 全部导出与第 3 轮审计的流存在性、分辨率、格式、帧数、首末 frame index 和
  timestamp 一致，校验错误为 0；
- 已用 `oni_farmers_carry_002` 执行真实重复导出，帧数、三个流的 index SHA256
  和逐帧内容聚合 SHA256 全部一致；
- 输出 `batch_report.json`、`determinism_report.json` 和
  `export_summary.md`；手机接口、录制意图和主体运动者均未参与导出判定。

## 第 5 轮：ONI 内部同步

实现：

- frame index 检查；
- 时间戳最近邻；
- 配对误差统计；
- 不合格 ONI 记录标记。

当前完成状态（2026-07-24）：

- 已实现确认采集同步且 frame index 匹配时的 `frame_index` 配对，以及默认的
  `nearest_timestamp` 配对；没有直接假设第 N 张 Color 等于第 N 张 Depth；
- 配对表保留 RGB/Depth 输出帧号、原始 frame index、原始 timestamp、`delta_us`
  和同步方法，并计算覆盖率、offset、中位/P95/最大绝对误差；
- 已实现 `good`、`usable`、`video_level_only`、`invalid` 质量门和精细事件训练
  排除标记；
- 当前 32 个 ONI 均无 Color，因此可执行 Color–Depth 同步为 0，32/32 均明确记录
  `applicable=false`、`reason=missing_color`、`video_level_only`，实际配对数为 0；
- 没有把 IR 当作 RGB，没有生成虚假配对，32 条全部排除出需要精确 RGB–Depth 对齐
  的事件训练；
- 已输出逐记录 `sync_report.json`、带表头的空 `color_depth_pairs.csv`、
  批量报告、排除清单和同步摘要；
- 已生成独立的手机时间轴 schema，仅包含 `record_id`、`source_frame_index`、
  `timestamp_ms`、`source_fps`、`timestamp_source`；当前 ONI—手机配对、偏移、
  漂移和互相关同步均为 0。

## 第 6–10 轮共同补强目标

第 6–10 轮不仅要“把手机视频放进数据目录”，还要一次性为后续自动动作识别、主体锁定、
2D/3D 坐标化、判定评分、纠正提示和关节点低延迟建立可复用的数据契约。五轮结束时应形成：

```text
30 段 phone_rgb 的可追溯数据底座
→ 经人工确认的目标运动者轨迹
→ 多后端原始姿态与统一坐标缓存
→ 动作/空闲/过渡/阶段/事件/错误/器械证据标注
→ 自动动作识别、规则判定、评分、提示和低延迟显示的产品契约
```

共同原则：

- 最大化利用现有数据不等于重复切片后把样本数虚报变多；同一原视频的切片、增强、姿态
  后端输出和不同模态始终属于同一数据分区；
- 文件名、现有规则、DTW、MediaPipe、YOLO/RTMW 和 ONI 目录都只能产生
  `proposal` 或辅助证据，未经人工确认不得成为训练真值；
- 当前 ONI 与手机视频仍然不配对。第 6–10 轮只统一标签本体和证据 schema，不生成任何
  手机帧对应的 ONI 深度标签；
- 离线多模型和非因果平滑可以帮助标注复核，但正式实时特征必须另存为严格因果版本，
  不得用未来帧制造看似更低的延迟；
- “显著提升”必须通过同一冻结数据、同一主体分区和成对指标比较证明；没有未见受试者时
  只能报告内部试点和置信区间，不能写成生产级结论；
- 旧 8 项视频继续只做软件兼容性回归；新手机示例、训练、模板和评测角色必须互斥并在
  manifest 中显式声明。

## 第 6 轮：接入手机 RGB、冻结数据角色并建立三类基线

这是第 1–5 轮完成后的下一轮，不再执行对当前数据不适用的 Color–Depth 配准。

### 6.1 手机数据正式接入

实现：

- 为 30 个真实手机视频生成独立且稳定的 `record_id`、SHA256、只读完整副本和解码审计；
- 将 `phone_rgb_future` 迁移为 `phone_rgb`，旧值仅作为读取兼容别名，不再生成；
- 原样保存正常中文文件名，明确排除 30 个隐藏 AppleDouble 元数据文件；
- 逐帧解码到声明的最后一帧，保存真实帧数、时间戳、分辨率、FPS、旋转、编码、色彩空间、
  音轨、时长和异常帧；
- 建立独立时间轴，不与 ONI 配对；`paired_group_id` 保持 `null`；
- 记录使用授权、受试者身份确认状态、动作专家审核状态和是否允许用于训练、评测、示例；
- 将第 0.4 节选定的 8 段视频登记为 `example_candidate`，但默认
  `training_eligible=false`、`golden_eligible=false`，直到数据角色审定；
- 输出源目录、只读副本和 manifest 三方 SHA256 一致性报告。

### 6.2 覆盖矩阵与不可观测项盘点

除了动作和录制意图，还要统计：

```text
subject_id / subject_pending
camera_view
device_id
resolution / fps
distance_bin
lighting
clothing
other_people_present
equipment_visible
floor_or_lane_visible
full_body_visible
recording_intent
expected_error_unverified
```

为每个动作分别生成：

- 合规、错误、边界和未知样本缺口；
- 受试者、视角、速度和设备缺口；
- 球、雪橇、绳索、把手、目标板、地面线、壶铃/沙袋和测功器显示区域的可见性缺口；
- 仅靠人体姿态无法判断的规则清单，例如实际负重、墙球目标命中、雪橇越线和测功器距离；
- `idle`、准备、离场、动作过渡和非目标人物可用区间的候选清单。候选区间仍需第 9 轮
  人工确认，背景人物不能自动变成负样本。

### 6.3 冻结三类基线

建立三个相互独立的基线：

1. `legacy_compatibility_baseline`：现有 8 项旧视频，只验证软件兼容；
2. `phone_rgb_data_baseline_v1`：30 段手机视频的解码、姿态检出率、主体/全身可见率和
   当前规则输出，不把文件名当真值，不计算准确率；
3. `realtime_latency_baseline_v1`：当前 Lite/Full 模型、当前摄像头链路和当前平滑配置的
   推理 P50/P95、帧年龄、队列丢弃、显示延迟待测项和 sensor-to-photon 待测项。

同时冻结 `coordinate_baseline_v1`：

- 原始 image normalized 2D、pixel 2D 和 MediaPipe World 的可用率；
- 每个关节缺失率、可见度、骨段长度变异、左右交换、跳点率和 2D–World 冲突率；
- 明确 MediaPipe World 是人体中心相对坐标，不冒充相机坐标或公制场地坐标。

### 6.4 输出

```text
datasets/hyrox/manifests/phone_records.json
datasets/hyrox/manifests/data_roles_v1.json
datasets/hyrox/reports/phone_rgb_decode_audit.json
datasets/hyrox/reports/coverage_gap_matrix_v1.json
datasets/hyrox/reports/observability_gap_v1.json
reports/baseline/phone_rgb_data_baseline_v1.json
reports/baseline/realtime_latency_baseline_v1.json
reports/baseline/coordinate_baseline_v1.json
```

### 6.5 验收

- 30/30 真实视频进入 manifest，AppleDouble 计数为 30，且 AppleDouble 对应训练记录为 0；
- 30/30 从源文件解码到声明的最后一帧，当前总帧数应为 15,515；
- `phone_rgb_future` 不再出现在新生成记录中；
- 数据角色明确且不存在示例、模板、训练和测试的静默重叠；
- ONI—手机配对数仍为 0；
- 已得到后续主体锁定、3D、器械证据、动作识别和延迟优化所需的明确缺口清单。

## 第 7 轮：主体锁定、器械/场景候选和低延迟 ROI

**实施状态（2026-07-24）：工程实现与首轮逐片视觉审计已完成；验收状态为
`passed_with_independent_second_human_review_pending`。30/30 已生成规范目标轨迹、
器械/场景候选和 ROI 对照。独立第二名人类逐帧复核尚未完成，因此身份切换漏检率和
复核分歧保持 `null`，不伪造双人一致性。**

### 7.1 目标运动者锁定

实现：

- 人工在目标清晰帧指定真正运动者的 bbox/关键点，保存选择人、时间和理由；
- 为手机数据建立稳定且仅在本记录内有效的 `target_track_id`；
- 缓存所有人物候选轨迹，但正式姿态、阶段、事件和动作标签只能绑定目标轨迹；
- 结合 bbox IoU、中心速度、骨段比例、外观嵌入和动作连续性维持身份；
- 检测其他人进入、遮挡、交叉、出画、重新入画和疑似身份切换；
- 支持分段人工重初始化，并保存每次重初始化前后的帧范围；
- 生成其他人 ignore mask；背景人物只有拆成独立记录并完成人工确认后才能利用；
- `target_ambiguous`、`TARGET_IDENTITY_SWITCH`、严重遮挡区间禁止进入训练、模板和阈值统计；
- 30/30 全片检查开头、中间、结尾及全部人物交叉点。

### 7.2 器械和场景候选层

为后续判断“仅姿态无法覆盖的 HYROX 规则”建立独立候选轨迹：

```text
wall_ball
wall_target
sled
rope
erg_handle
erg_display_roi
farmers_carry_weight
lunge_load
lane_or_finish_line
floor_region
```

要求：

- 第 7 轮只形成候选 bbox、mask、轨迹和可见性，不把通用检测器结果直接当比赛规则真值；
- 器械候选必须带 `object_track_id`、类别置信度、遮挡、出画和与目标运动者的关联置信度；
- 实际重量、机器距离、墙球是否命中指定高度等无法可靠观测的项目标记
  `UNOBSERVABLE`，不得从人体动作猜测；
- 目标板、地面线和器械区域允许人工校准，保存原始画面坐标和版本。

### 7.3 低延迟目标 ROI

离线验证“全图低频检测 + 目标 ROI 高频姿态”的候选方案：

- 人体检测器负责多人候选与重新捕获，跟踪器在检测间隔内保持目标；
- ROI 必须保留足够上下文，包括手、脚、球/把手和地面，不得为了速度裁掉判定证据；
- 保存 ROI 到原图的可逆仿射变换，所有 2D 关键点能准确映回原始像素；
- 比较全帧姿态与 ROI 姿态的检出率、关节误差、身份稳定性、推理时间和端点漏检；
- 检测频率、ROI padding 和重新捕获阈值通过当前 30 段视频离线扫描选择，但不使用测试
  角色视频调参；
- ROI 方案未同时通过精度和延迟门时保持关闭。

### 7.4 输出与验收

```text
datasets/hyrox/tracks/{record_id}/people.jsonl
datasets/hyrox/tracks/{record_id}/objects.jsonl
datasets/hyrox/tracks/{record_id}/ignore_masks/
datasets/hyrox/reports/target_lock_audit_v1.json
datasets/hyrox/reports/object_scene_visibility_v1.json
datasets/hyrox/reports/roi_latency_accuracy_ablation_v1.json
```

- 30/30 有人工初始化、目标轨迹和全片审计结果；
- 每个正式可用区间 `target_locked=true` 且可追溯到同一 `target_track_id`；
- 身份切换漏检率和人工复核分歧被量化，不以“最大框/画面中心”作为默认真值；
- 器械不可见不会导致人体姿态链路失败，只会降低对应规则的可观测性；
- ROI 若启用，映回原图的几何误差、姿态检出率和 P95 推理延迟均有对照报告。

实测验收结果：

- 30/30 完成 AI 辅助人工视觉初始化与开头/中间/结尾/候选交叉点检查；
- 所有正式帧绑定记录内规范 `target_athlete_001`，同时保留
  `source_candidate_track_id` 追溯候选来源；
- 15,166/15,515 帧 `target_locked=true`，349 帧因目标缺失、陈旧传播或疑似身份切换
  排除；5,640 帧存在其他人物并生成同数 ignore mask；
- `phone_sled_push_005` 精确在第 550 帧从 `person_candidate_001` 分段重初始化到
  `person_candidate_002`，两段仍属于同一规范运动员，重叠候选不进入 ignore mask；
- 审计 3,235 个交叉/遮挡候选并标出 2 个疑似身份切换；漏检率和复核分歧等待独立
  第二名人类复核，当前不作估计；
- 10 类器械/场景共生成 43,532 个候选搜索区域，确认可见数为 0；实际重量、机器距离、
  墙球命中、正式终点线等不可可靠观测项保持 `UNOBSERVABLE`；
- ROI 对 15,515 帧完成全帧/裁剪双路消融，检测间隔 10、padding 1.6；检出率由
  96.758% 变为 97.267%，仿射回映 P95 误差为 `2.27e-13 px`，但关节误差 P95 为图像
  对角线的 9.819%，含检测器 ROI 管线 P95 18.761 ms 对全帧 20.433 ms，精度门和
  延迟门均未通过，故 `roi_enabled=false` 且默认产品链路不变。

## 第 8 轮：多后端离线姿态教师、统一 3D 坐标层和关节点时延审计

### 8.1 多后端原始姿态缓存

对已锁定目标至少运行：

```text
MediaPipe Lite
MediaPipe Full
```

本机已有且依赖可用时，可以把 YOLO Pose、RTMW 或其他后端作为离线教师候选，但不得因此
增加产品默认依赖。每个后端分别保存：

- 后端名、模型 hash、输入分辨率、设备、软件版本；
- 原始 2D、原始 World/3D（若有）、可见度、存在度、bbox、mask；
- 推理开始/结束时间、输入帧时间戳和结果年龄；
- 缺失关节、左右语义和该后端原生关键点 schema；
- 映射到统一 33 点 schema 的结果及映射损失。

多后端共识只能生成：

```text
teacher_proposal
teacher_agreement
teacher_uncertainty
review_required
```

不得自动替代人工标签。后端差异最大的帧优先进入第 9 轮复核，以主动学习方式最大化有限
标注时间的价值。

### 8.2 统一坐标层

每帧同时保存并明确命名：

```text
image_normalized_2d          # [0,1] 图像坐标
image_pixel_2d               # 原始画面像素坐标
camera_ray_direction_3d      # 只有可靠/估计内参时生成，不包含绝对深度
mp_world_body_3d             # MediaPipe 人体中心相对 World
body_canonical_3d            # 髋中心、身体尺度和朝向归一化
oni_surface_metric_3d        # 仅 ONI 自身、仅第 11 轮可靠深度区间
```

要求：

- 不把单目 MediaPipe World 宣称为真实相机外参坐标或绝对公制坐标；
- 优先读取经过验证的相机内参；若后续可控制同一手机/电脑摄像头，使用
  Checkerboard/ChArUco 标定并保存内参矩阵、畸变参数、分辨率、设备 ID 和标定误差；
- 当前已录手机视频缺少可靠内参时，只能使用带 `estimated_intrinsics`、来源和不确定度的
  `camera_ray_direction_3d`，不能从射线方向输出虚假绝对距离；
- 未知内参的视频不因无法获得公制相机坐标而被丢弃，仍可用于 2D、body canonical 3D、
  阶段、错误和动作识别；
- 保留原始坐标，归一化、镜像修正、骨长约束和滤波全部作为派生层；
- 建立髋中心原点、躯干尺度、左右轴、竖直轴和面向轴，保存变换矩阵和逆变换；
- 镜像视频只改变显示，不改变人体左右语义；
- 对器械和地面使用原图/相机坐标，不能直接与 body canonical 坐标混算；
- 3D 每个关节保存置信度/协方差代理、质量原因和来源后端；
- 检测骨段突变、左右交换、z 跳变、人体朝向翻转和 2D–3D 冲突；
- 可选的骨长约束或运动学优化必须保留优化前结果，并证明没有抹去真实快速动作。

### 8.3 分离三套时序数据

必须生成互不覆盖的：

```text
raw_pose
causal_analysis_pose
low_latency_display_pose
```

- `raw_pose`：后端原始结果，用于审计和重新处理；
- `causal_analysis_pose`：只使用当前及历史帧，供计数、阶段、事件和模型训练/部署；
- `low_latency_display_pose`：允许受约束的短时预测，只用于骨架显示，不直接作为规则真值；
- 离线双向平滑/未来帧插值另存为 `offline_annotation_assist`，只能帮助人工标注；
- 每个派生点保存 `source_frame_id`、`source_timestamp_ms`、`generated_at_ns`、
  `filter_version` 和 `prediction_horizon_ms`。

### 8.4 关节点级延迟与稳定性审计

“关节节点延迟”不能只看一次模型调用耗时。对手腕、脚踝、膝、髋、肩和躯干分别统计：

```text
joint_temporal_lag_ms
event_endpoint_error_ms
pose_age_at_render_ms
jitter_normalized
overshoot_after_reversal
missing_rate
left_right_swap_rate
bone_length_variation
```

实施：

- 在手机视频中先由一名复核者标记一组清晰的起跳、落地、触底、完全伸展、手臂反向和脚
  接触时间点，作为首轮离线事件时间锚；第 9 轮双人复核后更新最终延迟报告；
- 比较 Raw、当前平滑、One Euro 不同档位和受限常速度预测的延迟—抖动 Pareto；
- 分快速关节与稳定核心关节设置不同参数，不用同一滤波强度处理全身；
- 分别测试 Lite/Full、全帧/ROI、CPU/GPU（可用时）和不同输入尺寸；
- 实际 sensor-to-photon 仍须在目标电脑和手机上用 120/240 FPS 外部录像测量，软件时间戳
  不得冒充物理测量；
- 选择参数时同时约束反向运动过冲、脚底漂移和端点漏检，不能只追求视觉“跟手”；
- 记录当前设备做不到 60 FPS 的情况，不把请求 FPS 当成真实 FPS。

### 8.5 ONI 兼容接口

第 8 轮只建立共享的 `criterion_id`、质量字段和坐标术语，使第 11 轮 ONI Depth + IR 证据
能够进入相同报告结构。当前仍然禁止：

```text
phone frame ↔ ONI frame
phone joint ↔ ONI depth pixel
unpaired teacher-student distillation
```

### 8.6 输出与验收

```text
datasets/hyrox/pose_cache/{record_id}/{backend}/raw_pose.*
datasets/hyrox/pose_cache/{record_id}/causal_analysis_pose.*
datasets/hyrox/pose_cache/{record_id}/low_latency_display_pose.*
datasets/hyrox/reports/backend_agreement_v1.json
datasets/hyrox/reports/coordinate_quality_v1.json
datasets/hyrox/reports/joint_latency_jitter_v1.json
datasets/hyrox/reports/pose_observability_v1.json
```

- 正式姿态 100% 绑定已确认 `target_track_id`；
- 所有坐标层可追溯、可逆转换处保存逆变换，且不会相互覆盖；
- 单目 3D、估计内参和真实深度三者在字段与 UI 中明确区分；
- 多后端分歧不会静默平均成伪真值；
- 选定的因果分析滤波相对当前基线在延迟—抖动组合指标上改善，且关键事件漏检不恶化；
- 选定的显示预测降低可测关节滞后时，反向过冲、脚底漂移和错误骨长必须通过门限；
- 没有可靠相机标定、同步深度或其他 3D 真值时，只报告相对 3D 稳定性和一致性，不报告
  手机单目 3D 的绝对空间精度；
- 没有外部高速录像时，sensor-to-photon 保持 `not_measured`，不伪造“显著降低”。

实测验收结果：

- 30/30 段、15,515 帧完成 Lite/Full 独立原始缓存；Lite 成功 15,071 帧、正式目标绑定
  14,790 帧，Full 成功 14,766 帧、正式目标绑定 14,721 帧；正式姿态只能来自已绑定
  `target_athlete_001` 的帧；
- 每个后端保存原始 2D/World、bbox、RLE mask、模型与软件版本、推理时间、统一 33 点及
  映射损失；2,439 个高分歧帧列入第 9 轮主动复核，`teacher_proposal` 明确不是
  ground truth，禁止静默平均或替代人工标签；
- 已生成 `image_normalized_2d`、`image_pixel_2d`、`camera_ray_direction_3d`、
  `mp_world_body_3d` 和可逆 `body_canonical_3d`，并审计骨长、z 跳变、左右交换、
  朝向翻转及 2D–3D 冲突；手机内参仍为 `estimated_intrinsics`，不输出虚假绝对深度或
  手机单目绝对 3D 精度；
- 每条记录分别保存严格因果 `causal_analysis_pose`、仅显示
  `low_latency_display_pose` 和使用未来帧的 `offline_annotation_assist`，三者各覆盖
  15,515 帧且互不覆盖；
- `joint_adaptive_round8_v2` 的延迟—抖动组合分数为 `0.256649`，相对当前 responsive
  基线 `0.271957` 改善约 5.63%，缺失率同为 `0.082080`；15 ms 受约束显示预测把当前
  聚合指标中的滞后从 0.4167 ms 降为 0 ms，并通过反向过冲、支撑脚漂移与骨长门，
  但仍禁止驱动规则、计数或正式报告；
- CPU/XNNPACK 已完成 Lite/Full × 原尺寸/长边 640/ROI 的 6 组矩阵，每组 90 帧；
  当前 Windows 环境未配置可用 GPU delegate，故 GPU 明确记为未测试，不伪造结果；
- 30 个首轮事件锚和 5 张总览已完成 AI 辅助视觉复核，只用于本轮初步时延审计；第 9 轮
  仍须独立双人逐帧复核后才能冻结最终事件报告；
- 第 7 轮 ROI 双门失败结论不变，默认产品链路继续关闭 ROI；手机—ONI 帧/深度像素
  配对及未配对蒸馏仍禁止，`sensor_to_photon` 保持 `not_measured`；
- 产物完整性验证确认 Lite/Full 原始姿态、坐标、后端共识、因果、显示和离线辅助各
  15,515 行，正式目标绑定/身份/用途/未来帧约束违规数为 0；第 8 轮状态为
  `passed_with_round9_double_reviewed_event_anchors_pending`。

## 第 9 轮：全 30 段动作/OOD 标注与三个核心动作精标试点

### 2026-07-25 实施状态

第 9 轮的工程层和 AI 辅助试点复核已经收尾：新增 `tools/run_round9_annotations.py` 和
`tools/review_round9_annotations.py`，以第 8 轮主体绑定的严格因果姿态生成 30/30 连续
时间轴候选、15/15 核心动作的次数/阶段/事件/错误候选、器械场景可观测性、评分纠正
schema、主动复核队列及本节要求的 7 个正式路径。第二遍复核同时使用原始 RGB 关键帧、
严格因果姿态、离线中心平滑姿态、Lite/Full 分歧和动作特定序列检查，修正了 18 段动作
边界和 5 段核心动作锚；当前共生成 70 个主体绑定 rep 候选。可复现复核决定保存在
`configs/annotation/round9_ai_review_decisions_v1.json`，复核报告保存在
`round9_multimethod_review_v1.json`。

第 9 轮尚不能标记为完整验收通过：当前没有独立人类标注者，30 段动作区间、15 段逐帧
精标、关键事件/错误和评分子集仍需两名具备规则知识的人员复核；授权和受试者身份也仍待
确认。`round9_implementation_summary.json` 因此如实记录
`engineering_complete_human_ground_truth_and_double_review_pending`，一致性指标保持
`null`，不得以两次 AI 辅助复核伪造双人人类一致性或训练真值。当前内部状态为
`round9_ai_assisted_pilot_complete_human_release_gate_pending`；所有文件名、姿态和器械
proposal 仍保持非真值、`UNSURE` 且训练不可用。复核记录见
`round9_multimethod_review_v1.json`，后续人类复核优先级见
`round9_active_review_queue_v1.json`。

**2026-07-26 单人快速复核接入更新：**已校验并接入
`hyrox_human_review_a_20260726_181146` 的 30/30 条 reviewer A 记录。动作类别 30/30
有人确认；29/30 有人确认的可用动作区间；`phone_sled_push_005` 被人工明确标为整段
不可用，因此没有为了凑齐数量而提升为可用区间。手机 30 条和 ONI 32 条的授权用途已按
`internal_storage`、`manual_annotation`、`model_training`、`model_validation`、
`model_testing`、`internal_demo`、`public_example` 和 `external_reviewer_access` 逐项
确认为允许。subject ID 按当前要求暂不处理，只允许内部实验回退为按原视频分组，不改变
`subject_pending`，也不代表发布身份门通过。

复核覆盖层保存在
`datasets/hyrox/reviews/human_quick_review_application_v1.json`，进度报告保存在
`datasets/hyrox/reports/round9_human_review_progress_v1.json`。原 AI proposal 及协议冻结
hash 未被改写。由于本次快速复核没有 rep/event 逐帧标注，且只有一名人类复核者，15 个
核心动作的精标完成数仍为 0，`reviewed_ground_truth`、训练资格、双人一致性和第 8 轮
事件锚冻结仍不得提升。

**2026-07-26 当前复核策略与网站实施更新（覆盖本轮此前的“双人复核”要求）：**

- 第 9–11 轮当前阶段采用 `single_human_review_sufficient_for_current_stage`，一名人工
  复核者完成并保存完整审计记录即可通过当前人工门；不生成也不伪造双人一致性指标。
  第二名复核者与仲裁作为未来质量升级任务，不再阻断本阶段标注推进。
- 人工复核台新增“15 段核心动作逐次/事件精标”和“ONI Depth/IR 主体人工复核”两个任务。
  核心动作逐次保存 `rep_id`、开始帧、结束帧，并用
  `phase_error_intervals` 保存所属次数、开始帧、结束帧、中文阶段选项、中文错误选项、
  可观测性和备注。
- ONI 的 32 条记录拆成 64 个独立模态任务；Depth 与 IR 各自显示 24 个候选检查点，
  分别保存主体、候选框和表面可靠性结论。不得互相复制结论，不做 RGB–Depth 配准，
  不把手机标签迁移到 ONI。
- subject ID 当前可留空且不阻断内部复核；手机和 ONI 的 8 类授权用途均按已确认处理。
- 自动动作识别、`idle/transition/unknown` 连续切换数据采集及自动门控训练整体暂缓，
  改列为 `deferred_tasks`。正式动作入口保持 `manual_only`，网页只允许人工切换动作；
  AI proposal 仅在人工点击“载入”后成为可编辑草稿。

### 9.1 标注工具必须支持的层级

在原有 rep、phase、event、error、severity、quality 基础上增加：

#### 连续时间轴动作标签

```text
idle
setup
target_action
transition
unknown_motion
target_out_of_frame
```

并保存：

```text
action_type
start_frame
end_frame
action_confidence
annotator_id
review_status
```

30 段手机视频全部完成人工确认的视频级动作类别和有效动作时间段。这样可以利用每段开头、
结尾和动作间停顿训练后续动作门控，但不得把背景人物或未复核片段自动当负样本。当前数据
没有连续混合 8 项动作，因此第 9 轮同时输出“连续动作切换/OOD 补拍清单”。

#### 次数、阶段、事件和错误

- rep、阶段、接触、起跳、落地、完全伸展等沿用第九章 schema；
- Lunge、Burpee Broad Jump、Wall Ball 共 15 段完成帧/次数级精标；
- Rowing 和 Farmers Carry 先完成视频级动作区间与错误候选，详细精标留在后续批次；
- SkiErg、Sled Push、Sled Pull 只有合规意图时，只标动作、阶段、周期和可观测性，不把
  未出现错误当成可靠负例；
- 同一次动作可以有多个错误，所有错误必须关联阶段、数值证据、严重度和可观测性；
- 起跳、落地、最低点、接触和完全伸展等关键时间点当前由一名人工复核者完成；保留审计
  记录，以便未来需要时追加第二人质量复核并更新第 8 轮关节时延审计。

#### 器械和场景证据

```text
object_class
object_track_id
object_visible
athlete_object_contact
target_region
line_crossing
display_reading
observability
evidence_source
```

- Wall Ball 标注球和目标区域可见性；看不清球/目标时不能只凭手腕轨迹判定命中；
- Sled Push/Pull 标注雪橇、绳索、运动方向和地面线可见性；真实重量未知时保持不可观测；
- Rowing/SkiErg 标注把手和显示区域可见性；无可靠 OCR/设备数据时不输出距离或功率真值；
- Farmers Carry/Lunge 标注负重物可见性和掉落/接触候选，不从外观猜重量。

### 9.2 弱监督和主动学习

为最大化现有有限数据的作用，标注工具可以显示但必须分层保存：

```text
filename_prior
rule_proposal
dtw_proposal
teacher_pose_proposal
object_detector_proposal
human_annotation
reviewed_ground_truth
```

- 优先复核多后端分歧最大、规则与录制意图冲突、关节跳点、遮挡和错误边界帧；
- 任何 proposal 的接受/修改/拒绝都保存，以便分析规则和教师偏差；
- 同一条 proposal 不得同时影响标注者并被当作独立模型证据计算性能；
- 使用 ONI 时只允许查看该 ONI 自身深度/IR 预览并建立独立标注；同动作错误本体可以共用，
  时间轴和数值证据绝不复制到手机视频。

### 9.3 数据增强规范

只为后续训练登记允许的增强，不在本轮把增强样本混入真值统计：

- 水平镜像必须同步交换左右关节、受影响侧、相机视角和动作语义；
- 时间缩放必须同时调整时间戳、速度和事件容差；
- 裁剪、缩放、亮度、压缩和轻度遮挡保留原始 `record_id` 与变换参数；
- 禁止把会改变动作错误本质的几何扭曲作为普通增强；
- 训练、验证和测试先按主体/原视频划分，再只在训练集合内增强；
- 示例候选、黄金集和同一原视频的增强版本不得进入训练。

### 9.4 评分与纠正标注

- 分项评分 rubric、不可观测项和评分置信度按动作分别定义；
- 当前先由一名具备规则知识的复核者完成核心动作子集评分；第二人一致性与分歧报告列为
  后续质量升级任务，不阻断当前第 9 轮精标。
- 每个错误关联 `correction_id`、目标阶段、身体部位、优先级、抑制条件和预期变化；
- 低置信度、主体丢失、未知视角或器械证据缺失时，标注正确行为应是保守降级；
- 为后续验证“指导是否真正有效”预留 `cue_issued`、`next_rep_error_change`、
  `athlete_response` 字段；当前离线数据没有接受提示后的重复动作时保持空值，不虚构效果。

### 9.5 输出与验收

```text
datasets/hyrox/annotations/action_segments_v1.json
datasets/hyrox/annotations/core_rep_phase_event_error_v1.json
datasets/hyrox/annotations/object_scene_evidence_v1.json
datasets/hyrox/annotations/scoring_correction_v1.json
datasets/hyrox/reports/annotation_agreement_v1.json
datasets/hyrox/reports/proposal_acceptance_bias_v1.json
datasets/hyrox/reports/continuous_ood_gap_v1.json
```

- 30/30 的动作类别和有效动作区间完成人工确认；
- 三个核心动作 15/15 完成主体绑定的次数/阶段/事件/错误精标；
- 关键事件时间锚、核心错误和评分子集有独立复核与一致性报告；
- 文件名与人工真值分别保存；
- 不可观察的器械/场地规则明确为 `UNOBSERVABLE`，不被姿态相似度代替；
- 所有进入后续训练的数据都通过主体、质量、授权和防泄漏门。

## 第 10 轮：自动动作识别、判定评分、3D 与低延迟产品契约

### 2026-07-25 实施状态

第 10 轮工程实现已经完成，但依赖人工真值的训练与正式 A–F 数值验收仍被数据门诚实
阻断。已新增 `configs/contracts/` 下四份严格校验、版本化且只扩展旧输出的契约：动作
门控、判定评分与纠正、坐标空间、实时延迟。桌面状态和网页实时 API 已输出契约版本、
人工/自动来源、统一 Evidence、未标定分数保守降级、2D/单目相对 3D/估计相机射线/
公制深度区分，以及 stale 骨架、动作和提示的同步抑制；现有动作分析器、网页字段和默认
`mediapipe_rgb` 模式保持兼容。

已实现不依赖 scikit-learn 的可审计多项 Logistic Regression 基线，包括 body-canonical
因果短窗特征、标准化参数、类别平衡、温度概率校准、模型 hash、按主体/原视频互斥的
分组交叉验证、unknown/OOD、进入/退出双阈值、最小持续时间、冷却和 stale 保护。离线
回放只在显式使用 `--auto-action-shadow --auto-action-model PATH` 时运行旁路，并逐帧保存
概率和切换候选；旁路绝不切换人工指定的正式分析器。因为连续切换、unknown 拒识和人工
真值门尚未通过，正式 `--hyrox-action auto` 入口继续不创建。

`python -m tools.run_round10_shadow` 已生成
`round10_shadow_ablation_v1.json`、`round10_failure_pool_v1.json` 和
`round10_implementation_summary.json`。30/30 手机记录已走通“主体—姿态—坐标—动作
区间—证据—产品输出”工程闭环；人工真值闭环为 0/30。当前授权确认 0/30、受试者身份
确认 0/30、人工动作区间 0/30、训练可用 0/30，且 idle/transition/unknown 独立记录不足，
所以 A–F 指标保持 `null`，没有训练模型、没有虚构显著提升。当前内部状态为
`engineering_complete_human_training_gate_pending`；完成人工复核和补拍后重跑同一命令，
数据门才允许提取严格因果样本、执行分组交叉验证并生成默认关闭的版本化影子模型。
本轮代码已通过 595 项 Python 回归、16 项前端 Node 回归、无摄像头/无 ONI 冒烟和
`python -m src.doctor --json`；新的 wheel 构建成功并确认包含四份契约与全部第 10 轮模块。

**2026-07-26 复核后重跑更新：**第 10 轮已读取单人复核覆盖层并重新生成 A–F、失败池和
实施摘要。授权确认由 0/30 更新为 30/30；单人确认动作类别为 30/30、可用动作区间为
29/30；独立人类复核者为 1。subject ID 仅在内部 record-id 分组实验中临时豁免，因此不再
列为当前内部实验阻断项。当前单人复核策略不再要求第二名独立复核者。自动动作识别已经
明确暂缓，因此 idle/transition/unknown、连续动作切换数据和动作门控训练列为后续任务，
不再作为当前手动动作工作流的阻断项；自动动作门控继续默认关闭且 CLI 入口不启用。

同时依据人工文字结论和严格因果姿态缓存修复了 Rowing/SkiErg 周期计数：后方/斜后方
不再平均不可见侧；使用较可靠一侧的关节置信度与端点迟滞；SkiErg 后方视角允许手腕完整
行程驱动周期、髋折叠和膝屈仍只作为技术证据；Rowing 使用动作特定可见度，避免远侧遮挡
把完整周期降为 `UNSURE`。复核片回放结果为 Rowing `8/5/5`、SkiErg `13/73`，五段均
`unsure_count=0`。由于人工没有给出逐次边界或精确次数，该结果只作为回归和故障诊断证据，
不宣称为次数真值；详见 `human_review_counting_regression_v1.json`。

### 10.1 自动动作识别与 OOD 契约

补充独立的动作门控输出：

```text
action_probabilities
predicted_action
action_confidence
action_state              # idle/setup/active/transition/unknown
supported_view
equipment_context
switch_candidate_since_ms
action_model_version
```

要求：

- 支持 8 个 HYROX 动作，同时必须支持 `idle`、`transition` 和 `unknown/OOD`；
- 低置信度时不强行选择最相近动作；
- 使用最小持续时间、进入/退出双阈值和冷却时间防止频繁切换；
- 动作切换时重置旧动作状态机、缓存和提示，禁止把上一动作结果错配给下一动作；
- 器械上下文可以提高置信度，但器械不可见时仍允许人体动作证据工作；
- 保留人工选择/锁定动作作为覆盖方式，并显示“自动/人工”来源；
- 当前 30 段主要是单动作视频，只允许形成内部动作可学习性基线；没有连续混合和 OOD
  测试前不得升级为默认自动模式。

### 10.2 判定、评分与纠正契约

实现：

- VALID / NO_REP / UNSURE 优先级；
- 动作特定 `score_breakdown`、权重、置信度、rubric 版本和不可观测项；
- 规则、姿态、3D、器械和时序模型证据使用统一 Evidence，但保留来源；
- 人体硬规则明确失败时，模型不得无证据覆盖；器械规则不可观测时不得推断为通过；
- 错误证据到纠正提示可追溯，每次只输出 1–2 条最高优先级建议；
- 增加提示迟滞、冷却和“错误消失后撤销”，避免每帧反复变化；
- 主体丢失、低置信度、不支持视角、未知动作或关键器械不可见时保守降级；
- 区分“动作技术评价”和“完整 HYROX 比赛规则判罚”。无法观察重量、距离、目标命中或
  场地线时，产品不得宣传为完整裁判；
- API、网页展示、离线报告和历史结果兼容测试。

### 10.3 3D 坐标与质量契约

统一输出：

```text
coordinate_spaces
coordinate_transform_version
body_canonical_landmarks
world_landmarks
camera_ray_landmarks
metric_depth_landmarks
three_d_confidence
three_d_failure_reasons
```

- UI 明确显示当前使用的是单目人体相对 3D、估计相机射线还是可靠公制深度；
- 任何 3D 证据都必须通过可见度、骨长、左右语义、z 稳定性、2D–3D 一致性和主体身份门；
- 3D 失效时自动退回 2D，不清空现有规则结果；
- 规则阈值按坐标空间分别配置，禁止把 MediaPipe World 阈值直接用于 ONI 公制深度；
- 训练特征记录坐标版本，模型加载时校验 schema 和归一化版本。

### 10.4 低延迟契约

实时链路明确分成：

```text
最新摄像头帧
→ 目标 ROI/姿态因果分析坐标
→ 规则与动作门控
→ 低延迟显示坐标
→ 可选异步时序模型
```

输出至少包括：

```text
capture_timestamp
pose_input_timestamp
pose_finished_timestamp
analysis_timestamp
render_timestamp
pose_age_ms
prediction_horizon_ms
source_frame_id
stale
```

要求：

- 分析使用因果姿态，显示预测不得反向写入规则、评分或训练真值；
- 队列容量固定，只保留最新帧/窗口，忙时丢旧不积压；
- 超过 `max_pose_age_ms` 的骨架、动作结果和纠正提示不得继续显示或进入当前动作；
- 分别为人体核心、快速末端和支撑脚配置显示预测上限；
- 模型/跟踪/显示异常时回退现有 MediaPipe + 规则链路；
- 发布报告同时给出延迟、抖动、过冲和漏检，不能只报告平均 FPS。

### 10.5 小样本影子基线与消融

在不改变默认产品的前提下，使用第 9 轮已确认标签建立可复现的影子实验：

```text
A. 当前全帧 MediaPipe + 当前规则
B. 目标锁定 ROI + MediaPipe
C. B + 统一 body canonical 2D/3D 与质量门
D. C + 多后端人工复核后的姿态/事件标签
E. D + 自动动作门控的简单传统分类基线
F. E + 低延迟显示预测（仅评测显示，不改变判定）
```

其中 E 至少实现一个基于 body canonical 关节角、角速度、身体中心轨迹、器械可见性和短时
窗口统计的 Logistic Regression/Random Forest 动作门控基线，并：

- 只使用人工确认区间，以分组交叉验证训练 8 动作 + idle/transition/unknown；任一类没有
  足够独立记录时应触发数据就绪失败并输出补拍任务，不用其他类或合成片段伪装该类；
- 保存特征 schema、标准化参数、模型 hash、训练记录和概率校准；
- 提供默认关闭的 `auto_action_shadow` 离线回放或实时旁路，不直接切换正式分析器；
- 只有连续动作切换、unknown 拒识和延迟门通过后，才允许新增默认关闭的
  `--hyrox-action auto` 实验入口；
- 模型置信度不足时输出 `unknown`，不得退化为 8 类强制最大值。

约束：

- 主体身份未确认时至少按原视频分组交叉验证，并明确这不等价于未见受试者测试；
- 每项实验使用完全相同的冻结切片与标签版本；
- 报告动作 Macro-F1、unknown 拒识、切换延迟、核心事件误差、规则判定、3D 稳定性、
  每关节延迟/抖动和 P50/P95 推理时间；
- 给出逐记录结果、置信区间和失败样本，不只给总体平均；
- 如果现有数据无法证明显著增益，输出“未证实”和下一批最有价值的补拍组合，不调测试集
  或放宽门限制造通过；
- 本轮传统分类器只验证数据和契约可学习，正式传统模型和 TCN 仍分别在第 15、16 轮训练。

### 10.6 输出与验收

```text
configs/contracts/action_gating_v1.yaml
configs/contracts/scoring_correction_v1.yaml
configs/contracts/coordinate_spaces_v1.yaml
configs/contracts/realtime_latency_v1.yaml
datasets/hyrox/reports/round10_shadow_ablation_v1.json
datasets/hyrox/reports/round10_failure_pool_v1.json
```

- 新字段只扩展现有输出，神经网络和自动动作门控默认关闭；
- 旧 8 项兼容性回归、现有网页和无模型/无 ONI 环境全部通过；
- 30 段手机数据能够走完“主体—姿态—坐标—动作区间—证据—产品输出”离线闭环；
- 自动动作门控包含 unknown/OOD 和切换保护，而不是 8 类强制取最大值；
- 2D、单目相对 3D、估计相机坐标和 ONI 公制深度不会混用；
- 关节节点改善使用关节级时间锚和延迟—抖动指标证明，显示预测不会污染分析；
- 评分与提示能追溯到规则、阶段、身体部位、器械可观测性和 rubric 版本；
- 第 10 轮只报告当前数据可支持的内部证据，不把小样本试点宣传为生产级显著提升。

## 第 11 轮：ONI Depth + IR 主体审计与辅助研究

**实施状态（2026-07-26）：工程实现和首轮自动候选视觉审计已完成；当前状态为
`engineering_complete_human_review_and_calibrated_rgbd_pending`。32/32 条 ONI 的
Depth/IR 各自抽取 24 个检查点并独立生成主体候选，共 1,536 个检查点；Depth 380/768、
IR 414/768 通过自动候选门，低置信和缺失检查点不插值。已生成 64 张独立模态复核图、
可观测性矩阵、手机补拍计划和未来同步 RGB-D 价值报告。独立人工主体确认、动作错误
真值、地面/接触标定仍待完成，当前对应计数保持 0；未生成 RGB–Depth 配准、手机—ONI
配对、手机逐帧标签或未配对蒸馏。本轮代码已通过 602 项 Python 回归、16 项前端
Node 回归、`pose-doctor` 契约检查和包含新命令/契约的 wheel 打包检查。**

**2026-07-26 授权与复核边界更新：**32/32 条 ONI 已逐项确认上述 8 类授权用途，重新
运行第 11 轮后 `authorization_confirmed_record_count=32`。本次人工复核包只包含手机 RGB，
没有 Depth/IR 检查点或主体框人工结论，因此不能迁移为 ONI 主体确认；当前
`human_confirmed_subject_count` 仍为 0，地面/接触标定、错误真值和同步 RGB-D 门保持不变。

**2026-07-26 人工入口更新：**人工复核台现已提供 32×2 个 ONI 模态任务，直接读取
`round11_subject_previews/{record_id}/{depth|ir}_subject_proposals.jpg` 和对应 24 个
检查点。结果独立保存到
`datasets/hyrox/reviews/human_v1/reviewer_a/oni_records/{record_id}__{modality}.json`。
第 11 轮重跑时会读取这些单人人工结果，分别统计已复核模态、确认候选框和两模态主体门；
在人工实际完成前所有计数仍保持真实值，不因网站上线而预先置为完成。

实现：

- 在 IR/深度预览中独立锁定 ONI 的目标运动者；
- 研究地面、距离、接触、轮廓和空间运动证据；
- 明确哪些错误可以从当前模态可靠观测；
- 不生成 RGB–Depth 配准、手机—ONI 配对或手机逐帧标签；
- 形成后续手机补拍建议和未来同步 RGB-D 采集价值报告。

## 第 12 轮：补采手机 RGB 与建立版本化黄金集

实现：

- 优先为 SkiErg、Sled Pull 和 Sled Push 补齐错误与边界样本；
- 为已有具体错误补充重复录制和临界严重度，不让每类错误只有一个样本；
- 增加受试者、视角、设备、距离、光照和背景人员变化；
- 按受试者互斥划分 train/validation/test；
- 近重复、切片和增强防泄漏；
- 建立 `hyrox_golden_v2_phone_rgb`。

## 第 13 轮：规则阈值和分项评分标定

实现：

- 特征分布与置信区间；
- PASS/FAIL/UNSURE、持续时间和视角容差；
- 动作特定评分 rubric 与教练一致性；
- 困难负样本；
- 未见受试者验证。

## 第 14 轮：多模板与分阶段 DTW

实现：

- 仅聚类人工确认的多个合规动作风格；
- 多参考模板；
- 分阶段 DTW；
- 关键关节权重；
- 差异阶段定位并映射到可执行提示；
- 不使用网页示例作为唯一模板或测试样本。

## 第 15 轮：传统机器学习可学习性基线

实现：

- 数据加载与特征基线；
- 按受试者划分；
- 主体轨迹防污染和切分防泄漏；
- 阶段、事件、错误和有效性分类报告；
- 特征重要性与置信度校准。

## 第 16 轮：RGB-only 轻量时序模型

仅在训练前数据就绪门通过后实现：

- 阶段识别；
- 边界检测；
- 事件检测；
- 多标签错误；
- 有效性与分项评分辅助头；
- ONNX 导出。

## 第 17 轮：异步旁路接入

实现：

- latest-window；
- 队列容量限制；
- 过期结果丢弃；
- 模型异常降级；
- Evidence、评分和提示融合；
- 延迟回归。

## 第 18 轮：评测、示例替换与发布门

实现：

- 未见受试者/视角/设备评测；
- 判定、计数、错误、评分和纠正提示评测；
- 按第 0.4 节为 8 个动作生成规范化示例发布文件和版本化示例 manifest；
- 修改网页示例发现逻辑，不再依赖 `HYROX视频/` 和旧文件 stem 硬编码；
- 原子更新网页示例、姿态缓存、manifest、SHA256 和文档；
- 确认 `HYROX视频/` 缺失时程序和测试仍可正常运行。

------

# 现在最先执行的任务

第 1–6 轮已经完成。接下来严格按下面顺序：

1. ~~**执行第 6 轮：接入 30 个真实手机 RGB、排除 30 个 AppleDouble 文件，迁移
   `phone_rgb` manifest，冻结数据角色、覆盖矩阵、坐标质量和实时延迟基线。**~~
   **已完成：30/30 视频、15,515/15,515 帧、30 个 AppleDouble 排除、0 个 ONI 配对，
   第 6 轮验收报告为 `passed`。**
2. ~~**执行第 7 轮：逐片人工锁定真正运动者，审计背景人物、遮挡和身份切换；同时建立
   球、雪橇、绳索、把手、目标板、地面线等器械/场景候选与低延迟 ROI 对照。**~~
   **工程实现与首轮逐片视觉审计已完成；ROI 双门未通过并保持关闭。独立第二名人类
   逐帧复核仍是明确待办，完成前身份切换漏检率和复核分歧保持 `null`。**
3. ~~**执行第 8 轮：生成绑定目标轨迹的 Lite/Full 多后端原始缓存、严格因果分析姿态和
   低延迟显示姿态；完成 2D、单目相对 3D、body canonical 和相机射线坐标契约，以及
   每关节延迟—抖动审计。**~~
   **工程实现、30/30 全量缓存、三套时序流、坐标/共识/可观测性报告和完整性审计均已
   完成；当前状态为 `passed_with_round9_double_reviewed_event_anchors_pending`。
   sensor-to-photon、GPU 和第 9 轮双人事件锚复核保持明确待办，ROI 继续关闭。**
4. **第 9 轮工程层和 AI 辅助试点复核已收尾：30/30 时间轴、15/15 核心动作共 70 个
   rep 候选、器械/评分/纠正 schema、主动复核队列与全部要求产物均已生成；第二遍多方法
   复核修正 18 段动作边界和 5 段核心锚。当前人工台已支持 15 段逐次、阶段—错误区间和
   事件精标；本阶段单人完成即可，第二人一致性不再阻断。实际精标完成前不得标记为
   `reviewed_ground_truth`。**
5. **第 10 轮工程实现已完成：四份版本化契约、统一 Evidence、默认关闭的 Logistic
   Regression 动作门控旁路、unknown/OOD、切换保护、stale 抑制、分组交叉验证管线及
   A–F/失败池报告均已落地；正式动作选择保持人工手动模式。自动识别及其所需的
   idle/transition/unknown、连续切换数据现已暂缓并列入后续任务，不作为当前工作流阻断项；
   自动模型不训练，正式 `--hyrox-action auto` 入口继续不创建。**
6. **第 11 轮工程实现已完成：32/32 条 ONI 的 Depth/IR 独立主体候选、表面距离/轮廓/
   空间运动可观测性、具体错误限制、手机补拍计划和未来同步 RGB-D 价值报告均已生成；
   Color–Depth 配准、手机—ONI 配对、手机逐帧标签和未配对蒸馏保持禁止。人工台已加入
   64 个 Depth/IR 独立主体任务；本阶段单人完成即可，实际主体复核、地面/接触标定和
   动作错误真值仍是研究结论升级门。**
7. **根据第 6–10 轮失败池和价值排序补拍：优先连续混合动作/OOD、未见受试者、快速
   端点、遮挡、不同设备视角，以及 SkiErg、Sled Pull、Sled Push 的错误/边界 RGB。**
8. **建立按受试者划分的新黄金集，用人工确认数据标定规则、评分与多模板 DTW，再训练
   正式传统模型和 RGB-only TCN；审核并原子替换网页示例，保证删除 `HYROX视频/` 后
   产品仍能正常运行。**

最终形成三个明确隔离的模式：

```text
mediapipe_rgb
# 当前产品默认模式，普通电脑或手机摄像头

offline_oni
# ONI Depth + IR 主体审计和辅助研究模式

mediapipe_rgb_neural
# 后期加入轻量 TCN 的普通摄像头增强模式
```

目前不创建：

```text
orbbec_rgbd_realtime
```

当前手机视频作为独立的 `phone_rgb` 数据源接入 RGB-only 主数据管线，不与当前 ONI
做帧级配对。这样既直接面向最终普通摄像头产品，又能把 ONI 深度数据保留为受质量门约束
的辅助研究证据，不会污染主体身份或让产品绑定奥比摄像头。
