上传视频性能修复与角度正确性验证实施方案
一、实施目标与边界

本轮只解决两个问题：

上传视频分析时，视频被拖成慢放、处理耗时过长。
确认页面显示的关节角度是否计算正确、显示正确。

本轮暂时不做：

不切换到新的姿态模型；
不新增神经网络；
不修改 HYROX 动作阈值；
不修改 VALID / NO_REP / UNSURE 规则语义；
不正式切换到 3D 主导；
不接入奥比摄像头；
不删除现有网页、桌面、报告和回归功能。

当前正式分析使用 MediaPipe Pose Full；分析平滑流与网页显示平滑流彼此独立，显示链路可以使用更激进的平滑和短时预测，但显示结果不得回流到正式规则。
因此，本轮必须同时区分：

视频播放速度
视频处理速度
骨架显示延迟
正式分析延迟
角度计算错误
角度显示不同步
第一部分：解决上传视频慢放和分析耗时过长
1. 首先明确问题本质

上传视频后出现“视频慢放”，通常有两种不同原因。

原因 A：播放速度被推理速度绑定

当前程序可能采用类似流程：

读取一帧
→ MediaPipe 推理
→ 规则分析
→ 绘制骨架
→ 发送到网页
→ 网页显示这一帧
→ 再处理下一帧

假设原视频是 30 FPS，但每帧处理需要 80 ms：

实际处理速度约 12.5 FPS

如果网页把“处理完成的帧”直接当成视频播放，原本 10 秒的视频就会显示成约 24 秒，视觉上就是慢放。

这不是原视频发生了变化，而是：

播放时钟 = 推理完成时钟
原因 B：生成标注视频时写入 FPS 错误

例如原视频是 30 FPS，但输出视频使用：

processed_fps = 12

作为 VideoWriter 的 FPS，最终生成的视频也会变成慢放。

正确逻辑应该是：

输出视频 FPS = 原视频 FPS

而不是：

输出视频 FPS = 实际推理速度
2. 第一阶段：加入完整性能分析工具

在优化前，先对上传视频的完整链路进行计时，避免只凭感觉判断“MediaPipe 太慢”。

2.1 每帧记录以下耗时
decode_ms             视频解码
resize_ms             推理前缩放
color_convert_ms      BGR/RGB 转换
pose_inference_ms     MediaPipe 推理
smoothing_ms          One Euro 平滑
feature_ms            角度和相对位置计算
rule_ms               状态机和动作规则
draw_ms               骨架、文字和角度绘制
encode_ms             标注视频编码
serialize_ms          JSON/CSV 序列化
web_transfer_ms       向网页传输结果
total_frame_ms        当前帧总耗时

同时记录：

source_fps
source_frame_count
source_duration_ms
processed_frame_count
pose_inference_count
output_fps
output_frame_count
total_analysis_time_ms
processed_fps
real_time_factor

其中：

processed_fps = processed_frame_count / total_analysis_time
real_time_factor = total_analysis_time / source_video_duration

例如：

视频时长：20 秒
分析耗时：30 秒
real_time_factor = 1.5
2.2 输出文件

建议新增：

outputs/performance/upload_video_profile.csv
outputs/performance/upload_video_summary.json

摘要至少输出：

{
  "source_fps": 30.0,
  "source_frame_count": 600,
  "source_duration_ms": 20000,
  "output_fps": 30.0,
  "total_analysis_time_ms": 28500,
  "processed_fps": 21.05,
  "real_time_factor": 1.425,
  "p50_pose_inference_ms": 31.2,
  "p95_pose_inference_ms": 42.8,
  "p50_encode_ms": 8.4,
  "pose_inference_count": 600
}
2.3 验收要求
每次上传视频分析都能生成性能摘要。
能明确判断主要瓶颈是推理、绘制、编码还是传输。
pose_inference_count 必须可检查。
优化前后必须使用相同视频进行比较。
3. 第二阶段：彻底解除视频播放与推理速度的绑定

这是解决“慢放”的核心修改。

3.1 推荐的短期实现：先分析，后正常播放

上传视频后的默认流程改成：

上传视频
→ 显示分析进度
→ 后端按最快速度完成推理
→ 生成关键点时间轴和分析结果
→ 分析完成后正常速度播放视频
→ 按视频时间戳叠加骨架、角度和反馈

分析过程中不要把后端处理完成的一帧一帧当成视频播放。

页面在分析期间只需要显示：

正在分析：43%
已处理：516 / 1200 帧
处理速度：21.4 FPS
当前主要耗时：姿态推理

这种方案最稳定，也最容易保证：

视频不会慢放；
骨架和角度时间一致；
报告和视频结果一致；
不会因为推理速度波动而导致播放卡顿。
3.2 前端播放原始视频

浏览器应使用上传文件创建本地视频地址：

URL.createObjectURL(uploadedFile)

视频元素按照原始媒体时间播放。

分析结果单独形成时间轴：

[
  {
    "frame_index": 0,
    "timestamp_ms": 0,
    "landmarks": [],
    "angles": {},
    "feedback": []
  },
  {
    "frame_index": 1,
    "timestamp_ms": 33.33,
    "landmarks": [],
    "angles": {},
    "feedback": []
  }
]

网页绘制骨架时，根据：

video.currentTime × 1000

寻找最接近的分析结果，而不是等待后端发送下一帧。

正确关系应当是：

视频负责播放
时间轴负责叠加分析结果

而不是：

后端推理完成一帧
网页才播放一帧
3.3 时间轴匹配规则

建议前端维护：

current_video_timestamp_ms
nearest_result_timestamp_ms
timestamp_difference_ms

结果选择：

选择 timestamp_ms <= 当前视频时间
且最接近当前视频时间的结果

如果相差过大，例如超过 150 ms：

暂时隐藏骨架；
或保持上一帧骨架不超过 100 ms；
不要暂停视频等待推理结果。
4. 第三阶段：修复输出标注视频慢放问题

若程序会生成带骨架的标注视频，必须检查以下逻辑。

4.1 输出 FPS 必须取原视频 FPS

正确：

source_fps = capture.get(cv2.CAP_PROP_FPS)
writer = cv2.VideoWriter(
    output_path,
    fourcc,
    source_fps,
    output_size,
)

错误：

writer_fps = processed_frame_count / processing_seconds

也不能使用实时窗口当前测得的 FPS。

4.2 输出时长一致性检查

要求：

输入时长 = 输入帧数 / 输入 FPS
输出时长 = 输出帧数 / 输出 FPS

自动检查：

abs(output_duration - input_duration) <= max(1 / source_fps, 0.05 秒)

例如原视频为 30 FPS，允许误差最多约一帧或 50 ms。

4.3 禁止使用推理时间控制离线视频播放

上传视频分析路径中，不应使用：

cv2.waitKey(processing_time_ms)
time.sleep(...)

控制输出视频节奏。

离线处理应该尽可能快地运行。视频播放速度由媒体 FPS 和时间戳决定，不由处理循环耗时决定。

5. 第四阶段：确保每帧只运行一次 MediaPipe

当前显示流和正式分析流可以独立平滑，但不应各自重新运行一次姿态模型。

正确架构：

一帧 RGB
  → 一次 MediaPipe Pose 推理
  → image landmarks + world landmarks
       ├→ 正式分析平滑流
       ├→ 网页显示平滑流
       ├→ 动作规则
       └→ 报告和缓存

错误架构：

正式分析运行一次 MediaPipe
显示骨架又运行一次 MediaPipe
生成报告再次运行 MediaPipe
3D 分析再次运行 MediaPipe

增加运行时断言：

pose_inference_count <= decoded_frame_count

在逐帧正式分析模式下应满足：

pose_inference_count == analyzed_frame_count

还要检查：

MediaPipe 模型是否在每帧内部重新创建；
视频分析函数是否每次调用都重新加载模型；
网页预览和正式分析是否同时处理相同视频；
报告生成阶段是否重新读取视频并再次推理。

模型实例应在整段视频开始前创建一次，结束后释放一次。

6. 第五阶段：增加关键点缓存

上传视频第一次分析完成后，保存姿态结果：

outputs/cache/<video_hash>/pose_landmarks.npz
outputs/cache/<video_hash>/pose_metadata.json

缓存内容：

frame_index
timestamp_ms
image_landmarks
world_landmarks
visibility
presence
segmentation_available
source_width
source_height
source_fps

缓存键至少包含：

视频 SHA256
MediaPipe 模型类型
模型文件哈希
推理分辨率
是否启用 segmentation
姿态配置哈希

以后只修改：

动作阈值；
状态机；
角度显示；
反馈文字；
计数规则；

时，直接从缓存运行：

landmarks
→ 平滑
→ 角度
→ 状态机
→ 规则

不重新运行 MediaPipe。

这能显著提高你后续调试角度和规则的效率。

7. 第六阶段：分离“分析”和“完整视频渲染”

上传视频提供三个模式。

模式一：快速分析，默认开启
姿态推理
规则分析
生成报告
不生成完整标注视频

输出：

文字报告；
JSON；
CSV；
关键事件截图。
模式二：分析并保存关键事件

只保存：

动作开始；
最低点；
触地；
起跳；
落地；
完全伸展；
NO_REP 证据帧。
模式三：完整标注视频

仅在用户明确选择时生成。

建议采用两遍式：

第一遍：
视频 → MediaPipe → landmarks 缓存 → 动作结果

第二遍：
原视频 + landmarks 缓存 → 绘制 → 编码标注视频

第二遍不能再次运行 MediaPipe。

8. 第七阶段：在不明显降低准确率的前提下减少推理耗时

这部分必须在前面的问题修好后再做。

8.1 降低姿态推理输入分辨率

原视频保留原始分辨率，但推理使用缩放图像。

测试：

长边 640
长边 768
长边 960
原始分辨率

关键点坐标最终映射回原始视频坐标。

每种分辨率比较：

平均推理时间
P95 推理时间
人体检出率
全身关键点可见率
角度差异
候选计数差异
VALID/NO_REP/UNSURE 差异

不能仅根据速度直接选择最低分辨率。

8.2 默认不全程启用分割

当前 segmentation 主要对部分接触代理有帮助，不是所有动作、所有阶段都需要。

建议：

Lunge、Wall Ball 普通角度分析默认不启用全程分割；
Burpee 接近胸部触地阶段才需要分割证据；
完整分割保留为精细分析选项；
修改前后必须进行规则结果对比。
8.3 先不要直接跳帧

第一轮性能修复先保持逐帧推理，以确定：

慢放是否已经消失；
重复推理是否已消除；
编码是否仍是瓶颈。

完成基线后，再考虑：

普通阶段低频分析
关键事件附近逐帧分析

否则一开始同时改播放、跳帧、分割和模型分辨率，很难知道哪个修改导致结果变化。

第二部分：解决角度是否显示正确的问题
1. 先明确需要验证的三种“正确”

角度正确性分三层。

第一层：数学计算正确

输入三个确定坐标，程序是否计算出正确夹角。

第二层：显示一致

页面画出来的髋、膝、踝三个点，是否就是用于计算屏幕数字角度的三个点。

第三层：人体估计准确

MediaPipe 识别的髋、膝、踝位置，是否接近人体真实关节位置。

必须依次检查，不能直接拿屏幕上的数字判断 MediaPipe 好坏。

2. 第一阶段：追踪每个显示角度的完整来源

从前端显示文字开始反向追踪：

网页角度标签
→ WebSocket/API 返回字段
→ 动作分析结果字段
→ biomechanics/features 角度字段
→ 使用的关键点坐标
→ 原始还是平滑坐标
→ image landmarks 还是 world landmarks

为每个角度建立统一数据结构：

AngleObservation:
    frame_index
    timestamp_ms
    joint_name
    side
    angle_2d_raw_deg
    angle_2d_smoothed_deg
    angle_3d_raw_deg
    angle_3d_smoothed_deg
    display_angle_deg
    rule_angle_deg
    display_angle_source
    rule_angle_source
    landmark_visibility
    geometry_valid

示例：

{
  "frame_index": 125,
  "timestamp_ms": 4166.67,
  "joint_name": "knee",
  "side": "left",
  "angle_2d_raw_deg": 161.4,
  "angle_2d_smoothed_deg": 158.9,
  "angle_3d_raw_deg": 165.2,
  "angle_3d_smoothed_deg": 163.7,
  "display_angle_deg": 158.9,
  "rule_angle_deg": 158.9,
  "display_angle_source": "image_landmarks_smoothed",
  "rule_angle_source": "image_landmarks_analysis_smoothed",
  "landmark_visibility": 0.91,
  "geometry_valid": true
}

当前系统正式动作判定仍然主要使用二维证据，world landmarks 只是辅助证据。

所以本轮角度验证优先验证：

正式规则实际采用的 2D 分析角度

3D 角度先显示和记录，不正式替换。

3. 第二阶段：统一角度数学函数

所有动作不能各自实现一套角度算法。

建立统一函数：

calculate_angle_2d(a, b, c, frame_width, frame_height)
calculate_angle_3d(a, b, c)

其中 b 是关节顶点。

例如膝角：

A = 髋
B = 膝
C = 踝
3.1 二维角度必须处理宽高比

若 MediaPipe 返回归一化坐标：

x ∈ [0,1]
y ∈ [0,1]

不能直接将归一化 x、y 当成同尺度坐标计算角度，因为视频通常不是正方形。

应先转换：

x_px = x_normalized * frame_width
y_px = y_normalized * frame_height

然后再计算。

否则在 1920×1080 和 1080×1920 视频中，同样的姿态可能产生不同角度。

3.2 数学安全处理

角度函数必须：

检查三个点是否有效；
检查向量长度是否接近零；
检查 NaN 和无穷值；
对余弦值执行 clip(-1, 1)；
明确返回角度单位为 degree；
无效时返回 None 或明确的无效状态；
不得把无效角度自动变成 0°。
4. 第三阶段：建立角度几何单元测试

新增测试：

tests/test_angle_geometry.py
tests/test_angle_aspect_ratio.py
tests/test_angle_mirror.py
tests/test_angle_invalid_points.py

至少覆盖：

测试情况	预期结果
完全伸直三点	180°
直角	90°
60°夹角	60°
45°夹角	45°
平移所有点	角度不变
等比例缩放	角度不变
左右镜像	角度数值不变
非等比例宽高缩放	经像素修正后角度正确
两个点重合	返回无效
坐标含 NaN	返回无效
visibility 过低	角度可算但标记低可信，或直接无效

数学层验收：

固定合成坐标误差 <= 0.1°
5. 第四阶段：保证屏幕数字与屏幕骨架使用相同坐标

这是最容易被忽略的问题。

当前网页显示骨架和正式分析使用独立平滑流。

因此可能出现：

骨架使用显示平滑坐标
数字使用正式分析平滑坐标

两套坐标存在几度差异时，用户会觉得数字不正确。

推荐处理方式

调试模式同时显示两个角度：

左膝：
显示角度 162°
规则角度 158°

其中：

显示角度：严格从屏幕正在绘制的三个点重新计算；
规则角度：正式状态机和规则实际使用的角度。

正式产品界面可以只显示规则角度，但必须允许打开调试信息。

自动一致性检查

绘制完成前重新计算：

drawn_landmarks_angle

然后检查：

abs(display_angle_deg - drawn_landmarks_angle_deg) <= 0.5°

若超过门槛，记录：

ANGLE_DISPLAY_MISMATCH
6. 第五阶段：增加角度调试可视化

开启 --hyrox-debug 或网页调试开关后，对选中的关节显示：

三个实际使用的关键点；
两条关节向量；
关节顶点；
角度圆弧；
左侧或右侧；
原始坐标点；
平滑坐标点；
visibility；
角度来源；
规则采用角度。

例如：

LEFT KNEE
2D RAW: 163.8°
2D SMOOTHED: 159.6°
3D RAW: 166.1°
RULE USED: 159.6°
SOURCE: 2D ANALYSIS
VISIBILITY: 0.92

如果屏幕显示的是左膝，计算却使用了右膝，这个界面会直接暴露问题。

7. 第六阶段：建立人工角度对照工具

新增工具：

tools/inspect_joint_angles.py
tools/manual_angle_annotation.py
tools/compare_manual_angles.py
7.1 操作流程
打开视频。
暂停到指定帧。
人工依次点击髋、膝、踝。
使用与程序相同的数学函数计算人工 2D 投影角度。
同时显示程序的原始角度和平滑角度。
保存对照结果。

保存格式：

{
  "video_id": "phone_lunge_001",
  "frame_index": 326,
  "timestamp_ms": 10866.67,
  "joint": "left_knee",
  "camera_view": "side",
  "manual_points": {
    "hip": [812, 342],
    "knee": [834, 581],
    "ankle": [902, 811]
  },
  "manual_angle_deg": 164.7,
  "model_2d_raw_deg": 161.9,
  "model_2d_smoothed_deg": 159.8,
  "model_3d_raw_deg": 167.1
}
7.2 建议抽取的验证帧

优先从 Lunge、Wall Ball、Burpee 中抽取：

站立完全伸展；
开始下降；
最低点；
起身中段；
尚未完全伸展；
完全伸展；
快速运动；
遮挡；
visibility 低；
正面；
侧面；
斜侧面。

第一轮建议：

30～50 个代表性帧

重点验证：

左右膝；
左右髋；
左右肘；
躯干角。
8. 第七阶段：区分角度误差和角度延迟

角度看起来不正确，可能不是数值误差，而是平滑后出现时间延迟。

例如：

原始角度在第 100 帧达到最低值
平滑角度在第 103 帧达到最低值

30 FPS 下约为 100 ms 延迟。

因此需要同时导出：

原始角度曲线
显示平滑角度曲线
正式规则角度曲线
人工事件帧
程序事件帧

统计：

角度 MAE
角度中位绝对误差
角度 P90/P95 误差
最低点时间偏移
完全伸展时间偏移
原始与平滑曲线延迟

建议内部目标：

数学函数误差：<= 0.1°
绘制与显示数字误差：<= 0.5°
侧面高可见帧人工对照中位误差：初期目标 <= 5°
侧面高可见帧 P90：初期目标 <= 10°
平滑角度事件延迟：初期目标 <= 2 帧

这些是工程验收目标，不代表当前已经达到。

正面视角的 2D 膝角只能验证“投影角度是否正确”，不能把它解释成真实三维膝关节角。

三、推荐实施顺序

严格按以下顺序执行，不要同时大范围修改。

第 1 步：加入性能日志

先不改变任何算法，只记录：

解码、推理、规则、绘制、编码、传输耗时
第 2 步：修复视频慢放

完成：

原视频独立正常播放；
分析过程显示进度，不逐帧慢速播放；
输出视频使用原 FPS；
输入输出视频时长一致。
第 3 步：检查重复推理

完成：

每分析帧最多一次 MediaPipe；
模型只初始化一次；
报告和渲染复用姿态缓存。
第 4 步：分离分析与渲染

默认只分析和生成报告，完整标注视频改为可选第二遍渲染。

第 5 步：加入关键点缓存

修改规则和角度显示时不重新运行 MediaPipe。

第 6 步：追踪角度来源

明确：

数字显示角度来自哪套坐标
规则使用角度来自哪套坐标
屏幕骨架来自哪套坐标
第 7 步：完成角度数学测试

重点检查：

宽高比；
镜像；
左右侧；
点顺序；
角度单位；
无效坐标。
第 8 步：完成人工帧对照

建立真实视频上的角度误差和时间延迟报告。

第 9 步：性能优化实验

在前面结果稳定后，再测试：

推理分辨率；
segmentation 开关；
自适应分析帧率；
Lite 粗扫描与 Full 精细分析。
四、建议增加的配置项
upload_video:
  playback_mode: analyze_then_play
  generate_annotated_video: false
  save_pose_cache: true
  reuse_pose_cache: true
  profile_performance: true
  inference_long_side: 960
  preserve_source_fps: true
  segmentation_mode: action_required

angle_debug:
  enabled: false
  show_display_angle: true
  show_rule_angle: true
  show_2d_raw: true
  show_2d_smoothed: true
  show_3d_raw: true
  show_landmark_visibility: true
  export_frame_angles: false

暂时不要直接把 inference_long_side 固定为低值。先完成 960、768、640 的对比实验，再确定默认值。

五、自动化测试清单

新增或完善：

tests/test_upload_video_preserves_fps.py
tests/test_upload_video_preserves_duration.py
tests/test_single_pose_inference_per_frame.py
tests/test_pose_cache_reuse.py
tests/test_upload_performance_profile.py

tests/test_angle_geometry.py
tests/test_angle_aspect_ratio.py
tests/test_angle_mirror.py
tests/test_angle_invalid_points.py
tests/test_angle_payload_source.py
tests/test_display_angle_matches_drawn_landmarks.py
tests/test_rule_angle_not_using_display_prediction.py

重点断言：

输出 FPS == 输入 FPS
输入输出时长误差 <= 1 帧或 50 ms
推理次数不超过分析帧数
第二次规则分析可复用缓存
显示角度与绘制坐标重新计算结果一致
规则角度不使用显示预测点
六、本轮最终验收标准
上传视频部分

必须达到：

上传视频不再随着推理速度慢放；
播放时长与原视频基本一致；
标注视频 FPS 与原视频一致；
分析进度和视频播放相互独立；
每帧最多进行一次 MediaPipe 推理；
能查看完整性能分阶段报告；
修改规则后可以复用 landmarks 缓存；
完整标注视频生成可以关闭。

性能提升目标不要先写死绝对 FPS，因为取决于电脑和视频分辨率。应使用当前固定测试视频建立基线，然后要求：

同一设备、同一视频、同一模型下：
总分析耗时至少下降 30%
且动作候选和规则结果没有不可接受的退化
角度部分

必须达到：

每个显示角度都能查到来源；
屏幕骨架点与显示角度能够对应；
规则角度和显示角度明确区分；
2D 角度完成宽高比修正；
合成角度测试通过；
镜像不会改变角度数值或错误交换左右；
无效关键点不会显示成 0°；
可以导出原始、平滑、2D、3D和规则实际采用角度；
可以通过人工点击工具验证真实视频帧；
可以测出平滑带来的角度时间延迟。