# 遮挡与跨视角动作识别优化方案

## 1. 文档目的

本文针对当前 HYROX 姿态分析程序的两个主要问题制定优化方案：

1. 人体部分被遮挡时，关键点和骨架出现不同程度的抖动、跳变或左右侧误切换；
2. 动作阶段主要依赖二维关节角度和相对位置，不同拍摄角度可能导致阶段漏识别、误识别，最终影响动作周期计数。

方案同时说明人工标注的必要范围，以及本地视频分析中 MediaPipe、WHAM 和 OpenCap 的职责边界。目标是在不破坏现有正式规则语义的前提下，逐步提高显示稳定性、阶段识别准确率和结果可解释性。

---

## 2. 当前程序现状

### 2.1 姿态与骨架稳定

当前程序已经包含以下稳定机制：

- 正式分析流使用 One Euro 关键点平滑；
- 姿态整体短时丢失时，最多保持若干帧历史姿态；
- 对手部靠近身体关节并伴随明显跳变的情况进行有限遮挡保护；
- 显示层使用独立的置信度滞回、死区、短时保持和预测；
- 上传视频播放层允许对低置信度点进行短缺口插值、边缘保持和前后帧平滑。

其中，上传视频播放层的稳定结果仅用于绘制骨架，不修改正式关键点、动作阶段、规则结论和计数。这一隔离原则应继续保留。

### 2.2 动作阶段与计数

当前程序采用动作专属状态机：

- 先根据二维角度、相对位置、运动趋势和接触事件判断原始阶段；
- 阶段连续满足一定帧数后切换为稳定阶段；
- 对短时 `unknown`、`no_pose` 和 `low_visibility` 保留上一稳定阶段；
- 允许跳过部分过渡阶段，但最低点、触地、完全伸展、起跳、落地等关键端点不能跳过；
- 只有观察到完整的关键端点顺序，才形成动作候选或分析周期；
- 证据不足时应输出 `UNSURE`，而不是强制计入有效或无效动作。

当前风险在于：二维关节角度会随相机方位变化，同一个真实动作在正面、侧面和斜侧面可能得到明显不同的角度和相对位置，因此固定阈值难以跨视角泛化。

### 2.3 本地视频与三维分析

本地视频默认使用“快速分析”：

- MediaPipe Pose；
- 现有 HYROX 特征、阶段状态机和规则；
- 上传视频显示层稳定器。

只有选择“高级 3D 分析”并正确配置外部命令后，程序才会尝试运行：

1. MediaPipe 正式分析；
2. 外部 WHAM 三维重建；
3. MediaPipe 与 WHAM 的时间戳对齐；
4. 基于 WHAM 初始结果的 OpenCap Monocular / OpenSim IK 优化。

当前项目中的 WHAM 和 OpenCap 属于外部适配接口，不是内置模型。未配置 `POSE_WHAM_COMMAND_JSON` 或 `POSE_OPENCAP_COMMAND_JSON` 时，对应结果为 `UNAVAILABLE`。即使配置成功，WHAM/OpenCap 也只提供三维与运动学参考，不替换正式二维规则和计数。

---

## 3. 总体优化原则

### 3.1 显示稳定与正式证据严格分离

系统继续维护两条数据流：

```text
原始关键点
  ├─ 显示流：拒绝异常点 → 插值/预测 → 平滑 → 骨架绘制
  └─ 正式流：质量门控 → 保守平滑 → 特征计算 → 阶段/规则/计数
```

- 显示流可以短时插值和预测，以减少视觉抖动；
- 正式流不得用预测出来的关键端点证明触地、最低点、完全伸展、起跳或落地；
- 正式流缺少决定性证据时返回 `UNSURE` 或未完成，不通过插值强行补齐动作周期。

### 3.2 先拒绝错误测量，再进行平滑

滤波器只能平滑输入，不能自动判断输入是否符合人体结构。优化顺序必须是：

1. 评估当前测量是否可信；
2. 拒绝结构或时序上明显异常的关键点；
3. 对可信测量平滑；
4. 对短时缺失进行受限预测或保持；
5. 超过允许缺口后隐藏对应骨段，并降低正式可观测性。

### 3.3 阶段识别由“单阈值”升级为“多证据时序判断”

阶段不能只依赖某一个膝角、髋角或肘角。应综合：

- 关节角度及变化方向；
- 身体尺度归一化后的相对位置；
- 关键点速度和加速度；
- 触地、支撑、起跳、落地等事件；
- 当前相机视角；
- 前后若干帧的阶段概率；
- 阶段最短持续时间和合法转移顺序。

---

## 4. 遮挡与骨架抖动优化

### 4.1 增加逐关节异常测量门控

在 One Euro 前新增 `LandmarkQualityGate`。每个关节计算以下质量项：

| 质量项 | 作用 |
|---|---|
| `visibility/presence` | 过滤模型明确给出的低质量观测 |
| 单帧速度 | 检测不可能的坐标跳变 |
| 单帧加速度 | 检测方向和速度突然改变 |
| 骨长变化率 | 检测肢体长度突然伸缩 |
| 左右身份连续性 | 检测左右关节交换 |
| 关节活动范围 | 检测明显不符合人体结构的姿态 |
| 邻接关节一致性 | 判断单点是否脱离所属肢体链 |

门控输出不应只有布尔值，建议输出：

```json
{
  "accepted": false,
  "quality": 0.31,
  "reason_codes": ["velocity_outlier", "bone_length_inconsistent"],
  "occlusion_state": "suspected"
}
```

第一阶段先使用规则型门控，不需要训练模型。阈值采用人体高度、肩宽、腿长等归一化尺度，避免分辨率和拍摄距离变化影响结果。

### 4.2 建立逐关节遮挡状态机

建议为每个关键点维护以下状态：

```text
VISIBLE → SUSPECTED → OCCLUDED → REACQUIRING → VISIBLE
```

- `VISIBLE`：接受测量并正常平滑；
- `SUSPECTED`：出现异常但尚未确认遮挡，暂时降低测量权重；
- `OCCLUDED`：拒绝当前测量，显示流短时预测，正式流标记不可观测；
- `REACQUIRING`：重新出现后连续确认，并将预测位置渐进融合到测量位置；
- `VISIBLE`：重新稳定后恢复正常使用。

建议使用进入/退出双阈值和连续帧确认，避免状态在边界处反复切换。

### 4.3 短遮挡预测与长遮挡降级

按遮挡时长分级处理：

| 遮挡长度 | 显示流 | 正式分析流 |
|---|---|---|
| 约 0–200 ms | 匀速/Kalman 预测，置信度轻微衰减 | 可保留状态机进度，但不确认新端点 |
| 约 200–500 ms | 预测逐渐衰减并减少骨架亮度 | 对依赖该关节的指标标记不可观测 |
| 超过约 500 ms | 隐藏对应关节和骨段 | 返回 `UNSURE`、未完成或等待重新捕获 |

具体时间应根据实际姿态 FPS 调整，并通过遮挡视频验证，不能只按固定帧数配置。

### 4.4 引入骨架结构约束

显示层可在平滑后进行轻量骨长约束：

- 使用最近一段高置信度数据估计个人上臂、前臂、大腿、小腿长度；
- 当单个末端点偏移但父关节可靠时，将其投影回合理骨长范围；
- 躯干核心点优先作为稳定锚点；
- 不用结构约束伪造正式接触或端点证据。

上传视频可进一步使用双向平滑或全序列优化，因为离线模式没有实时延迟限制。建议优先比较：

1. One Euro + 异常门控；
2. Kalman + 异常门控；
3. 前后向 Kalman/RTS smoother；
4. Savitzky–Golay 与骨长约束组合。

### 4.5 重新捕获与多人干扰

重新捕获时应验证：

- 人体中心位置连续性；
- 身体尺度连续性；
- 左右骨长与历史一致性；
- 服装/外观特征或检测框重叠度；
- 目标丢失期间是否有其他人进入画面。

如果身份连续性不足，应重置正式动作状态机，避免把两个人或两段不连续姿态拼成一次完整动作。

---

## 5. 跨视角动作阶段识别优化

### 5.1 明确每个动作支持的视角

为每个动作建立视角能力矩阵：

| 视角等级 | 行为 |
|---|---|
| 推荐视角 | 使用完整阶段、规则和计数能力 |
| 可用视角 | 使用专属阈值或特征子集，降低部分结论置信度 |
| 不推荐视角 | 只提供能够可靠观察的阶段或技术提示 |
| 不可判断视角 | 不输出强结论，提示重新拍摄 |

非推荐视角不应仅因为视角名称直接判为 `UNSURE`，而应根据决定性指标是否真实可观测进行判断。

### 5.2 建立视角条件化特征

每个动作至少拆分正面、侧面、斜侧面三类特征策略：

- 正面：左右对称性、膝轨迹、双腕同步、脚间距；
- 侧面：髋膝屈伸、躯干角度、前后位移、动作端点；
- 斜侧面：使用可靠侧选择和适度放宽的二维投影容差；
- 后方：仅保留背面能够可靠观察的对称性和位置特征。

视角既可以由用户选择，也可以由程序估计。两者冲突时保留冲突诊断，避免静默采用错误阈值。

### 5.3 使用人体中心坐标和归一化量

优先使用对视角和距离更稳定的特征：

- 相对髋中心、肩中心的坐标；
- 人体高度、肩宽、腿长和脚长归一化距离；
- 局部身体坐标中的前后、上下方向；
- 角度变化方向和局部极值；
- 相对基线的 ROM，而非单一绝对角度。

MediaPipe world landmarks 和现有三维可靠性结果可以作为辅助证据，但在验证通过前不直接替换正式二维阈值。

### 5.4 阶段输出改为概率与置信度

建议将阶段判断拆为两层：

1. `PhaseEvidenceScorer`：根据当前帧特征输出各阶段分数；
2. `TemporalPhaseDecoder`：结合合法顺序和持续时间输出稳定阶段。

示例：

```json
{
  "raw_phase_scores": {
    "descent": 0.29,
    "bottom": 0.66,
    "ascent": 0.05
  },
  "stable_phase": "bottom",
  "phase_confidence": 0.66,
  "evidence_quality": 0.78
}
```

第一阶段可继续使用规则计算分数；数据积累后，再比较逻辑回归、梯度提升、TCN/LSTM 或 Transformer 等时序模型。不要在没有独立测试集时直接用复杂模型替换现有状态机。

### 5.5 增加状态机迟滞与持续时间约束

对每个关键端点设置：

- 进入阈值和退出阈值；
- 最短持续时间；
- 最大合理持续时间；
- 允许的前驱和后继阶段；
- 遮挡期间的最大保留时间；
- 重复端点的冷却时间。

计数条件建议统一为：

```text
合法关键端点顺序完整
+ 各关键端点至少一次真实可靠观测
+ 周期持续时间合理
+ 决定性规则达到可观测性要求
= 形成动作候选或分析周期
```

### 5.6 增加个人校准

在正式分析前增加 2–5 秒校准：

- 站直姿态；
- 人体高度、肩宽、腿长和脚长；
- 左右侧基础置信度；
- 相机是否移动；
- 初始地板线；
- 可选的轻度屈伸 ROM。

校准只用于尺度和容差，不应降低动作正式标准。

---

## 6. 人工标注方案

### 6.1 是否需要更多标注

结论：需要，但重点不是立即逐帧标注全部人体关键点。

- 如果只优化显示抗抖，可以先用现有模型输出、结构约束和合成遮挡完成第一轮工程改进；
- 如果要验证和优化跨视角阶段识别，需要新增人工阶段和端点标注；
- 如果要训练新的时序阶段分类器，人工阶段标注是必要条件；
- 只有计划微调或替换姿态估计模型时，才需要大量二维/三维关键点真值。

### 6.2 优先标注字段

每个视频至少标注：

- 动作类型；
- 人员匿名 ID；
- 拍摄视角和相机高度；
- 可用视频区间；
- 每次动作或分析周期的开始与结束；
- 关键阶段区间或关键端点帧；
- `VALID / NO_REP / UNSURE / INCOMPLETE`；
- 决定性关节的 `VISIBLE / PARTIAL / OCCLUDED / OUT_OF_FRAME`；
- 是否存在器械遮挡、其他人干扰、模糊、低光或相机移动；
- 算法与人工结果的分歧类型。

### 6.3 数据采集规模建议

先进行小规模高价值试点：

- 每个动作优先选择 2–3 个实际支持视角；
- 每个视角至少 5 名不同人员；
- 每名人员包含标准、边界动作和常见错误；
- 每个动作/视角先收集 10–20 个短视频；
- 重点补充部分遮挡、完全遮挡、快速动作和阈值附近样本；
- 10%–20% 样本由第二名标注者复核。

数据集划分必须按人员分组，不能把同一个人的相邻视频同时放入训练集和测试集。

### 6.4 主动学习与难例优先

不建议平均地标注所有帧。优先进入复核队列的片段包括：

- `raw_phase` 与 `stable_phase` 长时间不一致；
- 阶段概率接近；
- 算法计数与人工计数不同；
- `UNSURE` 比例高；
- 左右可靠侧频繁切换；
- 关键点速度、骨长或置信度异常；
- MediaPipe 与 WHAM/OpenCap 参考趋势明显冲突；
- 不同阈值或不同模型给出不同结论。

### 6.5 标注不会自动改善程序

新增标注必须进入以下闭环：

```text
人工真值
  → 自动回放算法
  → 生成阶段/计数/可观测性指标
  → 定位分歧片段
  → 修改单一机制或阈值
  → 在独立测试集上验证
  → 通过后进入正式配置
```

不能只增加标注文件而没有评测与阈值校准工具。

---

## 7. WHAM 与 OpenCap 的使用建议

### 7.1 短期定位

短期继续把 WHAM/OpenCap 保持为高级离线参考：

- WHAM 用于观察三维运动趋势、全身时序和短遮挡恢复；
- OpenCap/OpenSim IK 用于观察关节运动趋势、ROM、最低点和完全伸展时刻；
- 与 MediaPipe 统一按源视频时间戳对齐；
- 不直接用三维输出替换正式计数；
- 报告明确标记 reconstructed reference，而非 Ground Truth。

### 7.2 中期验证

只有满足以下条件后，才考虑把三维证据升级为正式辅助证据：

- 在人工标注测试集上证明阶段端点识别优于或补充二维结果；
- 不同视角下结果稳定；
- 失败状态和置信度可以可靠识别；
- 时间戳对齐误差满足端点判断需求；
- 关节角定义与现有正式规则完成统一；
- 对 WHAM/OpenCap 不可用时有明确的 MediaPipe 回退策略。

### 7.3 不建议的做法

- 不把 WHAM 的同号帧直接与 MediaPipe 同号帧比较；
- 不把 OpenSim joint coordinates 当作 MediaPipe 三点夹角直接比较；
- 不因为三维结果看起来更平滑，就默认其遮挡位置是真值；
- 不让高级模式的外部依赖影响默认快速分析的可用性。

---

## 8. 评测指标与验收标准

### 8.1 骨架稳定性指标

按动作、视角和遮挡等级分别统计：

- 姿态检测率和单关节可用率；
- 关键点闪烁次数；
- 遮挡前后重捕获时间；
- 静止时归一化位移和加速度；
- 骨长变化率 P95；
- 左右侧错误切换次数；
- 遮挡结束后的最大回弹位移；
- 显示延迟和正式分析延迟。

建议第一阶段验收目标：

- 静止或缓慢动作时，关键关节归一化抖动 P95 相比当前基线下降至少 30%；
- 短遮挡造成的骨段闪烁次数下降至少 50%；
- 不增加正式端点的伪观测；
- 显示稳定改动不改变既有黄金回归集的正式计数。

### 8.2 阶段与计数指标

- 逐帧阶段 Macro-F1；
- 各关键端点 Precision、Recall 和 F1；
- 阶段边界时间误差；
- 每段视频计数绝对误差；
- 多计率和漏计率；
- `UNSURE` 的正确率与覆盖率；
- `VALID / NO_REP / UNSURE` 混淆矩阵；
- 不同人员、视角、设备和遮挡等级的分层结果。

建议阶段性验收目标：

- 推荐视角关键端点 F1 不低于 0.90；
- 可用视角关键端点 F1 不低于 0.85；
- 每视频计数平均绝对误差不高于 1 次；
- 不通过降低 `UNSURE` 门槛来虚假提高计数召回率；
- 新方案在独立人员测试集上优于当前基线。

最终阈值应根据实际标注集规模和业务容错要求重新冻结。

---

## 9. 分阶段实施计划

### 当前实施状态（2026-08-16）

- 阶段 A 已完成：新增 `pose-occlusion-baseline`，从 8 个冻结黄金视频导出 raw/filtered 关键点、置信度、归一化速度/加速度、骨长变化、raw/stable phase 和正式计数；同时从现有人工错误库整理 75 个 FN/FP/UNSURE/状态不一致难例。结果位于 `outputs/occlusion_view_phase_a/`。
- 阶段 B 已完成：实时显示流在 One Euro 前增加逐关节规则型质量门控与 `VISIBLE → SUSPECTED → OCCLUDED → REACQUIRING → VISIBLE` 状态；短遮挡采用限时匀速预测和置信度衰减，长遮挡隐藏，重新捕获渐进融合。上传回放增加前向/双向策略和消融工具。
- 阶段 C 已完成：正式分析流新增独立质量门控；held/predicted/synthetic/rejected 点不会进入依赖指标、端点或接触证据，不可观测指标输出 `null`、依赖关节和原因码。身份时间、中心或尺度不连续时，清空进行中的阶段、候选、接触与地面状态，同时保留历史 VALID/NO_REP/UNSURE 计数。
- 阶段 D 的工程改造已完成：新增 8 动作的视角能力矩阵、人体中心坐标和归一化 ROM、确定性相位证据分数，以及带迟滞、最短持续时间和合法转移诊断的统一时序解码器。视角标签只调节证据分数和建议，不会单独改变正式判定。
- 隔离验证已通过：8 个黄金样例正式计数全部通过，消融报告的阶段/计数字段逐帧不变。确定性遮挡控制序列的轨迹加速度 P95 下降 93.2%。真实黄金视频没有逐帧遮挡等级，因此未把其动态加速度变化解释为静止抖动验收成绩。
- 阶段 D 第 6 项仍受数据限制：当前黄金集没有可靠的独立人员划分与逐阶段人工真值，因此不声明已经完成人员外阈值校准；本阶段报告明确标记为 `DATA_LIMITED`，待阶段 E 补齐标注后再冻结阈值。

阶段 A/B 的主要交付物：

| 交付物 | 路径 |
|---|---|
| 诊断基线摘要 | `outputs/occlusion_view_phase_a/baseline.json` |
| raw/filtered 逐关节轨迹 | `outputs/occlusion_view_phase_a/landmark_trace.csv` |
| raw/stable 阶段轨迹 | `outputs/occlusion_view_phase_a/phase_trace.csv` |
| 难例列表与失败分类 | `outputs/occlusion_view_phase_a/hard_cases.csv`、`failure_taxonomy.md` |
| 正式计数冻结回归 | `outputs/occlusion_view_phase_a/golden_regression.json` |
| 显示稳定消融 | `outputs/occlusion_view_phase_a/display_stability_ablation.json`、`.md` |
| 正式质量与跨视角报告 | `outputs/occlusion_view_phase_cd/phase_cd_report.json`、`.md` |

### 阶段 A：建立诊断基线

目标：先知道抖动和漏阶段发生在哪里。

任务：

1. 从现有视频中整理遮挡、跨视角、漏计和多计样例；
2. 导出 raw/filtered 关键点、置信度、骨长、速度、原始阶段和稳定阶段；
3. 建立逐关节抖动、阶段端点和计数基线报告；
4. 固定现有黄金回归结果；
5. 建立按动作、视角和遮挡等级分组的失败清单。

交付物：基线 JSON/CSV、难例视频列表、失败分类报告。

### 阶段 B：优化显示抗抖

目标：显著减少遮挡时的骨架跳变，不改变正式计数。

任务：

1. 实现逐关节异常测量门控；
2. 实现遮挡状态机；
3. 加入短时预测、置信度衰减和重捕获融合；
4. 上传视频增加双向平滑实验；
5. 对比不同稳定策略的抖动与延迟。

交付物：显示稳定器、稳定性测试、消融报告。

### 阶段 C：强化正式证据质量

目标：避免遮挡或异常点误触发阶段端点。

任务：

1. 将异常测量质量传入正式特征计算；
2. 对依赖不可观测关节的指标返回缺失及原因码；
3. 明确预测点不得参与端点和接触确认；
4. 增加身份不连续时的状态机重置；
5. 验证 `UNSURE` 与未完成语义。

交付物：正式质量门控、可观测性诊断、非回归测试。

### 阶段 D：跨视角阶段改造

目标：减少二维投影视角变化导致的阶段漏识别。

任务：

1. 建立动作—视角能力矩阵；
2. 拆分视角条件化特征和阈值；
3. 引入人体中心坐标和归一化 ROM；
4. 输出阶段分数与证据质量；
5. 加入时序解码、迟滞和持续时间约束；
6. 在独立人员测试集上校准。

交付物：阶段评分器、时序解码器、跨视角评测报告。

### 阶段 E：人工标注与模型实验

目标：用人工真值决定是否需要学习型阶段分类器。

任务：

1. 完善现有人工复核记录中的 `UNKNOWN` 和人员元数据；
2. 按难例优先补充视频；
3. 标注关键端点、阶段区间和可观测性；
4. 建立按人员隔离的数据集；
5. 比较规则评分器与轻量时序模型；
6. 只有在明确提高独立测试集表现时才上线模型。

交付物：版本化标注集、标注协议、训练/验证/测试清单、模型对比报告。

### 阶段 F：WHAM/OpenCap 参考验证

目标：判断三维参考能否可靠补充二维端点，而不是直接替换规则。

任务：

1. 配置隔离的 WHAM 和 OpenCap 环境；
2. 验证时间戳对齐；
3. 对比人工端点、MediaPipe、WHAM 和 OpenCap；
4. 统计三维参考在遮挡和斜视角下的收益及失败率；
5. 决定其保持报告参考还是升级为置信度辅助。

交付物：三维对齐报告、端点误差报告、是否进入正式辅助链的决策记录。

---

## 10. 推荐优先级

| 优先级 | 工作 | 原因 |
|---|---|---|
| P0 | 建立失败样例和量化基线 | 没有基线无法判断优化是否有效 |
| P0 | 异常点拒绝与遮挡状态机 | 直接解决高置信度错误点造成的抖动 |
| P0 | 正式端点可观测性门控 | 防止显示稳定结果误导正式计数 |
| P1 | 视角条件化阶段特征 | 直接解决不同角度下阶段不清的问题 |
| P1 | 端点/阶段/遮挡人工标注 | 为阈值校准和客观验收提供真值 |
| P1 | 阶段概率与时序解码 | 提高边界和快速动作鲁棒性 |
| P2 | 上传视频双向全序列优化 | 改善离线显示和参考轨迹质量 |
| P2 | WHAM/OpenCap 对照实验 | 评估三维参考收益，不阻塞主链优化 |
| P3 | 学习型时序阶段模型 | 需要足够标注和严格独立测试后再决定 |

---

## 11. 最终建议

当前最合理的路线不是立即增加大量逐帧关键点标注，也不是直接让 WHAM/OpenCap 替代 MediaPipe，而是：

1. 先建立可复现的抖动、阶段和计数基线；
2. 在 One Euro 前加入异常点拒绝和逐关节遮挡状态；
3. 保持显示预测与正式证据隔离；
4. 将阶段识别改为视角条件化的多证据时序判断；
5. 针对关键端点、可观测性和算法分歧补充人工标注；
6. 用独立人员测试集决定阈值、时序模型和三维参考是否真正带来收益。

完成 P0 和 P1 后，程序应能明显降低遮挡骨架抖动，同时减少跨视角阶段漏识别；后续是否需要更复杂的时序模型或 WHAM/OpenCap 正式辅助，应由人工真值上的量化结果决定。
