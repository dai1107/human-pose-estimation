# 一、总体目标

这轮只做两个目标：

```
A. HYROX：
让“角度是否可靠 + 是否满足官方人体动作要求”更稳定，
减少角度跳变、边界误判和错误 NO_REP。

B. 游泳：
让左右手身份跨帧稳定，
即使单帧姿态模型偶尔左右混淆或腕带消失，也不要导致整条轨迹换手。
```

明确不做：

```
- 不采集新数据
- 不训练大型新姿态模型
- 不做 HYROX 省力/效率评分
- 不重构现有实时架构
- 不让 3D 直接替代 2D 正式规则
- 不为了游泳问题改变 HYROX ReliableSideSelector
```

------

# 第一部分：HYROX——现有数据条件下优先优化角度

你现在最值得做的不是再增加角度种类，而是把现有六路角度信息真正用起来。

目前已经有：

```
raw 2D
filtered 2D
raw 3D
filtered 3D
canonical 3D
selected-rule angle
```

并且已经具备人工角度比较工具。

所以第一件事应该是：

# Round 1：重新挖现有 150 条人工角度数据

不要先改算法。

让 Codex 新增一个分析工具：

```
tools/analyze_existing_angle_errors.py
```

直接读取已有：

```
manual_angles.json
report.json
round12 validation outputs
```

对每一个人工标注点重新计算：

```
1. raw_2d_error
2. filtered_2d_error
3. raw_3d_error
4. filtered_3d_error
5. canonical_3d_error
6. selected_rule_error
```

然后不要只统计总 MAE。

必须按下面维度拆开：

```
action
joint
left/right
camera_view
movement_phase
angle_range
landmark_confidence
2D/3D disagreement
```

例如输出：

```
Lunge / knee / 150–160°
Lunge / knee / 160–170°
Lunge / knee / 170–180°

Wall Ball / bottom
Wall Ball / descent
Wall Ball / standing
```

你现在 round12 只有总体 MAE/P95 退化信息，还不足以知道“到底哪里差”。

这一步很可能直接发现：

```
某些关节 filtered 2D 更准
某些关节 raw 2D 反而更准
最低点被平滑削掉
伸展端点有时间延迟
某些 3D 通道只在特定动作有价值
```

------

# Round 2：从“一套统一滤波参数”改成“按关节配置滤波”

你现在已经有独立 analysis/display One Euro，这是对的。

但接下来不要所有关节共用一样的响应特性。

加入：

```
analysis_smoothing:
  knee:
    min_cutoff: ...
    beta: ...

  hip:
    min_cutoff: ...
    beta: ...

  elbow:
    min_cutoff: ...

  wrist:
    min_cutoff: ...
```

原因很简单：

```
膝盖：
轨迹比较平滑，但最低点非常重要

手腕：
运动速度快，需要更灵敏

髋：
通常相对稳定，但受遮挡影响大

踝：
容易跳，需要更强稳定处理
```

这里参数不要凭感觉。

Codex 应该直接利用现有人工标注做：

```
grid search / small parameter sweep
```

例如每个关节尝试少量组合：

```
min_cutoff:
1.0
1.3
1.6
2.0

beta:
0.03
0.06
0.10
0.15
```

然后按：

```
MAE
P90
P95
event lag
```

自动选。

注意不要搜索太大空间。

几十组组合就够。

------

# Round 3：增加“端点保护”

你当前 Angle 系统最值得改的地方之一，就是：

```
最低角度
最大伸展角
```

不能被平滑算法吃掉。

增加：

```
EndpointPreservingAngleFilter
```

不要替代 One Euro。

而是在 One Euro 后增加一个事件层。

例如检测到：

```
angle velocity:
negative → 0 → positive
```

说明达到局部最小值。

则在：

```
frame -2 ~ frame +2
```

里保留最可信的真实最小角，而不是直接用平滑曲线的最低点。

同理：

```
positive → 0 → negative
```

对应最大伸展。

最终：

```
display_angle = filtered
rule_event_angle = local robust extremum
```

这对：

```
Lunge full extension
Wall Ball lowest squat
Farmers Carry elbow extension
```

都会非常有帮助。

而且不需要任何新数据。

------

# Round 4：使用“时间窗口满足条件”，不要单帧角度判定

这是我最建议你马上改的一项。

不要：

```
if knee_angle >= 170:
    full_extension = True
```

应该改成：

```
最近 5 个有效姿态帧里
至少 3～4 帧满足伸展条件
```

或者：

```
连续保持 ≥ 100~150 ms
```

具体时间仍从已有视频回放做敏感性测试。

推荐统一做：

```
TemporalRuleEvidence
```

例如：

```
condition:
knee extension

evidence:
[
  PASS,
  PASS,
  UNSURE,
  PASS,
  PASS
]

→ PASS
```

而：

```
PASS
FAIL
PASS
FAIL
PASS
```

则：

```
UNSURE
```

不要让一帧关键点跳动直接制造 `NO_REP`。

------

# Round 5：增加角度迟滞 hysteresis

和你现有 landmark enter/exit confidence 思路一样。

例如：

```
进入 FULL_EXTENSION：
需要 > T_enter

保持 FULL_EXTENSION：
允许略低至 T_exit
```

其中：

```
T_enter > T_exit
```

避免：

```
169.8
170.1
169.9
170.2
```

导致状态不断切换。

结构：

```
full_extension:
  enter_threshold: X
  exit_threshold: Y
```

不要先调整最终正式数字。

第一步只对现有阈值：

```
±2°
±3°
±5°
```

做 shadow ablation。

选择：

```
状态翻转最少
但真实 phase timing 没有明显延迟
```

的方案。

------

# Round 6：增加骨长一致性质量门

这完全不需要新数据。

人体骨段长度短时间内本来应该基本不变。

例如：

```
hip → knee
knee → ankle
shoulder → elbow
elbow → wrist
```

建立每段视频的：

```
median bone length
```

当前帧如果：

```
bone_length / median
```

突然偏差很大：

```
> 15%
> 20%
```

就说明关键点很可能错了。

此时：

```
angle_valid = false
```

或者：

```
angle_confidence *= penalty
```

不要继续正常裁决。

你 CHANGELOG 里其实已经有 bone-length audit 基础，因此最好复用现有质量代码，不要重做。

------

# Round 7：加入左右侧角度一致性检测

对于双侧对称或近似对称阶段：

```
standing
full extension
bottom
```

可以比较：

```
left knee
right knee
```

但不要简单要求相等。

用途是：

```
检查某一侧是否突然异常
```

例如：

```
left = 174°
right = 172°
正常

下一帧：
left = 112°
right = 173°
```

如果：

```
左侧 landmarks confidence 又明显较低
```

则把：

```
left angle
```

标记为 outlier。

这可以直接复用你现有 `ReliableSideSelector` 的侧质量分数，但注意：

```
这里只拿它的 quality score
不要重新定义动作左右身份。
```

------

# Round 8：让 3D 真正发挥“异常检查器”的作用

目前 3D 不能做正式裁决，这个保持。

但可以比现在用得更聪明。

增加：

```
2D/3D consistency gate
```

例如：

```
2D knee = 175°
3D knee = 172°

→ agreement
```

正常。

如果：

```
2D = 175°
3D = 128°
```

而且双方 confidence 都高：

```
severe disagreement
```

则不要：

```
2D verdict = PASS
```

直接改为：

```
angle evidence = UNSURE
```

你的 CHANGELOG 已经实现 3D Assist Mode 和严重 2D/3D conflict 降级到 `UNSURE` 的基础。

所以这轮重点是：

```
把 conflict 阈值从固定全局值
改成 joint-specific
```

例如：

```
2d_3d_conflict:
  knee: ...
  hip: ...
  elbow: ...
```

然后用现有视频自动统计正常分布：

```
median disagreement
P90
P95
```

把：

```
P95 或 robust percentile
```

作为异常门限。

这样完全不需要人工新数据。

------

# Round 9：不要用绝对像素位置判断 Wall Ball squat

这项可以直接在现有视频验证。

Wall Ball 最低点：

不要只依赖：

```
knee angle
```

加上：

```
hip center relative to knee center
```

但为了降低拍摄倾斜影响：

使用：

```
body-relative coordinate
```

例如构建：

```
pelvis center
shoulder center
knee midpoint
ankle midpoint
```

使用人体 canonical 轴：

```
body vertical axis
```

而不是直接屏幕 y。

你已经有 canonical 3D/body-relative coordinate 基础。

即使正式决策继续 2D，也可以构造：

```
canonical 2D projection
```

减少摄像机稍微倾斜的问题。

------

# Round 10：重新利用现有 30 条手机 RGB 做规则阈值 sweep

你已经有 30 条手机 RGB，而且 29 条有可用精标。

现在不要训练网络。

做：

```
rule parameter sweep
```

以现有人工结果作为固定 truth。

例如：

```
full_extension_threshold ± 2~5°
minimum_hold_ms
hysteresis_width
angle_confidence_min
2d_3d_disagreement_limit
```

每次参数组合重新 replay。

优化目标不要只有 accuracy。

建议：

```
loss =
3 × false_NO_REP
+
2 × false_VALID
+
1 × excess_UNSURE
+
0.5 × event_frame_error
```

这里：

```
false NO_REP
```

权重最高。

因为你现在目标只是：

```
可靠判断“及格/不及格”
```

最应该避免：

```
人明明完成，系统错误判罚
```

------

# 第二部分：游泳——没有新数据也能优先解决左右手身份

这部分现在**完全不建议先训练腕带 detector**。

因为你没有新数据的话，最有效的是时序 tracking。

# Round 11：先建立永久 anatomical left/right track

如果你当前代码里每一帧都是：

```
pose.left_wrist
pose.right_wrist
```

直接画轨迹：

```
立刻改。
```

新增：

```
SwimWristIdentityTracker
```

持有：

```
left_track
right_track
```

每帧只做：

```
observation → track update
```

而不是：

```
observation = identity
```

------

# Round 12：加入 constant velocity + Kalman

现有视频就可以直接优化。

每只手维护：

```
x
y
vx
vy
```

预测：

```
predicted_position
```

MediaPipe 输出后：

```
distance(
    observed_left,
    predicted_left
)
```

和：

```
distance(
    observed_left,
    predicted_right
)
```

比较。

如果 MediaPipe 某一帧把左右标签交换：

正常情况下：

```
MP left
```

反而会离：

```
right_track prediction
```

更近。

这样即可发现疑似 swap。

------

# Round 13：肩-肘-腕骨链身份约束

这一步不用任何新模型。

对于一个腕点 candidate：

分别计算：

```
left shoulder → left elbow → candidate
right shoulder → right elbow → candidate
```

检查：

```
forearm length
elbow-wrist direction
elbow connection continuity
```

然后得：

```
left_chain_cost
right_chain_cost
```

这是解决游泳左右腕错认最重要的信号之一。

尤其当左右手在画面里交叉时：

```
x 位置会交换
```

但：

```
shoulder-elbow anatomical chain
```

一般不会瞬间交换。

------

# Round 14：Hungarian assignment

每帧把：

```
MediaPipe left wrist
MediaPipe right wrist
```

先当成：

```
candidate A
candidate B
```

不要直接相信语义。

构造：

|             | Track L | Track R |
| ----------- | ------- | ------- |
| Candidate A | cost    | cost    |
| Candidate B | cost    | cost    |

cost：

```
0.40 motion distance
+ 0.35 shoulder-elbow chain
+ 0.15 MediaPipe semantic label
+ 0.10 trajectory smoothness
```

先用这一组权重起步。

之后用现有游泳视频做：

```
A/B replay
```

再调。

然后：

```
linear_sum_assignment()
```

决定身份。

------

# Round 15：增加 identity hysteresis

禁止：

```
一帧判断候选更像右手
→ 立即 L/R swap
```

应该：

```
连续 3～5 个可靠姿态帧
```

都支持 swap，才允许改。

或者：

```
new_assignment_score
```

必须比旧 assignment：

```
高一个明确 margin
```

例如：

```
confidence improvement > 0.15
```

否则：

```
retain previous identity
```

思路与你现在 HYROX ReliableSideSelector 的：

```
margin + consecutive confirmation
```

其实完全一致。当前实现已经有 0.08 margin 和连续 2 帧确认。

游泳可以复用这种状态机设计，但代码保持独立。

------

# Round 16：腕带先作为颜色证据，不训练

如果现有游泳视频里确实有腕带，就直接利用。

不要训练模型。

每个预测 wrist 周围建立：

```
ROI
```

大小根据：

```
forearm length
```

动态决定。

例如：

```
radius = 0.25 × forearm length
```

在 ROI 中统计：

```
HSV histogram
Lab histogram
saturation
```

初始化后建立：

```
left_wrist_appearance
right_wrist_appearance
```

以后不用判断：

```
这是红腕带，所以一定是左手
```

而是判断：

```
这个区域的颜色分布
更像之前 Left Track 的腕带外观
```

形成：

```
appearance_cost
```

这样即使你不知道腕带实际颜色，也可以用。

------

# Round 17：腕带 appearance model 要在线更新

不要只记第一帧。

因为游泳里：

```
入水
高光
白平衡
水下
阴影
```

颜色会变。

维护：

```
EMA histogram
```

但只在：

```
identity confidence HIGH
AND
wrist visibility HIGH
```

时更新。

例如：

```
appearance =
0.95 * old
+
0.05 * new
```

低置信度时：

```
禁止学习
```

避免一次误匹配污染整个 track。

------

# Round 18：使用现有视频跑 optical flow 作为短时补偿

不一定一开始就加 CoTracker。

你可以先使用：

```
Lucas-Kanade Optical Flow
```

OpenCV 已经有：

```
cv2.calcOpticalFlowPyrLK
```

用途：

```
MediaPipe wrist confidence HIGH
→ 初始化 tracking point

接下来 1～5 帧
→ optical flow 跟踪

下一次高可信 MediaPipe
→ correction/re-anchor
```

对于：

```
短时水花
运动模糊
单帧漏检
```

可能已经足够。

优点：

```
几乎无额外模型
CPU 轻
非常容易集成
```

所以如果 Codex 要分优先级：

```
先 LK optical flow
再 CoTracker
```

比较合理。

------

# Round 19：CoTracker 直接用现有视频，不需要训练

这一点很关键。

即使没有新数据：

```
CoTracker 本身就是预训练好的
```

所以仍然可以直接做。

不需要你自己训练。

建议新增：

```
experimental_swim_tracker:
  backend: lk
```

以及：

```
backend: cotracker
```

离线上传视频直接跑两种。

比较：

```
轨迹连续性
左右交换次数
短时丢失
视觉稳定度
```

目前不用人工完整标注也可以先做：

```
self-consistency benchmark
```

------

# Round 20：实现 forward-backward tracking consistency

没有 ground truth 时，非常有用。

例如 CoTracker / optical flow：

```
frame t
→ forward track
→ frame t+5

再：
frame t+5
→ backward track
→ frame t
```

如果回不到原始点：

```
forward-backward error high
```

说明：

```
这一段 tracker 不可靠
```

于是：

```
track_confidence ↓
```

这是一种不需要标注的质量评估。

------

# Round 21：轨迹增加身体尺度归一化

现在如果直接存：

```
pixel x/y
```

摄像机远近一变：

```
轨迹尺度就变
```

增加：

```
body_scale
```

例如：

```
shoulder width
或
torso length
```

输出：

```
wrist_x_body
wrist_y_body
```

相对于：

```
shoulder_center
或
pelvis_center
```

这样后续分析：

```
划水幅度
左右对称
轨迹形状
```

更稳定。

这也不需要新数据。

------

# Round 22：不使用 Savitzky-Golay 做实时主轨迹，但可以用于离线报告

游泳轨迹上传分析可以分：

```
causal trajectory
offline trajectory
```

实时/原始：

```
Kalman / One Euro
```

报告：

```
Savitzky-Golay
```

用于：

```
平滑位置
计算速度
计算轨迹曲率
```

但保存：

```
raw
causal_filtered
offline_smoothed
```

三份。

不要覆盖原始数据。

------

# Round 23：加入轨迹物理异常过滤

不用数据训练。

例如单帧出现：

```
wrist displacement
```

远大于：

```
forearm length
```

说明大概率跳点。

可以定义：

```
normalized displacement =
pixel displacement / body scale
```

如果：

```
远超最近速度分布
```

则：

```
reject observation
```

不要硬编码特别死的数字。

使用当前视频自身：

```
median
MAD
```

建立 robust threshold：

```
median + k × MAD
```

------

# Round 24：使用现有视频自动建立“难帧池”

虽然你现在不增加数据，但可以先自动整理。

每次游泳分析输出：

```
outputs/swim_hard_cases/
```

包含：

```
suspected_identity_swap
wrist_missing
high_motion_error
appearance_conflict
pose_tracker_conflict
forward_backward_failure
```

每个难例保存：

```
前 15 帧
当前帧
后 15 帧
```

以后等你准备标数据时：

```
直接标这些最有价值的难例
```

不用重新找。

------

# 第三部分：这轮我建议实际让 Codex 做的顺序

如果你现在就给 Codex，我不建议一次喂 24 个任务。

按照下面 **6 轮** 做更稳。

## 第 1 轮：现有角度数据重新分析

完成：

```
- 重新分析已有 150 条人工角度
- joint/action/phase/angle-range 分桶
- raw/filtered/2D/3D 对比
- 找出主要误差来源
```

不修改正式规则。

------

## 第 2 轮：HYROX Angle V2

完成：

```
- joint-specific smoothing
- bone-length quality
- temporal outlier rejection
- endpoint preservation
- hysteresis
- temporal evidence window
```

全部先 shadow。

然后现有数据 replay。

------

## 第 3 轮：HYROX 现有数据自动调参

使用已有手机 RGB + 人工标签：

```
- threshold sweep
- confidence threshold
- hysteresis width
- hold duration
- 2D/3D conflict threshold
```

以：

```
false NO_REP
false VALID
UNSURE
event error
```

作为指标。

只有明显改善才替换默认配置。

------

## 第 4 轮：游泳身份稳定

先不增加新模型。

只做：

```
- persistent L/R WristTrack
- shoulder-elbow-wrist chain
- constant velocity / Kalman
- Hungarian assignment
- identity hysteresis
```

这一轮解决：

> 左右手为什么会换身份。

------

## 第 5 轮：游泳轨迹稳定

增加：

```
- LK optical flow
- forward/backward consistency
- trajectory outlier rejection
- occlusion/lost/reacquired state
```

先看效果。

------

## 第 6 轮：腕带 + CoTracker 实验

再做：

```
ROI color appearance
+
CoTracker offline
```

只使用现有视频。

比较：

```
MediaPipe only
Pose + identity
Pose + LK
Pose + CoTracker
Pose + CoTracker + wristband
```

最后决定默认方案。

------

# 最重要的两个验收指标

HYROX 不要只看：

```
angle MAE
```

要重点看：

```
false NO_REP
```

也就是：

> 人实际完成了，系统却因为角度抖动判成违规。

现阶段这个指标比“平均角度多准 0.5°”重要。

游泳则不要先看轨迹漂不漂亮，第一指标应该是：

```
Identity Switch Count
```

也就是：

```
左手轨迹变成右手轨迹多少次
```

第二才是：

```
track coverage
reacquisition
trajectory jitter
```

因为只要左右身份会交换，后面所有游泳轨迹分析——划水周期、手腕路径、左右对称性——都会失去意义。

所以以你目前的数据条件，我最推荐的最短路线实际上是：

```
HYROX
已有人工角度
→ 误差分桶
→ 按关节滤波
→ 端点保护
→ 时序证据
→ hysteresis
→ 现有标签自动调参

Swimming
MediaPipe wrist
→ persistent identity
→ shoulder/elbow anatomy
→ Kalman
→ Hungarian
→ hysteresis
→ LK optical flow
→ CoTracker
→ wristband appearance
```